# 2026-08-20 10:30 JST ｜ サイクルI: H1 Phase 2 light Structured Output schema

要求: Anthropicのschema compilation 400を避けるため、H1 Phase 2だけをflat transport schemaへ分離する。
実行: root 1 object・19 required fieldsのH1 schema、action別whitelist normalizer、H1専用transport promptを追加した。
実行: H1の実送信と予約計算が同一prompt/schema helperを使い、M3/H3は従来canonical schemaを維持するようにした。
検証: 13 actionのtransport→canonical→既存parser変換、unused field拒否、JSON-in-string異常系をfakeで固定した。
検証: 新しいH1予約は$0.026800000、full pytestは722 passed, 4 warnings、git diff --checkは成功した。
発見: 軽量schemaはoptional 0 / union 0 / object 1 / 1,280 bytes以下だが、provider compile可否は別承認の実APIでのみ確定する。
