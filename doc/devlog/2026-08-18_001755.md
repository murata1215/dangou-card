### 2026-08-18 00:17 JST | サイクルX.X: 18モデル疎通テスト基盤 Phase 1 実行（弱6モデル・実API）

**要求**: 前サイクルで実装した `scripts/model_matrix.py` の Phase 1（超軽量疎通）のみを、弱（L）ティア
6モデル（Anthropic/OpenAI/Google/xAI/Moonshot/DeepSeek）に対して実APIで実行し、疎通結果と
コストを表にまとめてほしいという依頼。Phase 2/3は絶対に実行しない。

**実行**: `bash scripts/run_matrix.sh --phase 1 --tier weak --run-id run_20260817_235518 --resume
--max-tokens 256 --retries 0 --max-calls 6 --max-cost 0.02 --max-cost-per-model 0.01
--max-cost-total 0.05` をデタッチ起動。実行順は安い順（L6→L2→L3→L4→L5→L1）。既定の
`--max-tokens 64` ではなく256に引き上げた（thinking制御のないxAI/Googleの偽陰性回避）。

**結果**: **6/6モデルすべて疎通成功**（API成否・JSON成否とも全て✓）。所要時間は約18秒
（最速719ms=Gemini 3.5 Flash-Lite、最遅8,039ms=Claude Haiku 4.5）。`phase1_calls.jsonl`が
ちょうど6行（1モデル1コール厳守）。`state.json`: `phases.1.status=="done"`、
`budget.calls_made==6`、`spent_usd=$0.000548`。Phase 2/3は未実行のまま
（`phases.2`/`phases.3` とも `pending`）。予算上限（$0.02/$0.05）は一度も発動せず。

**発見**: xAI (grok-4.3) が `reasoning_tokens=229`・`cache_read_input_tokens=192` を伴う応答を
返し、`run_phase1` のコスト計算が `total_tokens`/`cache_read_input_tokens` を `estimate_cost()`
に渡していないため、記録コスト$0.000294に対し補正後コストは$0.000665と約2.3倍のズレが
発生した（6モデル合計では記録$0.000548 vs 補正$0.000919）。また前サイクルで既知だった
「API返却モデル名が現行アダプタでは取得できない」制約も実測で再確認した
（`adapter.complete()` が `response.model` を破棄する設計のため）。いずれも指示どおり
その場では修正せず、結果としてのみ報告した（`.devrelay-output/phase1_weak6_report.md`）。

**次段**: Phase 2（談合カードJSON互換、Phase1通過分=弱6モデル全て対象）は別サイクルで実施予定。
