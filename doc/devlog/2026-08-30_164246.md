# 2026-08-30 16:42 JST ｜ サイクル8.5: v0.8 本戦の結果分析

## 要求

v0.8 レギュレーション改訂（契約無料化・提案失効・解除条件・倍掛け順序/DOUBLEガード・公開情報の提示・
便益/コスト対称化）を投入した初の LLM 本戦 `trial_C_l12_r12_v08_20260830`（seed 1202、seat_map は
v0.7 `trial_C_l12_r12_20260828` とバイト同一）の結果を、`doc/uso8000000_dangou_card_spec_v0_8_regulation_season2.md`
§12 の計測項目1〜10に沿って v0.7 と比較分析する。分析のみ。コード・テスト・設定は無変更、LLM API
呼び出しなし、`logs/` 配下は読み取り専用。

## 実行

- `/tmp/cycle85_work/agg_events.py` を新規作成し両trialの `game01_events.jsonl` を集計（イベント種別
  カウント・NEGOTIATION_ACTION内訳・BANKRUPTCY/FORCED_LIQUIDATION/ELIMINATION/SURVIVAL_CHECK・契約
  ライフサイクル・card_trade・DOUBLE_UP系・MARKET_RESULT・COMMIT・ACTION_ERROR 等を全件ダンプ）。
- `/tmp/cycle85_work/parse_llm_logs.py` を新規作成し、両trialの `llm_logs/game01_P*_llm_calls.jsonl`
  を本番の `llm.response_parser.parse_response()`/`extract_json()` で再パース。フェーズ別JSON成功率・
  reason_category分布・新表示語彙（高騰/倍掛け/借入/公示/契約/雛形）の席別出現・target_market vs
  COMMIT のミスマッチ率（strategy JSONフィールド方式、および v0.7 §8-1 と同一正規表現による
  broadcast/dmメッセージ本文方式の両方）・コスト/入力トークンを集計した。
- P06（唯一の契約違反脱落、R2）の R1〜R2 全ターンの negotiation/commit ログを llm_logs から抽出し、
  `contract_cancel`/`repay`/`transfer` を一度も試みずに脱落した経緯を時系列で確認した。
- `honsou_l12_r12_v08_20260830.log` の memory拒否4件・切り詰め3件・salvage1件の警告を
  `extract_memory_with_status()`/`normalize_memory_with_truncation()`/`parse_final_reflection()`/
  `parse_post_game_reflection()` の再実行で player/round に突合した。
- 以上を `doc/analysis/trial_v08_l12_r12_20260830_analysis.md` にまとめた。

## 検証

- `git status --short` を確認し、`doc/analysis/trial_v08_l12_r12_20260830_analysis.md`・本devlog・
  `INDEX.md` 追記の3件のみであることを確認する。
- `git diff --stat engine/ llm/ tests/ bots/ scripts/ viewer/` が空であることを確認する。
- `git status --short logs/` が空であることを確認する（入力ログ無改変）。
- 集計スクリプト2本は `/tmp/cycle85_work/` にのみ存在し、リポジトリに混入していない。
- LLM API呼び出し0件（`parse_response`/`extract_json` はテキスト解析のみ、`llm/adapters.py` 非import）。

## 発見

- 契約は無料化・可視化の狙いどおり活性化: `contract_propose` 6→23、`contract_sign` 4→19、
  `CONTRACT_EXPIRED` は新設どおり5件発生。一方 `card_trade_propose` は1→0とむしろ消滅し、想定外だった。
- `DOUBLE_UP_BLOCKED` が実戦で1回発火（P05 R6、預託後現金が最低返済額を割り込む直前で阻止）。
- 生還者は5/12→6/12に改善したが、v0.8で唯一の契約違反脱落（P06 R2）が発生。原因は
  `TYPE_A_FAILURE`「Atomic execution failed - insufficient free cash」で、cash_before=2,546,250円
  という一見余裕のある現金でも Free Cash 制約で型A決済に失敗する構造的問題（サイクル9.0の
  Free Cash 棚卸しの発見と直結）。P06はR2交渉中に支払義務を認識する発言をしながら`contract_cancel`
  等の回避行動を一度も取らなかった。
- target_market/COMMIT ミスマッチ率は50.8%→34.9%（JSONフィールド方式）、51.4%→41.1%
  （v0.7 §8-1と同一正規表現のメッセージ本文方式）と、両定義とも改善。
- reason_category はv0.8で新設された任意フィールドで、v0.7の応答には一切出現しないことを確認した。
- memory失敗8件中7件（拒否4件・切り詰め3件）はplayer/roundまで完全一致で特定できたが、salvage1件
  （fields=['emotion','defeat_cause','comment']）は再パースでは確証を得られず、未特定として正直に
  記載した。

commit/push未実施。
