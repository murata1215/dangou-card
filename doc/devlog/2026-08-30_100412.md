# 2026-08-30 10:04 JST ｜ サイクル8.3: v0.8 プロンプト文面の仕上げ（レビュー17点）

## 要求

サイクル8.2で一括修正した `llm/prompt_builder.py` の実出力（`doc/prompt_dump/actual_prompts_20260830_v2.md`）
をレビューした結果、LLMの判断を実際に誤らせうる不正確な記述5点（F1/F2/F12/F13/F16）と、事実と食い違う
文言・表示12点（F3〜F11/F14/F15/F17）が見つかった。いずれもengineの挙動自体は正しく、プロンプトの記述
だけが間違っていた。本サイクルは `llm/prompt_builder.py` の局所修正・テスト追加・ダンプv2の同名上書き
再生成のみを行い、`engine/` は一切変更しない方針で実施した。

## 実行

- Step1〜3で `llm/prompt_builder.py` を修正: トレード提案の現金表示を「渡す側」基準＋支払者明記に統一
  （F1）、署名待ち契約の手札なし警告を今R/将来Rで文言分岐（F2）、`double_up` 次R見通しの結論行を没収額・
  強制返済不能・回避必要額を明示する形へ置換（F12）、次R型A義務の払出原資／不足額を明示（F13）、
  `double_up_blocked` 通知をengineの実判定（預託後現金 = `cash - eligible_prize`）に合わせて「受領後」の
  誤記を除去（F16）。F3/F4/F8〜F11/F15/F17（文言・表示の是正12点）とF5〜F7（system prompt微修正3点、
  +10字）も同時に実装した。
- Step4でテストを追加・更新。F1×2/F2×2/F12×2/F13×2/F16×1を含む新規ケースと、`tests/test_prompt_v08.py`
  の既存文言更新、`tests/test_contract_cancel.py`（`_render_contract_notice_block`のシグネチャ変更に伴う
  更新）、`tests/test_phase2_schema.py`（system prompt長変化に伴うH1予約額の七度目更新）を修正。
- Step5でサイクル8.2の生成スクリプト（`/tmp/dumpgen2/gen.py`）が既に消滅していたため、恒久的な
  `scripts/dump_prompts.py`（`llm/adapters.py`をimportしない読み取り専用ツール）として書き直し、旧v2の
  全文からダミー入力を逆算して再構成。ダミー値の変更はF14実演のためP05の倍掛け預託へ`success_round=8`
  を設定、F1の受諾者払い分岐を実演するためトレード`T_5C1A`の`cash_amount`を正から負へ反転、契約無料化後
  の文脈に合わせてメッセージ文言を1件変更の3点のみに限定し、`doc/prompt_dump/actual_prompts_20260830_v2.md`
  を上書き再生成した。

## 検証

- `uv run pytest -q` は1547 passed（サイクル8.2完了時点の1533から+14。Step4のテスト追加・更新のみによる
  もので `engine/` は無変更）。
- `git diff --stat engine/` は空、`git status --short logs/llm/` も差分なし（実LLM APIを一切呼び出して
  いないことの担保）。
- `git status --short` の変更は `llm/prompt_builder.py` / `tests/test_contract_cancel.py` /
  `tests/test_model_matrix.py` / `tests/test_phase2_schema.py` / `tests/test_prompt_v08.py` /
  `doc/prompt_dump/actual_prompts_20260830_v2.md`（既存ファイルの変更）と `scripts/dump_prompts.py`
  （新規）のみ。
- 再生成前にバックアップした旧v2との`diff`を全行目視確認し、変化した行がF1〜F17の文言修正・3点のダミー値
  変更・「この文書について」節の補遺追記のみであることを確認した（想定外の差分なし）。
- system prompt文字数は8,177→8,187字（F5+1字/F6±0字/F7+9字、想定どおり）で8,300字の上限を維持。
  `tests/test_model_matrix.py`のH1予約額（M3: 0.0167085 / H3: 0.027078、いずれも0.03未満）は実行結果に
  合わせて実測値へ更新した。

## 発見

- `double_up_blocked`通知の従来文言「獲得賞金受領後の現金が強制最低返済額未満」は誤りで、engineの実判定
  （`engine/game.py:1302`）は預託後の現金（`cash - eligible_prize`）と強制返済額の比較だった。F16でこの
  ずれを是正した。
- `total_prize_budget`はR12の3倍市場高騰を含めて算出されている（`engine/game.py:2052-2059`）ため、
  「システム基本賞金の合計」という従来ラベルは不正確だった。F9で「全12R・R12の3倍込み」に修正した。
- サイクル8.2の生成スクリプトは`/tmp`配下に置いていたため次セッションで消滅しており、旧v2全文からの
  逆算による再構築が必要だった。今回`scripts/dump_prompts.py`としてリポジトリに恒久化したため、以後の
  再生成ではこの手戻りが発生しない。

commit/push未実施。
