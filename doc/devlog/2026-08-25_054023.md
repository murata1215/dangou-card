# 2026-08-25 05:40 JST ｜ 全面監査: clean L12×R12（trial_C_l12_r12_20260824）実戦ログ

## 要求

Acceptance Test の raw logs（1,697イベント / 1,449 LLMコール / prompts / messages / finance）を
現行コード `ac844b3` と突き合わせ、読み取り専用で全面監査する。目的は
(1) Cycle 1〜4 修正の実戦回帰確認、(2) 実際に発現した system defect の抽出、
(3)「ライアーゲーム的知能戦」として成立しているかの評価、(4) Cycle 5 の要否判断。

## 実行

コード変更 0行・実 API コール 0件・再走なしで、イベント再集計、prompt/response の
文字列走査、両 run の system_prompt 実 diff、git 履歴による仮説検証を行った。

## 検証

Cycle 1〜4 の回帰ゲートは**全PASS**。AUTO_COMMIT schema は `AUTO_COMMIT=1 / FAILURE=0`・
missing=`[]`（前回は6項目欠落＋reason上書きで FAIL）。死者宛アクションは前回 25件 → **0件**。
prompt 秘匿は他者 cash 一致が30件出たが**全て偽陽性**で、読者自身の新設 `## Finance` 欄の金額が
他者 snapshot と数値一致しただけ（`Finance` 欄に他 PlayerID 参照 0件）。Cycle 4 の
anonymous self-marker は今回初めて実データ3件（R1T1 P02 / R1T1 P07 / R6T10 P06）で検証でき、
送信者のみ `[匿名/あなたが送信]`・第三者は `is_mine` キー自体なし・ラウンド跨ぎの owner 残留 0 を確認。
今回の脱落7件はすべて財務判断の帰結で、**system 事故による死は 0件**。

## 発見

**D1 [S1] 契約系アクションの実戦消滅（今回の主題）。** 同一 seed で `contract_propose` 11→**0**、
`contract_sign` 11→**0**、`card_trade_propose` 1→**0**、`bounty_*` は両 run 0。
funnel 解析で **ATTEMPT=0**、つまり engine の拒否でも parser 落ちでもなく AI が JSON を出していない。
モデル出力中の「契約」言及は 357/1130 (31.6%) → 72/1155 (**6.2%**)、「型B」言及 89 → **7**。
リスク言及も比例して落ちており、「検討して却下した」のではなく**思考の俎上から消えている**。

原因は engine ではなく **prompt の risk-framing / salience 欠陥**。両 run の system_prompt を実 diff すると、
追加された行は失敗コスト側のみだった —— `prompt_builder.py:726`「不成立アクションも枠を消費する」、
contract_cancel ブロックの「同じ相手と両立しない契約を二重に結ぶと必ず型B違反で脱落します」、
`:799`「脱落者を指定すると必ず不成立になり、アクション枠だけを失う」、
`:565`「カードトレードは必ず不成立になり、アクション枠だけを失います」。
**契約を結ぶ便益を述べる文は1行も追加されていない。** 加えて negotiation の user_prompt は
2,284字 → 4,321字（+89%）に伸び、最も recency の強い末尾に `## 残りアクション枠: n/10` が置かれ、
その直後の締め指示（`:1079-1083`）は「dm/broadcast/transfer/repay/pass 等」しか列挙しておらず、
S2 の6アクションが不可視のまま。

なお探索段階で立てた「アクション枠制約が新規に入って契約が割に合わなくなった」という仮説は
**誤りと確定**した。`negotiation_max_actions: int = 10`（`engine/config.py:121`）は
initial commit `8806964` から存在し、enforcement（`engine/game.py:386`）も不変、
枠飽和（10/10到達）も両 run とも 22 player-rounds で同一。新しいのは**開示文言だけ**（`49071e4`）。

その他の defect: **D2 [S2]** 締め指示のアクション列挙が不完全（`prompt_builder.py:1079-1083`、1行修正で済む）。
**D3 [S2]** R0 借入額が生存を過度に規定しており、最小借入120万の P06/P07/P09 が R6 に
まったく同一の required 109,345 / debt 765,411 で同時破産した。
**D4 [S3]** `ELIMINATION` イベントが **0件**で、脱落は `GAME_END.eliminated` /
`FORCED_LIQUIDATION` / `BANKRUPTCY` から層をまたいで読むしかない（前回 run も7件中2件のみ発火）。
S0（勝敗・秘匿・金銭状態を壊す欠陥）は 0件。

知能戦としては口約束ベースで実際に成立していた。単一市場を公言した player-round 88 のうち
公言通りが 55・**公言と違う市場に commit したのが 33（37.5%）**。双方向 DM が成立した pair は 73、
うち片側が単一市場を約束した 28件は **履行16 / 裏切り12（裏切り率43%）**。
「企み→交渉→信/疑→履行/裏切り→結果」の因果連鎖は **28回完了**した。
ただし契約が0件＝型A/型B違反も0件のため、**裏切りが常に無コスト**。
また P12 は 83回 pass してほぼ無交渉のまま3位で生還しており、受動が報われる構造も見えた。
`bounty_*` 0件 / `card_trade_*` 0件 / `anonymous_broadcast` 3件は AI が選ばなかった結果であり、
engine 拒否も schema 欠落も無い。**欠陥ではなく観測不足**で、D1 修正後の再走まで可否は判定不能。

8軸採点は 情報の非対称性 4 / 嘘・ブラフの余地 4 / **契約と裏切りの意味 1** / 相手を読む 3 /
資金・カード・契約のトレードオフ 2 / AIの自律判断基盤 4 / 事故に汚されない公平性 5 / 観戦性 3。
**総合 CONDITIONAL GO** —— 基盤（engine・秘匿・ログ・事故率）は GO 水準だが、
S2 の看板機構が実戦で0回しか使われずゲームが資金管理ゲームに縮退している。**D1 の解消を条件に GO**。

優先度は、Cycle 5 必須が D1（失敗コスト文は残しつつ契約・報奨・匿名・カードトレードの**便益**を
同格で明記し、「必ず型B違反で脱落します」を回避条件とセットの中立表現に調整、
`:565` の card_trade 警告に適用条件を明示 → **同一 seed 1202 で再走し
`ATTEMPT(contract_propose) > 0` を主要指標に切り分け**）。改善候補が D2（1行修正）、
D3（仕様判断が必要）、D4（脱落時に必ず `ELIMINATION` を発火させ監査を単一 event 型に集約）。

本監査は読み取り専用で、コード変更0行・実API0件・再走0回。D1〜D4 の修正は Cycle 5 として別途承認を得る。
詳細レポート全文は `.devrelay-output/l12_r12_full_audit_2026-08-25_054023.md`。
