# 2026-08-22 12:37 JST ｜ サイクル: Handover Memory 3000文字化+3バグ修正

要求: 事前に承認済みのPlan（`/home/uso8m/.claude/plans/quirky-sprouting-hennessy.md`）に従い、Handover Memoryの唯一の文脈継承路が3つの実装バグ（WRAPPER_LEAK/WRONG_PAYLOAD/TRUNCATED）で実質破壊されていた問題を修正し、上限を1000字→3000字化する。ゲームルール・勝敗ロジックは対象外。LLM API送信・trial実行・Viewer再起動・commit/pushは今回のスコープ外。

実行: `llm/response_parser.py`に`extract_memory_with_status()`を新設し、フェンス剥がし→JSON parse→memoryキー判定→復旧不能時のみ空文字、の3層判定に置換した（`_strip_code_fence`/`_recover_memory_field`）。`memory`キーが無いJSON（`{"strategy":...}`等）を丸ごと採用していた旧設計のfall-throughを廃止し、`rejected_no_memory_key`として空文字を返すよう反転（既存の「空文字での上書きはしない」セーフティにそのまま乗るため`llm_agent.py`の判定ロジックは無変更）。`normalize_memory_with_truncation()`で意味境界優先の縮約（`_shrink_to_boundary`、keep_ratio=0.85）を実装し、ハードカットは境界なし時のみのフォールバックとした。`engine/config.py`に`HANDOVER_MEMORY_MAX_CHARS = 3000`を単一ソースとして新設し、`GameConfig.memory_max_chars`既定値と`viewer/log_parser.py`の表示上限を統一。`build_reflection_prompt()`に切り詰め優先順位ヒントを追記。`_update_last_log_memory()`を新設し`_update_last_log_emotion`と同じ後付けログ更新パターンでobservabilityフィールド6種を記録。既存のREADME/仕様書のメモリ上限記述も更新した。

検証: `tests/test_memory.py`に新規テストケースを追加し既存ケースは全て維持、64/64 PASS。全体回帰 `uv run pytest tests/ -q` は808/808 PASS（警告4件は既存・無関係）。読み取り専用の新規スクリプト`scripts/reeval_memory_fix.py`でR12実ログ（`logs/llm/trial_C_l12_r12_20260822/game01`、reflection応答105件）に修正後ロジックを直接importして適用し、Before/After比較を`.devrelay-output/memory_fix_reeval_2026-08-22.md`に出力した。`git status --short logs/`が空であることを確認し、既存ログへの書き込みが一切無いことを確認済み。

発見: 実測でWRAPPER_LEAK 39件（37%）、WRONG_PAYLOAD 3件、1000字上限ヒット52件（50%）が実在し、修正後はWRAPPER_LEAK 0、WRONG_PAYLOAD 0、3000字上限ヒット1件（102件中）、fallback（旧Memory保持）3件に収束した。parse_status内訳は`ok`=66/`ok_recovered`=36/`rejected_no_memory_key`=3。保存文字数はmean 704→995、median 985→1037、p95 1000→2103に改善。fallback 3件（P05 R5/R7, P12 R3、いずれもgemini-3.5-flash-lite）は全て前ラウンドのMemoryを維持しており文脈が途切れていないことを手動突き合わせで確認した。縮約発生は102件中1件のみ（P04 R8: 3470字→2992字、文末`。`で終端）で文中切断は発生していない。コスト影響は実測tokens/charベースの試算で現実的ケース+2.4%、最悪ケース（全モデルが3000字上限に張り付く）でも+19.0%（$5.99→$7.13）に留まり、既存の`game_cost_cap_usd=15.0`の範囲内であることを確認した。

副次的な発見として、分析スクリプト自作の`_classify_old()`分類ロジックに、fence付きWRONG_PAYLOADケース（3件とも` ```json `で始まる）がWRAPPER_LEAK判定に先取りされる分類優先順位バグがあり、Before側の内訳表示のみに影響していた（本実装のAfter側ロジックには影響なし）。`"strategy"`キー存在かつ`"memory"`キー不在を先に判定するよう修正し、WRAPPER_LEAK 39/WRONG_PAYLOAD 3の正しい内訳に修正・再生成した。

次回R12を回してよい判定基準（Plan記載の10項目）は全て満たしている: 全テストPASS、WRAPPER_LEAK/WRONG_PAYLOAD=0、3000字上限ヒット1/102、fallback 3件全てで旧Memory非空、手動突き合わせ5件で先頭が```/`{"memory`/`{"strategy`で始まらないことを確認、縮約ケースの末尾が文中断でない、logs/無改変、マジックナンバー1000の残存なし、simulate.py無影響（memory_enabled既定False）、コスト試算が予算内。
