# 2026-09-22 10:19 JST ｜ サイクル10.13: DR席 timeout 180秒化

## 要求
`doc/analysis/l12r12_dr_1201_timeout.md`（サイクル10.12）の結論を受け、DevRelay 8席の `timeout_seconds` を契約上限の120秒→180秒へ最小変更する依頼。再試行ロジック・並列化・thinking設定・API席timeoutの変更、engine/等の既存未コミット差分への接触、子エージェント使用は禁止。

## 実行
`llm/models.py` のDR_*8席（DR_FABLE/DR_OPUS/DR_OPUS48/DR_SONNET5/DR_TERRA/DR_SOL/DR_HAIKU/DR_LUNA）の `timeout_seconds=120` を `180` に変更（8箇所）し、変更経緯コメントを追加。`llm/providers/devrelay_http.py` は `timeoutS = model_info.timeout_seconds`、httpxタイムアウト = `timeout_seconds + HTTP_TIMEOUT_MARGIN_S(30)` の派生計算のため無変更（自動的に210秒へ追従、`MAX_TIMEOUT_S=180`の上限チェックも通過）。`tests/test_devrelay_http.py` の `make_model()` フィクスチャと関連アサーション2件（`timeoutS==120→180`、`client.timeout.read==150.0→210.0`）を更新し、新規テスト `test_registry_dr_seats_send_timeout_180` を1件追加（実レジストリのDR席8本すべてでMockTransport経由 `timeoutS==180`・httpx read timeout==210.0 を検証）。

## 検証
`uv run pytest tests/test_devrelay_http.py tests/test_registry_18.py -v` で全58件PASS。実機スモークを `DR_OPUS48`（seatKey=P02）で1回のみ実行（`/tmp/c1013/smoke_capture.py`、repo外、httpx.Client.postをラップして送信bodyを観測）し、`timeoutS=180` が実際に送信され `[OK]`（latencyMs=3187、応答`{"answer":"はい"}`）を確認した。

## 発見
504対策の本命検証（次試合での504件数減少）はこの変更だけでは分からない。次回l12r12級の本戦を回した際に504件数を再集計する必要がある。変更は`llm/models.py`・`tests/test_devrelay_http.py`のみで、`engine/*`等の既存未コミット差分（約70件）には触れていない。`git push`未実施。
