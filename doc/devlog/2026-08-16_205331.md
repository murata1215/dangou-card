# 2026-08-16 20:53 JST | L7（Gemini 2.5 Flash-Lite）追加 — 明日朝の6体→18体負荷試験の準備

明日朝、Season 2最大想定の18体でengine/llm/logging/viewerが耐えられるかを、安価3モデル構成の低コスト負荷試験で確認する。その準備として、ユーザー指定の単価（input $0.10 / output $0.40）でGemini 2.5 Flash-Liteを`L7`として新規登録した。

**要求:** L7をロースター仕様書v1.0の12モデルとは別枠として追加し、明日使う6体（`L7,L7,L6,L6,L2,L2`）・18体（L7×6/L6×6/L2×6）ロースターがそのまま動くこと。既存のL3（Gemini 3.5 Flash-Lite）は絶対に壊さない。今夜は試合起動・API呼び出し一切禁止。

**実行:** `llm/models.py`に既存のGemini経路（`GEMINI_OPENAI_BASE_URL`・`adapter_type="gemini"`）をそのまま再利用する形でL7を追加。新規のreasoning/thinkingオプションは今回追加しない。テスト7件（登録内容・単価・アダプタ選択・model_id逆引き・コスト計算・L3/M3不変回帰・ロースター文字列解決）を追加し、README/changelog/v0.8仕様書を整備した。

**検証:** `pytest tests/test_llm.py`のL7関連11件PASSED、全テスト351件PASSED、失敗ゼロ。API呼び出しは一切発生していない（`create_adapter()`でインスタンス化するのみで`complete()`は呼ばない設計のテストのみ）。

**発見:** `scripts/llm_trial.py`の`--roster`解析は`.strip()`していないため、カンマの後にスペースを入れると`Unknown model`エラーになる運用上の落とし穴を再確認し、明日使う正確なロースター文字列（スペース無し）をそのまま固定するテストとして残した。これにより将来ロースター文字列を書き換える際に空白混入があればCIで検知できる。

**出力:** `llm/models.py` / `tests/test_llm.py` / `README.md` / `doc/changelog.md` / `doc/uso8000000_dangou_card_spec_v0_8_season2.md`
