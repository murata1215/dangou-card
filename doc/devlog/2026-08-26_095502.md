# 2026-08-26 09:55 JST ｜ サイクル5.2: 締め指示に対応付け説明句を追加

要求: Cycle 5.1実弾A/B（baseline0/81→patched2/81、p=0.50有意差なし）で締め指示のアクション全列挙が行動を多様化（pass16→6）させたのに契約系への変換をほぼ起こさなかったことから、「口頭合意→どのアクションで拘束力を持たせるか」の対応付けが締め指示に1行も無いことをD1残存要因と仮定し、llm/prompt_builder.pyの`_available_negotiation_actions()`のavailable側各アクションに15字前後の中立な説明句を括弧で追加する。engine/は変更せず、system_promptも変更しない（Phase2予算への影響を避けるため）。LLM API呼び出し・ゲーム実走・git書き込み系は一切行わない。

実行: `_available_negotiation_actions()`のavailable側append 13箇所を`_add(name)`ヘルパー経由の説明句付きに変更（`dm（特定の相手に非公開で送る）`等14種、`contract_propose（口頭合意を拘束力のある正式契約にする。発行料10万円）`のfeeは`config.contract_fee`/`config.anon_broadcast_fee`から算出しハードコードなし）。「選べないもの」側の8短句・判定ロジック・呼び出し側は無変更。tests/test_cycle5_prompt_salience.pyに新規クラス`TestActionDescriptionMapping`でテスト5件追加（各アクションに説明句が付くこと／助言語彙が混入しないこと／contract_proposeが「口頭合意」「正式契約」「発行料」を含むこと／fee変更に追随すること／「選べないもの」側が不変であること）。

検証: `uv run pytest tests/ -q` 1299→**1304 PASS**（新規5件のみ増、既存28件・他ファイルは無変更）。`uv run python scripts/dry_run.py` 578イベントで正常完了。`scripts/replay_probe.py --dry-run` 実測で**0行変更のまま**27/27の締め指示が説明句付きへ自動追従（`_build_patched_closing()`が`_available_negotiation_actions()`の戻り値をそのままjoinしているため）、API呼び出し0件。negotiation user_promptの長さ実測: worst fixture 5,535→**5,714字**（+179、予算≤6,400に対し余裕686）、std fixture 2,553→**2,685字**（+132、予算≤3,550に対し余裕865）。system_promptは**7,079字で不変**を実測確認、Phase2予算・scripts/model_matrix.py関連テストへの影響なし。禁止語5系統（推奨/有利/おすすめ/使うべき/積極的/活用/すべき等、JUDGMENT_WORDS、ADVICE_WORDS×2系統）と`tests/`の`not in prompt`アサーション58件（ユニーク45種）に対する説明句全文の総当たりで**衝突0件**。`git diff --stat`にengine/の変更は1行も出現せず、tests/の変更はtest_cycle5_prompt_salience.pyのみ（他はCycle5既存差分のまま）。

発見: (1) 契約propose等の説明句追加でも既存のD2系「action in available」部分一致テスト8件はそのまま通る（説明句が付いてもアクション名部分文字列が保持されるため）。(2) Cycle5.1のdevlog重複整理: `2026-08-26_084535.md`（見出し`# Cycle 5.1 — …`が規約非準拠、情報量少）を削除し`2026-08-26_084625.md`（見出し規約準拠、検証8項目、baseline側CI・P06 R6t8副次変化・truncation因果考察を含み情報量多い）を残した。INDEX.mdから08:45行を1行削除し時系列（06:45→08:46→09:55）を復旧。削除ファイル独自の情報「baselineのsample別契約言及率40.7%/37.0%/18.5%」はここに転記保全する。(3) 未解決: 5.1 devlog 2本が参照する`.devrelay-output/replay_probe_report_*.md`（084535.md分・082742.md分とも）は実測で`.devrelay-output/`が空のため実在しない。本Cycleの編集許可範囲外のため084625.mdの本文は書き換えず、事実のみここに記録する。実API0件・commit/push無し・本走/次Cycle着手なしで停止。
