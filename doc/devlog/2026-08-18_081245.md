### 2026-08-18 08:12 JST | H2 (gpt-5.6-sol) Phase 1 再テスト: max_completion_tokens対応でHTTP最大1回のまま成功

**背景:** 前回の強6一斉実行（2026-08-18 07:05 JST）で H2 `gpt-5.6-sol` のみ `HTTP 400 Unsupported parameter: 'max_tokens' is not supported with this model. Use 'max_completion_tokens' instead.` で失敗した。原因は `OpenAICompatAdapter` が全 openai_compat モデルへ一律 `max_tokens` を送っていたこと。H2はreasoning系で `max_completion_tokens` を要求する。

**設計方針（3回のプラン修正を経て確定）:**
1. モデル固有パラメータはデータ駆動: `ModelInfo` に `max_tokens_param: str = "max_tokens"` / `supports_temperature: bool = True` を追加。H2のみ両方を上書き。他17モデルは既定値のままでペイロード完全不変
2. `OpenAICompatAdapter.complete()` のペイロードを1つのdictに統合し、`self.model_info.max_tokens_param` をキーに使うことで `max_tokens`/`max_completion_tokens` の同時送信を構造的に排除
3. `supports_temperature=False` のモデルは最初から `temperature` を送らず、既存のtemperature 400フォールバックも到達不能にする
4. **リトライ責務は Phase 1（`scripts/model_matrix.py`）に一本化**: `_call_once_phase1` は常に `create_adapter(model, max_retries=0)` でアダプタを生成し、アダプタ内部リトライ・OpenAI SDK内部リトライを常に無効化。再試行は `_call_once_phase1` の外側ループ（`--retries`）だけが担う。これにより `--retries N` ⇔ HTTP最大 `N+1` 回が1対1で対応し、三重リトライ（最悪 `(N+1)^3`）を防ぐ。Phase 1以外（ゲーム本体・Phase 2/3・`model_smoke.py`）は `create_adapter()` を引数なしで呼ぶため従来のリトライ挙動（アダプタ内部2回＋SDK既定2回）を完全維持

**変更ファイル:** `llm/models.py`（2フィールド追加＋H2上書き）、`llm/adapters.py`（`OpenAICompatAdapter.__init__`/`_get_client`/`complete`/`create_adapter` に `max_retries` 追加、ペイロード統合）、`scripts/model_matrix.py`（`_call_once_phase1` の1行＋`--retries`ヘルプ更新）

**テスト:** `tests/test_llm.py` に新クラス2つ（`TestOpenAICompatParamPolicy` 5件、`TestSingleHttpRequestGuarantee` 11件）、`tests/test_registry_18.py` にフィールドリスト更新＋H2限定上書きの固定テスト、`tests/test_model_matrix.py` に3件（リトライ責務一本化の証明）。全543テスト PASSED（実API呼び出しなし）。

**実行結果（run_id: run_20260818_041810、H2のみ、`--retries 0` でHTTP最大1回）:**
- dry-run: 対象1モデル・最悪見積コスト$0.0926・通算コスト$0.0085（state不変）を確認してから実API実行
- 実API: **pass**（5333ms、$0.000445）。requested_model=`gpt-5.6-sol`、response_model=`gpt-5.6-sol`（match）、finish_reason=`stop`、reasoning_tokens=0、cache系0
- `phase1_calls.jsonl`: 既存のH2 fail行（2026-08-17T22:04:28）を保全したまま、新しいpass行を追記（18→19行）
- `state.json`: `calls_made` 18→19、`spent_usd` $0.008537025→$0.008982025、`models.H2.phase1.status`をpassに更新。他17モデルは無変更
- 18モデル全てPhase 1 pass（strong6の最後の1件が解消）、累積コスト$0.0090/$0.25

**申し送り:** `scripts/model_smoke.py:188` の別系統 `create_kwargs` は今回未修正（Phase 1経路を通らないため対象外）。H4/H5の推定単価問題は別件で継続。Phase 2/3は今回実行せず。

成果物: `.devrelay-output/phase1_h2_retest_report.md`
