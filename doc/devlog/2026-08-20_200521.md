# 2026-08-20 20:05 JST ｜ サイクルK: Phase 3 runtime overrides

- 要求: H1 adaptive-thinking effortとH4 timeoutを、Phase 3限定のCLI runtime overrideとして追加する。
- 実行: `--effort`（low〜max）と正の`--timeout-seconds`を追加し、H1だけへadaptive thinking + `output_config.effort`を送る配管にした。
- 実行: effective ModelInfo copyへmax_tokens/timeoutを反映し、registryを変更せずJSONL/state/reportへ実効max tokens・effort・timeoutを保存する。
- 検証: fake/mockを含む関連269件と全732件がPASS、既存warningは4件。
- 発見: 全体`git diff --check`は既存`doc/model_matrix_report.md`の末尾空行で失敗するため、今回変更対象の差分検査を別途実施する。
