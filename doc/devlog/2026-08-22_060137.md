# Gemini 3.7 Flash 技術検証基盤

- `M3` を変更せず、検証専用 `M3_G37_EVAL`（`gemini-3.7-flash`）を追加した。
- Google公式一次情報を2026-08-22 JSTに確認し、導入価格 input $0.75 / output（thinking込み）$3.75 per 1M tokens、OpenAI互換 `reasoning_effort="low"`、sampling parameters未送信を固定した。
- `scripts/gemini_37_eval.py` を追加。4種の実ゲームJSON、M3比較、Gemini 1席＋Random Bot 11席のS2/R3段階停止を、既存 `BudgetedAdapter`・LLM/Eventログ・コスト上限で実行できる。
- 料金スナップショットと未実行の結果テンプレートを追加。API実行・正式M3昇格は行っていない。
- 検証: `uv run pytest` → 756 passed。
