# 2026-09-21 22:28 JST ｜ サイクル10.10: L12R12 事実集計

完走した12体×12ラウンド本戦（run-id `l12r12_dr_1201`、seed=1201、DevRelay 6席+API 6席、実行時間38,133.5秒≈10時間35分）を対象に、コード変更なし・読み取りのみで「面白い結果が出たか」判断用の事実を集計した。作成物は `doc/analysis/l12r12_dr_1201_facts.md`（新規）のみで、`engine/`・`llm/`・`scripts/`・`tests/` は無変更。

`llm.response_parser.parse_response()` で全12席1,553 LLMコールのnegotiation phaseを再パースし、`contract_propose`のtermsを`viewer/log_parser.py::_collect_contract_terms`と同一方式で`contract_id`に復元。`game01_events.jsonl`（2,067件）を1回走査してイベント種別・首位公示・型C・倍掛け・市場高騰等を集計する使い捨てスクリプトを`/tmp/c1010/`に作成した（リポジトリ外、コミット対象外）。

首位P06は R2〜R12の11ラウンド一貫して単独首位公示（交代0回）を維持しつつ、実際の市場コミットでは12回中8回（R4-R10,R12）で自分より強いカードを持つ相手に同市場で挑まれ**8戦全敗**していた一方、R1-R3,R11の4勝で計664万円を先行確保していたことが判明（MARKET_RESULT.winnersとCOMMIT/CardRankの突合で確認）。型Cは前回6体戦の0件から一転、提案40件・成立25件・発火18件・支払総額445万円（TYPE_A_EXECUTION 45件=純型A支払27件+型C発火分18件で完全一致を確認）。契約terms内訳は型B不参加が成立ベース153/290=52.8%（前回6体戦は45/51=88.2%、`doc/analysis/l6_r12_v010_20260919_protagonist.md`L52より引用、母数の定義に曖昧さがあるため両解釈を併記）。

P11（DevRelay Opus 4.8）はDevRelay HTTP 504（応答本文0文字）が57件発生、R9・R10のcommitフェーズtimeoutが各1回AUTO_COMMITに直結。ユーザー指示に基づきP12（Sonnet 5）も同粒度で調査した結果、P12のtimeout15件は全てnegotiation phaseでcommitフェーズは12ラウンド全て自力成功（AUTO_COMMIT 0件）。したがってP12のR12脱落（condition_not_met、現金559,353円が生還条件200万円未満）の直接原因はtimeoutによる無効出力やAUTO_COMMITではなく、拘束契約下での資金不足であると事実ベースで確認した（timeoutが交渉機会をどれだけ奪い間接的に影響したかは未集計と明記）。

第8節「宣言と実行の差」は`scripts/analysis/lie_profit/`が対象・出力先の切替（`--trial`/`LIE_PROFIT_OUT`）自体は可能なものの本タスクの時間内で3段階パイプラインの完走を確認できず「未集計」と明記、独自簡易パーサは作成していない。第7-2節の報奨達成・取下げも専用イベント種別が存在せず「未集計」と明記した。

`git status --short`で本タスクによる差分が`doc/analysis/l12r12_dr_1201_facts.md`（新規）・本devlog（新規）・`doc/devlog/INDEX.md`（1行追記）のみであることを確認、`engine/llm/bots/viewer/scripts/tests`への追加差分は0。LLM API・DevRelay呼び出しは本タスク中0件。`git push`未実施。
