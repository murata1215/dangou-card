# 2026-08-17 11:55 JST | L3スモークテスト — L7全滅の代替をAPI4コールで確認

明け方の seed=701 6体試験で `L7`（Gemini 2.5 Flash-Lite）が404で全滅していたことが判明したため、18体本番に進む前に代替候補 `L3`（Gemini 3.5 Flash-Lite）だけを最小コストで実機検証した。

**要求:** いきなり18体・12Rを回すのではなく、L3単体の疎通・応答品質・コスト特性を数コールで見極めたい。API呼び出しは最大5回、上限$0.10。ゲーム本体は起動しない。

**実行:** 汎用スモークテストスクリプト`scripts/model_smoke.py`を新規作成（既存の`get_model`/`create_adapter`/`estimate_cost`/`parse_response`/`build_system_prompt`をそのまま再利用）。ping 1回 + 18体S2相当の本番同等プロンプト3回、計4コールでL3を実行した。

**検証:** ユニットテスト10件（API非依存、フェイクアダプタ）が全てPASS、全体テストは361件PASS。実験本体は計画通り4回のAPIコールに収まり、承認上限5回を超えなかった。

**発見:** L3は**4/4成功、404/429/timeoutなし、JSON率100%**。finish_reasonは全て`stop`（DeepSeekで見られた`length`途中切断はなし）。実測コスト$0.003624（上限の3.6%）。1回目のpingだけ11.5秒とやや遅かったが、以降3回は1〜1.2秒で安定。これによりL7の代わりにL3を18体負荷試験のGemini枠（6席）に投入してよいと判断した。エラー全文取得の仕組み（AdapterErrorの200文字切り詰めを回避する再取得ロジック）も実装したが、今回は発火しなかった（=正常系のみの確認）。

**出力:** `scripts/model_smoke.py` / `tests/test_model_smoke.py` / `logs/smoke/smoke_L3_20260817_115425.jsonl` / `.devrelay-output/l3_smoke_report.md`
