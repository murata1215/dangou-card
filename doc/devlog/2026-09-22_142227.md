# サイクル10.15: LP文言更新（Zenn公開前・S1前提の断定解消）

## 要求
`viewer/static/lp.html` に残る Season 1 前提の断定（AI 8体・生還はただ一体・敗者の手記）を現行ルール（人数可変、複数生還可、最終資産で順位付け）に合わせて更新。DevRelayプランモードで前回Planを2回修正（LIE DETECTED除去→最終資産カード化、kicker `8 MODELS`→`6 LABS`→`MULTI MODEL`）した最終版を実装。

## 実行
- `viewer/static/lp.html`: title/description/og/twitter（twitter:title・twitter:description 2行新規追加）、hero kicker（`AI BATTLE · MULTI MODEL · 12 ROUNDS`）・sub・CTA ghost ラベル（`AIたちの手記を読む`）、概要カード1（`8体`→`6社`、h3/pに「直近の本戦は12体で実施。」追記）・カード3（`1体`/LIE DETECTED→`最終資産`/生還はゴールではない）を編集。合計 diff +15/-13行。
- `tests/test_viewer_lp.py:20`: マーカ文字列を `"敗者の手記を読む"` → `"AIたちの手記を読む"` に更新（1行）。

## 検証
- grep: 旧断定・撤回済み案語（`8体`/`8 MODELS`/`6 LABS`/`12 PLAYERS`/`ただ一体`/`敗者`/`LIE DETECTED`/`神視点`）0件。kicker新表記1件。「12体」本文1箇所のみ。
- meta数: 9→11（twitter:title/description追加分）。
- HTML python3 `html.parser` によるタグ整合チェック: ok。
- `uv run pytest tests/test_viewer_lp.py -v`: 4/4 PASS。
- `git status --short`: 変更ファイルは `viewer/static/lp.html` / `tests/test_viewer_lp.py` の2件のみ（既存未コミット差分は無変更）。

## 発見
- 前回Planのmeta数見積り「8→10」は実際は「9→11」で1件ずれていたが内容自体は正しく、Plan記載の試算誤りのみ。
- commit/push/deploy/再起動は未実施（`viewer/server.py` の `FileResponse` により次回GET `/`で自動反映）。
