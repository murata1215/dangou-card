### 2026-08-18 06:43 JST | Phase 1 コスト計算バグ修正（cache割引・thinking差分の未計上）

## 目的

`scripts/model_matrix.py` の `run_phase1()` が `estimate_cost()` に `cache_read_input_tokens`/`total_tokens` を渡していなかった既知バグを修正する。前サイクルの中6モデル実測で発覚（M4 grok-4.5: cache割引未反映で過大計上、M3 gemini-3.5-flash: hidden thinking未計上で過小計上）。

## 修正内容

`scripts/model_matrix.py` に `_usage_cost()` ヘルパーを追加し、`run_phase1()` 内の該当1行のみ差し替えた。`estimate_cost()`（`llm/models.py`）自体・`llm/adapters.py`・`engine/`・Phase 2/3・`MODEL_REGISTRY` の単価は無改変。

## 調査で判明した追加事項

- **Finding B（重要・今回対応）**: Anthropic は `input_tokens` と `cache_read_input_tokens` を別建てで返す（OpenAI慣習と逆）。素朴に `cache_read` を渡すと過小計上・最悪`uncached_input`が負になるバグを新規に持ち込むところだった。`_usage_cost()` で Anthropic のみ `input_tokens += cache_read` と正規化して吸収し、回帰テストで確認済み。
- **Finding A（今回は据え置き・報告）**: 同じ引数漏れが Phase 2（旧407行付近の同型呼び出し）と `BudgetedAdapter.complete`（Phase 3の予算ガードが使用）にも存在。特に後者は予算ガードがGemini系のthinkingを見落とし実支出が上限を超えうるため、次サイクルの優先修正候補として申し送る。

## 検証（実APIなし）

既存 `run_20260818_041810` の12モデル分 `phase1_calls.jsonl` を修正後の `mm._usage_cost()` で再集計（実API再実行なし）。

- M4 grok-4.5: 旧$0.001082 → 修正$0.000681（cache割引が支配的で減少）
- M3 gemini-3.5-flash: 旧$0.000093 → 修正$0.001758（thinking加算のみで増加）
- L4 grok-4.3: 旧$0.000294 → 修正$0.000950（cache割引とthinking加算が両方効き、正味は増加。計画時点の想定より複雑なパターンと判明）
- 他9モデル: 旧値と完全一致（無影響を確認）
- 12モデル合計: 旧$0.002105 → 修正$0.004026

`state.json`・既存レポートは履歴として変更せず、比較表を新規レポートとして提示した。

## テスト

`tests/test_model_matrix.py` にフェイクアダプタ1つ＋テスト6件追加。`uv run pytest tests/test_model_matrix.py -v` 52 passed、`uv run pytest tests/ -q` 523 passed（既存514＋新規9）。実APIは通常pytestに混ぜていない。

## 成果物

- `.devrelay-output/phase1_cost_fix_report.md`（変更内容・12モデル比較表・Finding A/B・テスト結果・残存リスク）
- `scripts/model_matrix.py`（`_usage_cost()`追加＋1行差し替え）
- `tests/test_model_matrix.py`（6件追加）

## 申し送り

- Phase 2（`run_phase2`）と `BudgetedAdapter`（Phase 3予算ガード）に同じ引数漏れが残存。特に `BudgetedAdapter` はGemini系thinkingを見落とし実支出が上限超過しうるため優先度高。
- Anthropicのcache**書き込み**（`cache_creation_input_tokens`、通常1.25倍課金）は`estimate_cost()`に単価枠がなく、依然として未計上。
- 強（H）ティア6モデルのPhase 1実行時は、本修正が適用された状態で実施すること。
