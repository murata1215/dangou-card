# 2026-09-19 11:45 JST ｜ サイクル10.1: 型C（条件付き金銭契約）の実装

要求: 承認済みプラン（v0.10仕様書「型C: 条件付き金銭契約」の実装、Step0〜5＋検証＋devlog）の実行継続。前セッションでStep0（仕様書配置）〜Step4（ビューワー/ハイライト）と、Step5のうち`tests/test_contracts.py`追記・`tests/test_type_c_conditional.py`・`tests/test_type_c_parser.py`は完了済み。本サイクルは残りのプロンプト層テスト・通知層テストの作成、全テスト実行、検証（dry_run/simulate比較・S2完走確認）を実施した。

実行: `tests/test_type_c_prompt.py`（system prompt出し分け・「（保険型）」ラベルの無条件削除・新規文面/通知3種の禁止語スキャン・`_format_type_c_condition`/`_format_obligation_detail`の型C分岐・交渉プロンプトへの反映・破産余裕額計算からの除外）を21件、`tests/test_type_c_notices.py`（`_queue_type_c_notices()`のresult→kind写像・当事者限定配送・Settlement中に積んだ通知が次Negotiation Phase開始時にdrainされる規則）を9件、それぞれ新規作成した（前者は`_convert_action`同様の直接呼び出し、後者は`Game`インスタンスへ直接メソッド呼び出しで検証）。

検証: 型C専用テスト計4ファイル120件を含む全1701件PASS（回帰ゼロ）。S1（`type_c_enabled=False`）の`scripts/dry_run.py`出力578イベントを`git stash`で変更前後を再生成しtimestamp除外の正規化比較でdiff0を確認、同様にBot `simulate.py --ruleset S1` 5試合分もdiff0を確認（バイト完全回帰）。S2（`baseline_v1_s2`）で`simulate.py --ruleset S2` 5試合が完走（平均生還5.0/8）することを確認したが、ルールベースBotは`contract_propose`を一切発行しないため型C契約は自然発生せず`TYPE_C_EVALUATED`は出力されなかった。そのため一時検証スクリプト（非コミット）で型C契約を提案・署名する簡易エージェント2体を注入した4人フルGame（S2）を実行し、R3で`TYPE_C_EVALUATED`(eliminated条件, not_fired)が正しく記録され4人全員生還することを確認した（検証後スクリプトと一時ログは削除）。

発見: ルールベースBot群は契約提案経路を持たない（LLM Agentのみが対象）ため、S2 Bot simulateの完走確認だけではv0.10機能のイベント発火はカバーできず、フルGame + 手製エージェントでの補完実行が必要だった。ビューワーのgod/public目視確認は本環境にブラウザが無いため実施できず、既存`test_viewer.py`（god/public境界の全体テスト、変更なしで全PASS）とコードレビューで代替した。なお前セッション（本サイクル内）で、型Cプロンプト文面追加により`baseline_v1_s2`のsystem prompt長が8,181→8,996字に増え`scripts/model_matrix.py`のCORE_18予約合計が旧上限$0.40超過となったため`max_cost`0.40→0.42・`max_cost_per_model`0.035→0.04へ引き上げ済み（`AskUserQuestion`不可のため単独判断、コード側コメントで開示済み・本ビルドサマリでも再掲）。
