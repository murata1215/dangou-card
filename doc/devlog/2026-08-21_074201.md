# 2026-08-21 07:42 JST — L1〜L6 Phase C R1〜R3 実APIミニ試験

## 実行条件

- `S2` / `L1,L2,L3,L4,L5,L6` / 1 game / seed `1201`
- `num_rounds=12`を維持し、`--stop-after-round 3`を使用。
- 全席 runtime output `2000`、CoT無効、`$5/player`・`$40/game`、budget block即時中断有効。

## 結果

- 出力: `logs/llm/trial_C_20260821_072321/`
- R1〜R3の`ROUND_COMPLETE`、R3の`GAME_STOPPED`、`GAME_END(completed=false)`を確認。R4 event/callは0件。
- 6席全員がR3時点で生存。budget block、API error、timeout、retry、AUTO COMMITはいずれも0件。
- Reflectionは各席R1〜R3に3件ずつ保存され、正規化後のHandover Memoryは最大1000文字でR間連鎖を再構成できた。
- 総209 calls、実コスト `$0.8492`。モデル別内訳は`trial_C_report.md`に保存。

## Viewer確認

- 既存timelineは全席で取得でき、各席のReflection 3件も含まれる。
- 実ログの`MARKET_RESULT.participants`が整数である一方、round-detailの`_safe_round_result()`が配列を仮定するため、
  round-detail再構成は`TypeError`となった。DM本文・宛先、匿名発信者、契約条項を安全action変換から除外することは確認済み。
- 本試験ではViewer再起動・修正を行わない。round-detailの実ログ互換修正は別承認で扱う。
