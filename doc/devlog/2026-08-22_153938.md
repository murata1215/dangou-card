# 2026-08-22 15:39 JST ｜ サイクル: FINAL_REFLECTION 3000-token化+parser救済強化+L1/L6再試験

要求: 事前に承認済みのPlan（`/home/uso8m/.claude/plans/async-plotting-scott.md`）に従い、
直前の疎通試験（`doc/trials/final_reflection_smoke_2026-08-22.md`）が出した
NO-GO（条件付き）を解消する。原因はC1（L1: `finish_reason=max_tokens`によるJSON物理切断）と
C6（L6: `comment`内未エスケープ改行によるstrict JSON失敗）の2件が`ok_recovered`へ
フォールバックし`emotion`/`defeat_cause`を喪失したこと。(1)出力上限を1000→3000へハード引き上げ
（プロンプトは800〜1500字誘導のみ、3000という数値はプロンプトに一切出さない）、
(2)`parse_final_reflection()`の救済ティアを`emotion`/`defeat_cause`/`comment`独立救済へ強化、
(3)API-freeゲート必須の上で(4)L1・L6のみ計2callで実API再試験しGO/NO-GO判定を出す、という
4段構成で実装した。

実行: `engine/config.py`の`final_reflection_max_tokens`を`1000→3000`に変更（docstringで
「ハード上限であり狙う長さではない」旨を明記）。他フェイズは`llm/llm_agent.py:final_reflect()`の
`max_tokens_override`経由でのみ影響するため無変更（grep 2箇所のみで確認）。
`llm/prompt_builder.py:build_final_reflection_prompt()`に「800〜1500字程度で簡潔に」誘導と
`comment`内改行の`\n`エスケープ指示を追加、既存の`{max_chars}字以内`文言は
`test_prompt_respects_final_reflection_max_chars`のため温存。`llm/response_parser.py`に
`_recover_string_field_bounded()`（エスケープを尊重した境界スキャン。閉じクオートが
無ければNoneを返す。`emotion`/`defeat_cause`など後続キーがある場合に安全）、
`_unescape_json_string()`（`\\`/`\n`/`\t`/`\"`復元の共有ヘルパ）、`_degrade_wrapper_to_text()`
（3キーとも救済不能な場合でも生JSONラッパーを漏らさずプレーンテキスト化）を新設し、
`parse_final_reflection()`の救済ティアを全面書き換えた。既存の`extract_json()`（全フェイズ共有）
と`_recover_string_field()`（Handover Memory側`extract_memory_with_status`が使用）は無変更のまま
温存。`_finalize()`に`salvaged: list[str]`を追加し、`engine/game.py`のFINAL_REFLECTIONイベントと
`llm/llm_agent.py`の`_update_last_log_final_reflection()`へ追加伝播（既存キーは型・意味とも無変更）。
`scripts/final_reflection_smoke.py`は`FINAL_REFLECTION_MAX_TOKENS`をconfig由来へ変更、
`--cases`フィルタを新設（`resolve_memory_samples()`をcase一覧受け取りへシグネチャ変更し、
`[C1, C6]`のような非prefix部分集合でもインデックスがずれないよう修正）、
`finish_reason`/`truncated_by_provider`/`salvaged`を`calls.jsonl`・`report.md`へ追加、
`--parser-selftest`にC1/C6形状の追加ケースを実装した。

検証: `uv run pytest tests/test_final_reflection.py -q`は**51 passed**（既存41 + 新規10）、
`uv run pytest tests/test_memory.py -q`は**64 passed**（`_recover_string_field`共有経路に
無回帰）、`uv run pytest tests/ -q`は**859 passed**（既存849 + 新規10、失敗0）。
新規テストは実際のcycle 1生ログ（C1の途中切断JSON、C6の生改行入りJSON）を元にした
`test_c1_truncated_json_salvages_all_three_keys`/`test_c6_raw_newline_in_comment_salvages_all_three_keys`
ほか、境界スキャンが後続キーを飲み込まないことを確認する
`test_bounded_recovery_does_not_swallow_following_keys`、ラッパー漏洩ゼロを確認する
`test_unsalvageable_wrapper_never_leaks_braces`など9件と、config既定値確認1件。
API-freeゲートG1〜G7を全PASS確認（`--parser-selftest`拡張11/11、`--dry-run --cases C1,C6`
`calls_attempted=0`2/2、`--mock --cases C1,C6`ルーティング/`sent_max_tokens=3000`/非破壊/
二重発火なし全PASS、`dry_run_prompts.md`目視で`800〜1500`・`2000字以内`文言確認かつ
`3000`はスクリプト側コスト注記2箇所のみでプロンプト本文には不出現）。ゲート全PASS後、
`uv run python scripts/final_reflection_smoke.py --run-id fr_smoke2_20260822 --cases C1,C6
--max-calls 2 --max-cost 0.05 --max-cost-per-call 0.03`で実API実行し、2/2 API成功、
実測コスト合計**$0.009883**（計画worst_case $0.02025、上限$0.05以内）。C1は
`finish_reason=end_turn`（旧`max_tokens`から改善）、C6は`finish_reason=stop`で
`comment`内改行が正しく`\n`エスケープされており、両方とも`parse_status="ok"`（`salvaged=[]`、
救済不要）で一発成功。`emotion`/`defeat_cause`/`comment`の3キーとも非空、`emotion∈VALID_EMOTIONS`、
`truncated=False`、`state_unchanged`/`memory_unchanged`は前後完全一致、`second_call_fired=False`、
両commentの全文を人手で読み、脱落理由（借入額過大・R4 Entry Fee／初動借入過大・R2読み負け）
と清算内訳が整合することを確認した。二重送信防止も同一`--run-id`再実行で即中断
（API呼び出し0件）を再確認し、`git status --short logs/llm/`と`logs/`は空。
11項目のGO基準（Planの「Change 5」）はすべてPASSし、**判定はGO**。

発見: 3000-token化とプロンプトへの`\n`エスケープ指示という2つの根本原因対応だけで、
cycle 1のC1/C6の不具合は今回の1サンプルずつでは再現しなかった（C1はJSON物理切断解消、
C6はモデルが正しくエスケープしたJSONを返した）。単発サンプルのため後者を断定はできないが、
パーサ側の3キー独立救済（`_recover_string_field_bounded`・`salvaged`フィールド・
`ok_plaintext_wrapper`ステータス）は防御として実装済みのまま温存されており、将来同種の
不具合が再発しても`emotion`/`defeat_cause`喪失を防げる設計になっている。

未実施: L12×R12本番トライアル自体、Viewer側のFINAL_REFLECTION表示対応（`salvaged`含む）、
commit/push、Viewer再起動。次サイクルの候補として引き続き記録する。
