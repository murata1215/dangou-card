# Viewer脱落表示バグ修正: AUTO_COMMIT_FAILUREの取りこぼし

- 進行中run `trial_C_l12_r12_20260822 / game01` で、P09（gpt-4.1-mini）がR6 commit phaseの
  `AUTO_COMMIT_FAILURE`（合法Commit0件・reason=`contract_violation`）で
  Engine上は `forced_liquidation()` 済み・`is_alive=False` 確定にもかかわらず、
  Viewerで「生存」表示のまま資産ランキング`—`・提出カード不在になる不整合を読み取り専用調査で確定。
- 原因はViewer側の脱落判定漏れ。`AUTO_COMMIT_FAILURE` は engine/game.py commitフェーズの
  唯一の発火箇所で必ず `forced_liquidation()` を伴うが、`viewer/log_parser.py` の脱落イベント判定
  （`ELIMINATION`/`BANKRUPTCY`/`FORCED_LIQUIDATION`）に含まれていなかった。Engine側は正常（ghost-aliveではない）。
- `viewer/log_parser.py` を修正: `ELIMINATION_EVENT_TYPES` 定数を新設し `get_game_state()` の判定を統一。
  `get_round_states()` は既存の `elif` チェーンで `AUTO_COMMIT_FAILURE` が契約outcomes紐付け分岐に
  先取りされ、脱落記録分岐へ到達しないシャドウイングを発見。同分岐内に脱落記録を追記して対応（elif構造は変更せず）。
- `tests/test_viewer.py` に `TestAutoCommitFailureElimination`（6テスト）を追加。
  P09と同型の合成ログでの検出確認、elifシャドウイング回帰、public/god view一致、
  既存3経路（ELIMINATION+FORCED_LIQUIDATION二重発火／BANKRUPTCY／SURVIVAL_CHECK）の非破壊確認、
  AUTO_COMMIT（成功側）での偽陽性防止、実trial `trial_C_l12_r12_20260822` でのP09回帰（run有無でskip）。
- 検証: `uv run pytest tests/` 772 passed。`git diff --check` 空白エラーなし。
  修正後、実trial parserを直接呼び出しP09が `status="eliminated"`・R6の`eliminated`に記載されることを確認
  （試合はR9→現在完走・5席生還まで進行、eliminated=[P01,P02,P06,P07,P09,P11,P12]）。
- Engineは無変更。**当該R12結果はベンチマークとして採用可能**（勝敗・資産計算はEngine内で終始正しく、
  Viewer表示のみが誤っていた）。
- Viewer本番プロセス（127.0.0.1:9023, PID 1306737）は本作業中未再起動。表示反映には別途再起動承認が必要。

対象ファイル: `viewer/log_parser.py`, `tests/test_viewer.py`。
先行調査: 本devlogと同日の読み取り専用調査（Plan `whimsical-stargazing-patterson`）。
