# 2026-09-03 21:34 JST ｜ サイクル9.12: 公開面の説明資料の棚卸し

LP・ビューアー・OGP・README・図解など一般読者の目に触れる説明資料を全て列挙し、
Season 1 時点の記述（8体・旧ルール・SEASON 1 表記）が残っている箇所を洗い出した
（コード・文書・画像の変更は一切なし・調査のみ）。

## 調査対象と方法

1. **LP**（`viewer/static/lp.html`、`server.py:77-80` で `/` に静的配信）— 全文Read
2. **OGP/SNSカード**（`engine/blog/templates/*.html`・`engine/blog/cards_svg.py`）— `scripts/blog.py`（`--cards`）が現役で使用していることを`grep`で確認した上で調査
3. **`/watch` ビューアー**（`viewer/static/index.html`）— ルール解説セクション・感情アイコン・実況席・神視点UIをRead/Grep
4. **README.md・`doc/`対外資料**（`scripts/rules_blog.py`・`cards_blog.py`・`rules_panels.py`・`cards_panels.py`）— pitch/slides/LP素材ディレクトリの有無もGlobで確認（該当0件）
5. **PixBlog側**（GETの範囲）— `/tmp/cycle912_fetch_profile.py`（読み取り専用一時スクリプト）で`/me`・`/users/uso8m`・`/profile`・`/user`を試行（いずれもAPI未提供で404）、`GET /posts/241`（9.11で公開報告したガイド記事）と本番`feed.json?limit=4`を実測

## 主な発見

- **`viewer/static/index.html:100,109-111`が最も実害が大きい**: 「他者への譲渡不可」「発行料10万円」という**固定文言**が、`fillRules(cfg)`（`:280-289`）の更新対象（数値5項目のみ）に含まれず、実際のゲーム挙動（カードトレード可・契約無料）と矛盾したまま観戦者に表示され続けている
- **LP全体が8体表記のまま**（`lp.html:6,7,11,106,108,122-123,131-135`）。タイトル・meta description・OGPまで含め露出面が最も広い。BLOG PICKUPのフォールバックタイル（`:144-153`）もS1記事のまま
- **常設ガイド（9.11で公開したはずのpost_id=241）への導線がLP・`/watch`のどこにも無い**。9.11の作業の効果が現状ほぼ活きていない
- **重要な事実（対象外だが要フォローアップ）**: `GET /posts/241`を実測したところ `"status": "draft", "published_at": null` だった。9.11で「公開URL」として報告した常設ガイドは、**API上は現時点で非公開のまま**の可能性がある
- **本番 `feed.json?limit=4` を実測**したところ、直近4件は全て**Season 1記事**（2026-08-16〜19公開）だった。S2連載16本は`plan.md`上まだ未公開のため、LPの動的フィードも結果的にS1記事しか出せない状態
- **OGP/SNSカードのテンプレート3種＋`cards_svg.py`に「SEASON 1」が焼き込み**（`title_card.html:31-32`・`model_card.html:51`・`result_card.html:44`・`cards_svg.py:337,396,485`）。生成元の`scripts/blog.py`は現役のため、今後投稿するたびに陳腐化画像が生成され続ける。`.claude/skills/blog-visual/reference/brand.md:35`も追随して古い
- PixBlogのプロフィール文はAPIに取得エンドポイントが存在せず（`/me`等は404でNext.jsのHTMLが返るのみ）、判定保留とした
- pitch/slides/LP専用素材ディレクトリは存在しない（`doc/**/pitch*`・`doc/**/slides*`とも0件、`doc/**/*.png`は連載記事用画像のみ）

## 成果物

`doc/analysis/public_assets_audit_20260902.md`: 資料ごとに「場所（file:line/URL）／現行の文言（原文）／判定／書き換え案の要点」の表を6小節（LP／OGP・SNSカード／`/watch`／README・doc／PixBlog／Xプロフィール対象外）で整理、末尾に「LPの現行セクション構成」と「推奨する新構成（案）」（HERO→ガイドCTA新設→WHAT IS THIS→S2連載導線新設→FROM THE BLOG→FOOTER）、優先度サマリ上位5件を付記。

コード・文書・画像は一切変更していない（`doc/analysis/`・`doc/devlog/`のみ新規作成）。LLM API呼び出し0件、commit/push未実施。
