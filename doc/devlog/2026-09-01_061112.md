# 2026-09-01 06:11 JST ｜ サイクル9.8b: tweet_text/excerpt 対応（X連携確定）

PixBlog 側に `posts.tweet_text`（X共有ボタンのコンポーザー本文専用カラム）が新設され API 書き込みに
対応したため、サイクル9.8 の (b) 判定を **(a) 確定**に更新した。`scripts/publish_next.py` の公開ペイロードに
`tweet_text`（`x_text` + 半角スペース + `#嘘八百万`、URL 非含有）と `excerpt`（`lead` そのまま）を追加し、
公開前に excerpt へのハッシュタグ/URL混入検証と tweet_text（`#嘘八百万` 込み）+URL の加重文字数280超過
チェック（超過時は公開せず中断、`--force` で無視可）を実装した。`doc/blog/pipeline.md`「10. X 連携」節を
(a) 確定の内容に書き換え、旧 (b) 判定は「経緯」として圧縮して残した。

`--check-lengths` で16本全ての `#嘘八百万` 込み加重文字数を検算した結果、全て280文字以内（最大は
order 01 で tweet_text 197／+URL 221）、excerpt（lead）へのハッシュタグ・URL混入は0件だった。
`--dry-run`（order 01・order 10の2画像記事）で公開ペイロードに `tweet_text`・`excerpt` が正しく載ること、
tweet_text に URL が含まれないこと、X下書きの新文言出力を確認した。

変更ファイルは `scripts/publish_next.py`・`doc/blog/pipeline.md`・本devlog・`doc/devlog/INDEX.md` のみ。
`doc/blog/**/posts/*.md`（フロントマター含む）・`doc/blog/**/plan.md`・engine/llm/tests/logs/
`blog/pixblog_client.py`・`doc/blog/state.json` は無変更。実公開・画像アップロード・PATCH は一切実行せず、
LLM API 呼び出し0件、commit/push 未実施。
