# 2026-08-20 10:56 JST ｜ サイクルJ: M3/H3 Phase 2 reasoning budget tuning

要求: Phase 2でlength終了したGemini M3/H3を、モデル別の最小変更で再試験可能にする。
実行: M3だけreasoning_effortをminimalへ変更し、H3だけPhase 2 output上限を実測由来の912へ設定した。
実行: 両モデルのcanonical Structured Output schema、temperature、retry/fallback、他Phase・本戦の挙動は維持した。
検証: request optionのthinking制御非併送、モデル別token上限、reserve（M3 $0.014364 / H3 $0.023952）を回帰で固定した。
検証: 対象156件、full pytest 723 passed, 4 warnings、git diff --checkが成功した。
発見: 両モデルの同時予約はtotal capを超えるため、実APIはM3結果確認後にH3を別承認で判定する。
