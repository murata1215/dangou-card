# 2026-08-20 08:32 JST ｜ サイクルH: H1 Phase 2 timeout延長

要求: H1のStructured Outputs初回schema compilationを待つため、Phase 2だけのtimeoutを300秒へ延長する。
実行: ModelInfoのPhase 2 timeout overrideと、Phase 2 adapter生成時だけ使うModelInfoコピーを追加した。
検証: H1だけ300秒、元のH1とM1/L1/M3/H3は通常timeout、clientのretry=0をfakeで確認した。
検証: Structured Outputs payload、schema reserve、max_tokens=400は不変であることを固定した。
検証: full pytestは695 passed, 4 warnings、git diff --checkは成功した。
発見: H1の従来実効timeoutは90秒ではなくModelInfo既定の60秒だった。
