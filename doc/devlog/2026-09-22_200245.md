# 2026-09-22 20:02 JST ｜ サイクル10.17: L12R12 手記12本の一括公開

## 要求
サイクル10.16で総括記事1本を公開済みのL12R12（`l12r12_dr_1201`、12体×12ラウンド・実験席混合）について、12席すべての手記（生還記／脱落記）を1本ずつ執筆し、下書き確認なしで一気に公開する依頼。並び順は脱落順（早い順）→生還者を最終資産の低い順→優勝席P06を最後。

## 実行
`llm.response_parser`の公開関数のみで12席ぶんのドシエ（`doc/blog/20260921/dossiers/P01〜P12.md`）を機械生成し、`NEGOTIATION_ACTION`とのdedup+joinで`matched+events_only=1261`（合格）を確認した。各席の事実パケット（facts.md/timeout.mdから抽出した確定数値）を用意し、12本の手記をサブエージェント12体に並列執筆させ、フロントマター・引用の逐語性・字数・画像参照を人手で再検証した。`scripts/blog_images.py`に20260921用のROSTER/RESULTS/MEMOIRS/MOMENT_CARDS/DATE_CONFIGを追加し24枚の画像を生成、`scripts/publish_next.py`に画像アップロード失敗時のPOST前中断ガードを追加した上で、`--check-lengths`→`--dry-run --order 18`→`--order 18`〜`--order 29`の順に12本を実publishした。

## 検証
引用の機械照合で、ドシエ原文とのJSONエスケープ起因の見かけ上の不一致4件（実際は一致）と、本物の転記ミス1件（P11、「正確に指定」→原文「正確指定」）を検出し1件を修正した。画像生成過程で2件のデータ精度バグを発見して修正した：(1) `reconstruct_series`が型C発火分の送金（`TYPE_A_EXECUTION`）を反映せずP02の最終ラウンド資産が5万円ずれていた、(2) 同関数がSNAPSHOTの`debt_balance`を読まず、negotiation中の任意返済（`repay`アクション）で完済した席の借金系列がゼロ化されなかった（P02がR6の3回のrepayで188万円を完済したのに借金線が最後まで残る表示になっていた）。両修正後、20260824・20260830の既存28枚と20260921の総括2枚（17_results/17_roster）はsha256完全一致で非回帰を確認し、20260921の12席×2枚=24枚を新規生成した。`publish_next.py`の`update_plan_status`が「order番号で始まる行」を全表から無差別に置換する既知の弱点により、手記スロット表（新設）の行が投稿状態表と同時に誤って書き換えられたため、plan.md公開後に手動で復元した。12本の公開URLは全てHTTP 200を確認した。

## 発見
落とした引用は0件（全100件検証、1件修正して一致・4件は元から一致）。本文字数は2,527〜3,613字（目安2,000〜3,500字の範囲内、2本がわずかに超過）。plan.mdの「手記スロット表」と「投稿状態」表という2つの order 番号表を同一ファイルに持つ構成は、既存スクリプトの正規表現が表を区別しない設計だったため今回のような書き換え事故を招いた。次回同種のプランでは単一order表に統一するか、スロット表側は`order`列を持たない体裁にする方が安全。commit実施、`git push`未実施。
