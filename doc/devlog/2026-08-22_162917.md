# 2026-08-22 16:29 JST ｜ サイクル: Viewer FINAL_REFLECTION表示（public/god二層設計）

要求: 事前に承認済みのPlan（`/home/uso8m/.claude/plans/async-plotting-scott.md`）に従い、
Engine側で実装・実API試験済み（GO判定、`doc/trials/final_reflection_smoke2_2026-08-22.md`）の
FINAL_REFLECTION（脱落者の最終振り返り: `emotion`/`defeat_cause`/`comment`）をViewerで
閲覧可能にする。調査の結果、`comment`は実測2/2で§8.2秘匿情報（契約条項の復唱、交渉相手の特定）
を含んでいたため、本文（`comment`/`defeat_cause`）はgod限定、公開viewには非秘匿の骨組み
（`emotion`/`round`/`has_comment`/`truncated`/`availability`）のみという二層設計を採用した。
`engine/`/`llm/`/`bots/`は無変更（`llm.response_parser.VALID_EMOTIONS`の読み取り専用import
のみ）、`viewer/server.py`も無変更（既存の`view`パラメータ経路をそのまま利用）。

実行: `viewer/log_parser.py`の`get_round_states()`イベントループに`FINAL_REFLECTION`分岐を追加し、
`rd["final_reflections"]`へ収集する新設ヘルパ`add_final_reflection()`を`add_elimination()`と
同じ先勝ちdedupパターンで実装（top-level `round_num`で振り分け、`VALID_EMOTIONS`で読み取り時にも
emotion再検証——`_extract_emotion()`は`奸`欠落のため意図的に不使用）。`_safe_round_result()`の
返り値に`final_reflection`キーを追加し、当該pidのエントリをpublic/godで出し分け（本文が実質空
なら`None`、イベント自体が無ければ`None`）。`get_player_round_detail()`に発見性のための
`final_reflection_round`ポインタを追加（既読み込み済みの`round_states`を走査するだけで追加I/Oなし）。
`viewer/static/index.html`の`renderRoundDetail()`にmemory-endセクションの後（最後）へ
「脱落時コメント」セクションを追加（public: 感情+「本文は神視点のみ」ラベル、god:
`🔒`付きで敗因・最後の言葉を`escapeHtml()`経由で描画、`truncated`は双方で表示）。
`EMOTION_EMOJI`/`EMOTION_EN`への新規キー追加はテスト（`tests/test_viewer.py`の8キー一致assert）
との乖離を避けるため行わず、既存`emotionDisplay()`をそのまま再利用。ラウンドジャンプ列の
該当ボタンに💀マークを追加。`viewer/static/style.css`に`.final-reflection`系クラスを
既存の`.detail-section`/`.secret-section`/`.visibility-label`を再利用する形で最小追加。
`tests/test_viewer.py`に独立fixtureパターン（`TestEliminationDeduplication._write_llm_logs`と
同型、`TestPlayerRoundDetail._make_trial`は拡張せず温存）で`TestFinalReflectionViewer`を新設し
10ケース実装: 完全データ、public境界回帰（`comment`に仕込んだ「秘密のDM本文」
「type_b_market」がJSON全体に非出現）、`defeat_cause`欠損、emotion不正値のNone正規化
（`奸`正規ケースも確認）、FRなし旧runの完全互換、重複イベント1件化、
`final_reflection_round`ポインタと非該当round非表示、AUTO_COMMIT_FAILURE/
SURVIVAL_CHECK+FORCED_LIQUIDATIONとの共存、empty/rejected_no_comment時の`None`縮退、
frontend文字列検査（`🔒 脱落時コメント（神視点）`・`escapeHtml`経由描画の存在確認）。

検証: `uv run pytest tests/test_viewer.py -q`は**60 passed**（既存50 + 新規10）、
`uv run pytest tests/ -q`は**869 passed**（既存859 + 新規10、失敗0）。手動検証として
`logs/smoke/final_reflection_fr_smoke2_20260822/calls.jsonl`（実API応答2件、読み取り専用）を
tmp_pathの合成events.jsonlに流し込み、`get_player_round_detail(view="public")`と
`view="god"`を実際に呼び出して目視: publicは`{round, emotion, has_comment, truncated,
availability}`のみで`comment`（610字/746字）・`defeat_cause`が一切含まれないこと、
godは両方とも全文（借入額過大・R4 Entry Fee／初動借入過大・交渉欠如による資金ショート、
の各`defeat_cause`含む）が取得できることを確認した。加えてスクリプトのassertで
「実際の`comment`/`defeat_cause`文字列がpublic側JSON全体に一切出現しないこと」を
2件とも機械的に再確認した（境界回帰の直接証拠）。`git status --short logs/`は空
（読み取り専用アクセスのみ、ログへの書き込みなし）。

発見: 当初テストで`detail["final_reflection"]`という誤った参照パス（top-level直下）で
8ケースが`KeyError`となった。Planどおり正しくは`detail["result"]["final_reflection"]`
（`_safe_round_result()`が返す`result`辞書のキー）であり、テスト側のみを修正して解消。
実装コード自体は当初から正しいパスで実装されていた。この誤りは早期にpytestで検出でき、
実データ・実UIには一切影響していない。

未実施: Viewer再起動（`systemctl --user restart dangou-viewer`。本サイクルはPlanどおり
実施しない。Python側変更のため次回反映には要再起動）、実データでのUI目視確認
（次回L12×R12本番trialでFINAL_REFLECTIONが生成された後）、公開向け`last_word`フィールドの
Engine/prompt側追加（Planに記載の代替案、別サイクル）、`/final-reflections`ギャラリー
エンドポイント、`_extract_emotion()`の`奸`欠落是正（影響範囲が広いため独立サイクル）、
commit/push。
