# 2026-08-17 21:10 JST | 繰越（キャリーオーバー）と前ラウンド結果がプレイヤーに周知されていなかった問題の是正

`trial_C_20260815_202246 / game01` R4でM03の賞金が¥960,000（他市場の2倍）だったスクリーンショットを起点に調査し、
高騰ではなく「R3でM03が参加者0＝流札→¥480,000繰越」が原因と確定した（賞金計算は仕様どおり正しい）。
続けて「繰越はプレイヤーに周知されているか」を確認したところ、§8.1が公開情報と定義し、システムプロンプト自身も
「各市場の参加者・使用カード（決着後）、勝者と獲得額」を公開と明言しているのに、visible_state/プロンプトのどちらにも
一度も出力されていない実装漏れだと判明した。

**要求:** （プランモードでの調査依頼2件を経て）実装承認。プランどおりコード変更を実行し、フォアグラウンドでテスト・検証まで完了させる。

**実行:** `engine/game.py`の`_build_visible_state()`に`base_prize`/`carryover`を追加し、`_phase_settlement()`で
`market_results`/`new_carryovers`から前ラウンドの参加者・勝者・獲得額・高騰有無・繰越額サマリ（`_last_round_results`）を
構築して次ラウンドのvisible_stateに公開。`llm/prompt_builder.py`に`_render_market_line()`（内訳表示）と
`_render_last_round_results()`（「## 前ラウンド」セクション）を新設し、negotiation/commit両プロンプトに挿入。
`viewer/static/index.html`/`style.css`に繰越バッジと流札（不成立）表示を追加。`doc/…_season2.md`§9.1を更新。
`tests/test_prompt_market_info.py`（新規9件）・`tests/test_viewer.py`（2件追加、うち1件は実ログ
`trial_C_20260815_202246`のR3/M03・R4/M03を直接検証）を作成。

**検証:** `uv run pytest tests/test_prompt_market_info.py tests/test_viewer.py -v` 11件PASSED、
`uv run pytest tests/ -q` **382件PASSED**（既存371件＋新規11件、失敗ゼロ、回帰なし）、
`uv run python scripts/dry_run.py` がBot12人版で例外なく完走（イベント数566、繰越発生24件確認）。
LLM API呼び出しはこの検証には使用していない。

**発見:** 実ログ全文検索で「繰越」「キャリーオーバー」「流札」の言及は全12R×9人ぶんでゼロ件だった一方、
繰越で賞金が2倍に見えたR4だけ「高騰」への言及が89件に急増（R3は0件、R5は1件）。P04は「今回のM03は96万円と魅力的です」、
P01は「基本賞金96万×2=192万」（繰越後の96万を誤って基本賞金として計算）と発言しており、情報欠落が
「高騰への誘引」という誤解を生みM03への参加者集中を招いていたことを確認した。

**結論:** エンジンの賞金計算ロジックは変更不要（`engine/market.py`/`engine/settlement.py`無変更）。
プロンプトは1ラウンドあたり数行増えるのみだが、これまで一度も観測できなかった前ラウンド結果・繰越理由が
初めて見えるようになるため、今後の試合の挙動は過去ログとは異なりうる（ルール変更ではなく公開情報の周知漏れの是正）。

**出力:** `engine/game.py` / `llm/prompt_builder.py` / `viewer/static/index.html` / `viewer/static/style.css` /
`doc/uso8000000_dangou_card_spec_v0_8_season2.md` / `tests/test_prompt_market_info.py`（新規） / `tests/test_viewer.py`
