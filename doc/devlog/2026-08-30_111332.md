# 2026-08-30 11:13 JST ｜ サイクル8.4: 仕様書 v0.8 の作成

## 要求

サイクル8.0（監査）〜8.3（プロンプト仕上げ）で確定・実装した v0.8 の内容を、v0.7 と同じ「差分レギュレーション」
形式の仕様書として新規作成する。既存の v0.5 / v0.6 / v0.7 は歴史として変更しない。本文は指定された「仕様書
本文」をそのまま用い、独自の加筆・要約・言い換えはしない。§13 は `doc/prompt_dump/actual_prompts_20260830_v2.md`
の system_prompt 全文コードブロック（8,187字）をそのまま転記する。

## 実行

- 出力先を確認したところ `doc/uso8000000_dangou_card_spec_v0_8_season2.md` は既に存在し（2026-08-16付・909行・
  「Season 2 完全版」）、指示どおり同パスへ作成すると上書き消去になり、`README.md:89` の §9.6 参照と
  `doc/analysis/l12_r12_20260828_review.md:307` の行番号引用が壊れることが判明したため、プラン提示で報告し
  判断を仰いだ。承認により、新規ファイルは別名 `doc/uso8000000_dangou_card_spec_v0_8_regulation_season2.md`
  として作成し、両文書の冒頭にそれぞれ1行の相互参照注記を追加する方針（オプションB）で実施した。
- `TZ=Asia/Tokyo date` でタイムスタンプ取得後、仕様書本文をそのまま新規ファイルへ書き込み、冒頭引用ブロックへ
  承認済みの1行（既存完全版は改訂前スナップショット・食い違いは本文書優先）を追加した。§13 のプレースホルダは
  `doc/prompt_dump/actual_prompts_20260830_v2.md` の `## 1. system_prompt` コードブロック（:48-199、152行）で
  置換した。
- 既存の完全版 `doc/uso8000000_dangou_card_spec_v0_8_season2.md` の冒頭引用ブロック末尾に、承認済みの1行
  （regulation 文書への参照・食い違い時は regulation 優先）のみを追加し、他の行は変更していない。
- `README.md:89` 付近に、2文書の役割分担（完全版=2026-08-16スナップショット／regulation=2026-08-30改訂差分、
  食い違いは regulation 優先）を1段落追記した。既存の §9.6 参照は変更していない。`rules/project.md` は
  仕様書ファイルパスへの参照行が無く、記述はすべて v0.8 前提で既に整合していたため変更していない。

## 検証

- §13 の転記内容を、新規ファイルとダンプ元 `actual_prompts_20260830_v2.md`（行48-199）からそれぞれコード
  ブロックを抽出し SHA-256 ハッシュで突合。完全一致（8,188バイト＝本文8,187字+改行）を確認した。
- `uv run python scripts/dump_prompts.py` で `doc/prompt_dump/actual_prompts_20260830_v2.md` を再生成後
  `git status --short doc/prompt_dump/` が空であることを確認し、現物と仕様書§13が実装出力と一致していることを
  裏付けた（LLM API は一切呼んでいない）。

## 発見

- `doc/uso8000000_dangou_card_spec_v0_8_season2.md` という「v0.8」名の仕様書が既に存在していたため、指示
  どおりの同名新規作成は既存文書の破壊的上書きになる。この衝突を事前検出し、`_regulation_` サフィックスの
  別名で作成する判断を仰いでから実施した。
- README / `rules/project.md` は「v0.7 を現行として指す行」がゼロ件だった（`rules/project.md` に仕様書
  パス参照自体が無く、README の仕様書参照は既に v0.8 完全版を指していた）。参照更新は該当0件だったため、
  代わりに承認済みの役割分担注記のみを追加した。

commit/push未実施。
