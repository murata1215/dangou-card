# 2026-08-22 14:16 JST ｜ サイクル: FINAL_REFLECTION（脱落者の最終コメント）実装

要求: 事前に承認済みのPlan（`/home/uso8m/.claude/plans/quirky-sprouting-hennessy.md`）に従い、脱落確定ラウンドの末尾で脱落者本人に一度だけ最終コメント（敗因・他プレイヤー評価・最後の一言）を書かせる、演出/記録専用の機能を実装する。ゲーム結果・生存判定・払い戻しロジックには一切影響させない。LLM API送信・trial再実行・Viewer再起動・commit/pushは今回のスコープ外。Viewer側の表示対応も本サイクルのスコープ外（実データがまだ無いため次サイクルへ）。

実行: `engine/config.py`に`final_reflection_enabled`（既定False）/`final_reflection_max_chars`(2000)/`final_reflection_max_tokens`(1000)を追加し、`default_8_s2()`/`baseline_v1_s2()`のみで有効化した。`engine/game.py`に`_phase_final_reflection()`を新設し、Reflectionフェイズ直後・`_log_round_complete()`直前に実行する。対象は「`is_alive=False`かつ`elimination_round==round_num`」でstateから導出し、`self._final_reflection_done`集合で1脱落=1回を保証する（engineが同一脱落で`ELIMINATION`+`FORCED_LIQUIDATION`等2 eventを出しても二重発火しない設計）。`_build_elimination_context()`で`logger.events`から該当ラウンド・該当playerのイベントを走査し、liquidation内訳・reason_label（契約違反/破産/R12生存条件未達の3種、AUTO_COMMIT_FAILUREはlabelを補強）を合成する。`llm/prompt_builder.py`に`build_final_reflection_prompt()`を新設し、既存の`_render_memory_block`/`_render_last_round_results`/`_render_message_list`を再利用して脱落理由・清算結果・当ラウンド結果・Handover Memoryを描画し、3キーJSON（`emotion`/`defeat_cause`/`comment`）を要求する。`llm/response_parser.py`に`parse_final_reflection()`を新設し、`_recover_memory_field`を`_recover_string_field(stripped, key)`へ一般化して`comment`キーにも対応、4段フォールバック（JSON正常→他キー組立→復旧salvage→プレーンテキスト受理）で最終手段まで失敗させない設計にした（Handover Memoryとは逆に「失うより残す」安全側）。`llm/llm_agent.py`の`_call_llm()`に`max_tokens_override`引数を追加（既定None、後方互換）し、`final_reflect()`から`final_reflection_max_tokens`を渡す。`final_reflect()`は`self._memory`/`memory_history`を一切変更せず、結果を`self.final_reflection`にのみ保持し、`_update_last_log_final_reflection()`で`llm_logger`の最新エントリへ`final_reflection_status`/`chars`/`truncated`/`emotion`を後付けする。`final_reflect`は`PlayerAgent`基底クラスには追加せず、`Game`側の`getattr`+`callable`チェックでduck-typing判定するため、Bot/StubAgentは無変更でスキップされる。

検証: `tests/test_final_reflection.py`を新規41件で追加（発火条件9件、elimination_context導出6件、prompt描画6件、parser edge case9件、LLMAgent.final_reflect()単体8件、実ログクロスチェック1件、他）、全PASS。既存`tests/test_memory.py`64件は無回帰で全PASS。全体回帰`uv run pytest tests/ -q`は849 passed（808既存＋41新規）。過程で`tests/test_model_matrix.py`の4件がPhase3実ゲーム経路（`build_phase3_config()`が`baseline_v1_s2`使用）でP01が意図通り脱落しFINAL_REFLECTIONが末尾に追加発火したため`logical_calls`期待値と`max_tokens`捕捉ロジックが陳腐化して失敗した。これは仕様どおりの正しい挙動のため、テスト側を修正（`logical_calls`期待値を+1、`captured["max_tokens"]`は`setdefault`で本編最初のcallのみ記録するよう変更）して整合を取り、`tests/test_model_matrix.py`101件全PASSに回復させた（`scripts/model_matrix.py`・Engine本体は無変更）。`uv run python scripts/dry_run.py`（12人版Bot専用、`final_reflection_enabled`は既定Falseかつ`final_reflect`未実装のBotのみ）は例外なく完走（578イベント）。`git status --short logs/`は空で、実trialログへの書き込みが無いことを確認済み。読み取り専用の分析で`logs/llm/trial_C_l12_r12_20260822/game01_events.jsonl`のGAME_END確定値（`eliminated`7件・`survivors`5件、重複なし）に対し`_build_elimination_context()`と同じ導出ロジックを手動再現し、7脱落（P11 R3/P02 R5/P09 R6 AUTO_COMMIT_FAILURE/P06 R9/P01 R11/P07 R11/P12 R12 SURVIVAL_CHECK）が期待どおり1回ずつ、5生存者（P03/P04/P05/P08/P10）が0回として導出できることを確認し、結果を`.devrelay-output/final_reflection_targets_2026-08-22.md`に出力した（ログ・seat_mapへの書き込みなし）。

発見: 本機能を`baseline_v1_s2`で有効化したことにより、同プリセットを実ゲーム経由で使う`scripts/model_matrix.py`のPhase3統合テストが、意図せず「ゲーム本編最後のLLM call」の想定を崩すという相互作用が実際に起きた（プレイヤーが脱落すればFINAL_REFLECTIONが必ず本編より後に1回追加される設計のため）。このクラスの相互作用は、`baseline_v1_s2`/`default_8_s2`をfixtureとして使う他のテスト・スクリプトでも今後同様に起こり得るため、既存資産で同プリセットを使うものを新規に書く／改修する際は「ゲーム内最後のLLM callがFINAL_REFLECTIONである可能性」を前提に含めるべき、という運用上の注意点が判明した。

未実施（次サイクル候補）: Viewer側でのFINAL_REFLECTION表示（脱落者本人の最終コメントカード等）。理由は実データ（`final_reflection_enabled=True`での実trial）がまだ存在しないため、表示仕様を実データなしで確定させるのは時期尚早と判断（Plan記載の判断どおり）。次にS2プリセットで実trialを走らせた後、`FINAL_REFLECTION`イベントのペイロード（`comment`/`emotion`/`defeat_cause`/`status`等）を見ながらViewer統合に着手するのが妥当。
