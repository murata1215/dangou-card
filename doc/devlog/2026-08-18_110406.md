# 2026-08-18 11:04 JST | サイクルE: Phase 2 コスト計算修正（usageベース _usage_cost() へ統一）

## 要求
Phase 1 では既に「実測usageベース」のコスト計算 `_usage_cost()` に修正済みだったが、Phase 2 の
コスト記録経路には旧式の `estimate_cost(input, output)` 直呼び出しが残っていた。Gemini の hidden
thinking・xAI等のcache割引・Anthropicのusage慣習差を Phase 2 でも正しく反映させる。実API呼び出しは
禁止、pytestのみで検証すること。

## 実行
まず現行コードを読み、正確なバグ箇所（`run_phase2()` 内の1行）と影響範囲を確定した。Phase 2 は
1モデルにつきAPI呼び出しがちょうど1回（parse correctionによる再送なし、スクリプト層リトライなし）
であることを確認したため、usageの合算処理は不要で、`_usage_cost(info, usage)` への単純置換が構造的
に正しいと判断。1行を修正し、`_FakeGameJsonUsageAdapter`（有効な交渉JSON＋任意usage注入）を新設して
Gemini hidden thinking・xAI cache割引・cache+reasoning併存・Anthropic正規化・通常ケース非回帰
（4モデルparametrize）・欠損フィールド耐性・アダプタエラー時0円・state/JSONL/budget3箇所一致・
レポート反映・API1回のみの構造的証明、の12テストを追加した。

## 検証
`uv run pytest tests/ -q` で556件全PASS（既存543件+新規13件相当。うち新規は12テスト関数、1つは
parametrize×4展開）。実API呼び出しは0回、課金$0。Gemini M3ケースで旧計算$0.0000930→新計算
$0.0017580（約18.9倍、hidden thinkingが正しく計上されるようになった）を確認。

## 発見
Phase 2 はまだ一度も実行しておらず成果物（`phase2_calls.jsonl`・state の phase2 レコード）が存在
しなかったため、履歴の再計算・書き換えが一切不要という好条件だった。同一バグが Phase 3
`BudgetedAdapter.complete` にも残存していることを確認したが、予算ガードロジックへの影響評価が別途
必要なため今回はスコープ外として申し送りに記録した。
