# 2026-08-26 13:00 JST ｜ サイクル5.3: replay probe 再実行（5.2 締め指示）

## 要求
Cycle 5.1（列挙のみ、契約系変換率 baseline0/81→patched2/81・McNemar p=0.50）に対し、Cycle 5.2 で選べるアクションに15字前後の説明句を追加したコードを、同じ27レコード（P02 8/P03 6/P04 1/P06 4/P09 7/P11 1）・同じN=3で再計測し、変換率の変化を直接比較する。LLM API呼び出しは本Cycleに限り許可、`scripts/replay_probe.py --execute --samples 3 --max-cost-usd 0.90`のみ経由。コード変更は原則禁止（`engine/` `llm/` `tests/` 変更禁止）。

## 実行
sha256突合でbaseline prompt 27/27一致・patched system_prompt 27/27一致(7,079字)・patched user_prompt 0/27一致（+59〜+132字、平均+108.1字）を確認し、baselineは5.1流用と判断（provider ドリフト未分離を明記の上で）。preflight実測 $0.698504（上限$0.90以内、4種APIキー全present）を確認後、`--execute --variants patched --samples 3 --budget-mode hardened --max-cost-usd 0.90 --max-output-tokens 2000 --sleep-seconds 1.0 --rate-limit-cooldown 20.0 --out-dir logs/replay_probe/20260826_124634_cycle53` を実行。81 call完了、実測コスト**$0.281507**（見積の約40%）、`terminated_reason: null`、`budget_block_count: 0`。コード変更は0行（`scripts/replay_probe.py` mtime 1787700363で不変）。

## 検証
契約系変換率: baseline(5.1流用) 0/81 (0.0%, CI[0.0,4.53%]) → 5.1 patched 2/81 (2.47%, CI[0.68,8.56%]) → **5.3 patched 1/81 (1.23%, CI[0.22,6.67%])**。json_valid 81/81・parse_ok 81/81・truncation 0/81（5.1 patchedの`no_json_found`1件が解消）。mean_mentions_contract 0.5679→0.6296、type_b 0.2222で不変。アクション分布: dm46/broadcast19/pass15/contract_propose1（5.1: dm45/broadcast25/pass6/contract_propose2/anon2/error1）。index15(P06 R3t2/gpt-4.1-mini)の5.1での2/3再現は**なし**（3/3ともdmに回帰）。新規転換は index4(P02 R3t5/kimi-k2.6) sample2の1件（type_b市場×2+card 1件、obligor自他混在、JSON妥当）。McNemar（discordant b=2,c=1）p≈1.0、統計的有意差なし（n極小と明記）。pytest 1304 passed（不変）。`logs/llm/trial_C_l12_r12_20260824/`は`find -newer`で0件（未変更）。`engine/` `llm/` `tests/`に本Cycle起因の変更なし（既存Cycle5.2差分のみ）。commit・main run・Viewer再起動は未実施。

## 発見
**5.2の説明句は契約系変換率を統計的に有意には動かさなかった**（2/81→1/81、CI大幅重複）。契約系イベントは特定モデル・特定文脈依存の低頻度事象で、発生位置がindex15→index4へ移動しただけ。JSON妥当性・パース成功率は100%に改善（副次効果）。`.devrelay-output/replay_probe_report_5_2_20260826_125843.md`を作成し、5.1の2devlogが参照していた未作成レポート問題を一部解消（5.1相当は依然未作成、引き続き未解決として引き継ぐ）。seed 1202本走に進む根拠は弱いと結論。
