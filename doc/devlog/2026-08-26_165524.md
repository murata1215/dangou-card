# 2026-08-26 16:55 JST ｜ サイクル5.4: replay probe 選抜やり直し（合意直後手番）と再計測

## 背景

5.1/5.3のprobeは「契約を口にした手番」27件（多くは自分から持ちかけた直後）を対象にしていた。`contract_propose`が最も自然になるのは**相手から合意の返事が届いた直後の手番**のはずなので、選抜定義をそこに取り直し、同じ小型・非思考モデル群で再計測した。

## やったこと

- `game01_events.jsonl`にはDM本文・宛先が無く、`llm_logs`の`response_text`からのみ復元できることを実測確認。DM配送タイミング（t送信→t+1で受信側promptに反映）も実データで検証。
- 文字列＋文脈判定（LLM不使用）で「持ちかけDM→合意の返事」チェーンを抽出: 検出54件→STRONG（否定・条件変更語なし）35件→対象手番38件（proposer 19 / replier 19）。弱い合意14件は5.5用に温存し計測対象外。
- `scripts/replay_probe.py`に`--targets-file`モードを新設（既定27件テーブルの挙動は1バイトも変えず、dry-runバイト比較で確認済み）。`ReplayTarget`/`PreparedTarget`に`meta`フィールドを追加、`_load_targets_from_file`/`_read_targets_doc`/`_index_negotiation_records`を新設し、schema/role/座標実在を全件検証（不整合は`TargetsFileError`で即停止）。role分割集計（`_bucket`/`_action_dist`にrole引数）とMcNemar正確検定（`_mcnemar_exact`/`_paired_counts`、`math.comb`のみ）を追加。
- `logs/replay_probe/targets_cycle54.json`（固定パス・5.5再利用可）を生成スクリプトから作成し、実ログと突合検証（model_id・closing_replaced・events行番号）して38件全件が実在することを確認。
- `tests/test_replay_probe_targets.py`新規39件（既定モード回帰・targets-fileモード検証・McNemar golden vector・role分割paired比較）全PASS。既存スイート含め`uv run pytest tests/ -q` 1343 passed。
- preflight見積$0.6256（$0.90上限内）→スモーク4call（$0.0179）→本計測76call実行、実測$0.283179。

## 結果

- 契約系変換率: baseline 0/38（0%）→patched 1/38（2.63%）。McNemar正確検定 b=1,c=0, p=1.0（有意差なし）。
- role別: proposer 0/19→1/19（5.26%）、replier 0/19→0/19（変化なし）。転換した唯一の1件は、相手（P07）の破産危機を契機にproposer側が`contract_propose`を選んだケース。
- 5.1（0%→2.47%, p=0.50）・5.3（2.47%→1.23%, p≈1.0）と同水準の低い変換率が、母集団を「合意直後」に入れ替えても再現。n_discordant=1のため統計的有意性の議論自体が成立しない小n。

## 結論・次

「合意直後手番」でも契約系への転換はほぼ起きず、5.1/5.3の結論（小型・非思考モデルの限界）を覆す証拠は今回も得られなかった。5.5（上位モデルprobe）に進む価値は高いと判断する。`targets_cycle54.json`（38件）と`excluded_weak`（14件の弱い合意）はそのまま5.5で再利用可能な状態で保存済み。

## 非変更・非実施

`engine/`/`llm/`/既存`tests/`は無変更。`logs/llm/trial_C_l12_r12_20260824/`は読み取り専用のまま（`find -newer`で0件確認）。5.5・R12実戦起動・commit/pushは実施していない。

詳細: `.devrelay-output/replay_probe_report_5_4_20260826_165524.md`
