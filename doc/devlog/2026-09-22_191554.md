# 2026-09-22 19:15 JST ｜ サイクル10.16: L12R12 総括記事の公開

L12R12本戦（`trial_C_l12r12_dr_1201`、上位モデルを実験席として初めて混ぜた12体×12R）の総括記事を1本書き、
既存導線（`doc/blog/<date>/posts/*.md` → `scripts/publish_next.py` → PixBlog）に乗せて実際に公開してほしい、
という要求だった。事実は `doc/analysis/l12r12_dr_1201_facts.md`・`l12r12_dr_1201_timeout.md`・
`logs/llm/trial_C_l12r12_dr_1201/game01_events.jsonl` からのみ取り、数値は推測しない条件。

新シリーズ `doc/blog/20260921/`（`plan.md`＋`posts/17_summary.md`、3,185字）を作成。首位P06がR2以降
交代0回・8回挑まれ8回全敗でも逃げ切ったこと、型C契約が前回6体戦の0件から提案40・成立25・発火18・
445万円に化けたこと、P11(Claude Opus 4.8)がR9/R10コミットでDevRelayの504（応答打ち切り57件）に遭い
自動代行が弱いカードから消化した結果ROYAL_FLUSHが残り、R12で首位P06のFLUSHを撃破した経緯（故障が
生んだ偶然、と明記）を中心に構成。断定的なベンチマーク表現は避けた。画像2枚
（`17_results.png`・`17_roster.png`）は`scripts/blog_images.py`の`gen_results_chart`/`gen_roster_table`を
repo外の使い捨てスクリプト（`/tmp/c1016/gen_images.py`）からimportして生成し、目視確認済み。

`scripts/publish_next.py`の`DATES`に`"20260921"`を追加してorder=17を選べるようにし、
`--dry-run --order 17`で選定・画像2枚・tweet_text加重245/280・x_hook加重177/280・重複タイトルなしを確認後、
`--order 17`で実行。post_id=270が作成されたが、GETで確認すると`status=draft`のままURLが404だった。
原因は`blog/pixblog_client.py`のdocstring通り「POST /posts はpayloadのstatusに関わらず常にdraftで作成、
PATCHで明示的にpublishedへ遷移が必要」で、`publish_next.py`はこのPATCHを一度も呼んでいなかった
（既存16記事が全て「原稿済（未投稿）」のまま放置されていたのも、過去の試行がこの穴で公開に至らなかった
可能性がある）。`run_publish()`のPOST直後に`client.publish(post_id)`呼び出しを1箇所追加して修正し、
公開URL `https://pixblog.net/u/uso8m/s2-0921-17-summary` をcurlで200確認。`plan.md`/`INDEX.md`は
publish_next.pyの既存ロジックで自動的に「公開済」「公開完了」へ更新された。過去2シリーズの
plan.md/postsは無変更（`git status --short`で確認）。
