# 2026-08-24 12:09 JST ｜ サイクル2.1: Cycle 2 wording-only fix（境界事例2文の中立化）

## 要求
Cycle 2 Human Review（`.devrelay-output/cycle2_human_review_20260824_083108.md`）で唯一の留保事項として残った、
double-up Promptの不足警告文と契約義務の競合表示（type_b_card分岐）の**2文だけ**を、結果断定を含まない
数値事実・ルール事実のみの表現に置き換える。ゲームルール・計算式・Engineは一切変更しない wording-only fix。

## 実行
`llm/prompt_builder.py` の2箇所のみを変更した。
1. `build_double_up_prompt()`: `⚠ DOUBLEを選んだ場合、Finance後の現金は不足します（不足額…円）。不足するとMANDATORY_REPAY_FAILED（強制清算・脱落）になります。`
   → `DOUBLE選択時のFinance後現金見込み: -298733円（強制最低返済額649587円に対する差額: -298733円）`
2. `_obligation_conflict_warnings()` の type_b_card分岐のみ: `⚠ R6: カード使用義務が2件（1ラウンドに出せるのは1枚 → 必ず違反で脱落）`
   → `R6: カード使用義務が2件あります。1ラウンドに提出できるカードは1枚です。`
同関数内の他2分岐（市場競合・参加/不参加競合）と `_render_finance_block()` の不足警告文（L308）はユーザー指示によりスコープ外として一切変更していない。
影響した既存テスト3関数（`test_prompt_finance.py`のD1/D4系1件ずつ、`test_llm.py`の1件）のみ最小更新し、新文の直接assertと`⚠`/断定語の非存在assertを追記した。

## 検証
回帰テスト1239件（新規0件・純粋な文言更新のみ）全PASS、`dry_run.py`（12人R12）正常終了。
`git diff --stat engine/` はCycle 1由来の`game.py`89行のまま増減ゼロ、engine中核5ファイル（autocommit/finance/settlement/contracts/actions）は差分ゼロを維持。
`llm/prompt_builder.py`の本サイクル純増分は+4/-4行（2箇所）のみ。
API-freeの再チェックスクリプトでHuman ReviewのCase 3/Case 5と同一入力からプロンプトを再生成し、旧文0ヒット・新文出現・数値（-298733/641267/649587/350854/1290854等）と義務一覧・処理順の完全維持・助言語彙12語スキャンがHuman Review時点の5件から増加していないことを確認、`.devrelay-output/cycle2_wording_recheck_20260824_120820.md`に出力した。
非変更を指示された他2分岐・L308が実際に変更前と一致（`⚠`・「必ず違反で脱落」がそのまま残存）していることもコード再取得で確認済み。実APIコール0件。

## 発見
`_obligation_conflict_warnings()`には`必ず違反で脱落`を含む分岐が実は3つ（type_b_card / type_b_market異なる市場 / market×no_market同一市場）存在し、
今回はtype_b_card分岐のみを中立化したため、同一関数内・同一コードベース内で意図的に文体が不揃いな状態が残った（他2分岐とL308は次サイクル以降の判断材料として温存）。
commit/push・Cycle 3着手・R12実戦起動は行わず、ここで停止。
