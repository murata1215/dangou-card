# 2026-08-26 08:46 JST ｜ サイクル5.1: replay probe --execute実行（D1修正の効果をLLM API実弾で検証）

要求: Cycle 5で作成したscripts/replay_probe.py（--dry-runのみ実装済み）に--execute経路を実装し、trial_C_l12_r12_20260824の27レコード（26キー、P03 R6 t8のみ2レコード）をbaseline（ログのprompt無改変）/patched（Cycle 5修正後のprompt）の2 variant×N=3サンプルで実際にLLM APIへ再送信し、契約系アクション（contract_propose/card_trade_propose/bounty_post）への変換率がbaseline 0/27から動くかを実測する。コード変更はscripts/replay_probe.pyのみ、logs/llm/trial_C_l12_r12_20260824/は読み取り専用、事前見積が承認上限$0.90超なら実行前に中止、engine/llm/testsの変更が必要なら即停止という条件下で、Plan作成→承認→実装まで実施。

実行: scripts/replay_probe.pyへ--execute経路を新規実装（P1: --dry-run/--execute同時指定を明示エラー化、P2: 存在しないget_adapterではなくcreate_adapterを正しくimport、P3: PreparedTargetでbaseline/patched promptをレコードごとに1回だけ生成しキャッシュ＝非冪等な二重置換防止、P4: 締め指示置換が全27件で成功したことを事前ゲートでassert、P6: model_key解決でL系キーを優先しM5/L5等の記録tier誤りを回避、P8: realistic/worst_case/hardened 3方式の事前見積を実装、P10/P11: 出力先自動作成とlogs/llm/配下への出力拒否）。追加CLI: --samples/--variants/--max-output-tokens/--budget-mode/--preflight-only/--only/--models/--sleep-seconds/--rate-limit-cooldown/--resume。load_dotenv()を追加（他のllm系スクリプトと同じ規約）。llm.adaptersのimportはrun_execute()内でのみ行い、--dry-run経路には波及させない設計を維持。GameCostBudget（本番と同一クラス）で呼び出しごとに事前予約し、上限到達時はサンプル境界でgracefulに停止する二層ガードを実装。

検証: (1) --dry-run回帰: 27件抽出・baseline実測$0.0924・2 variant見積$0.1848で旧実装と完全一致、llm.adapters未import確認。(2) --preflight-only: realistic N=3見積$0.702764（≤$0.90で通過、exit 0）、hardened N=3見積$1.340888（>$0.90でexit 1、中止メッセージ表示）を両方確認。(3) 実弾スモーク（--only 8,9 --samples 1 --budget-mode hardened --max-cost-usd 0.05）: 4 call・実測$0.0020で正常完了、JSON解析・アクション分類・budget snapshotすべて正常動作を確認。(4) 本実行（--execute --samples 3 --budget-mode realistic --max-cost-usd 0.90）: 162/162 call完了、terminated_reason=null（上限未到達）、実測コスト$0.528249（見積の75%、承認上限の59%）。(5) uv run pytest tests/ -q → 本実行後も1299 passed, 0 failed。(6) git diff --statにengine/・llm/（prompt_builder.py以外）・bots/・viewer/は一切出現せず、tests/はCycle 5で既承認の2ファイルのみ。(7) logs/llm/trial_C_l12_r12_20260824/のmtime不変確認。(8) MEMORY.md不在確認（前回同様スキップ）。

発見: (1) 主指標（契約系変換率）はbaseline 0/81（0.0%、Wilson95%CI [0.0%,4.5%]）→patched 2/81（2.47%、CI [0.7%,8.6%]）で、McNemar厳密両側二項検定p=0.50（discordant数n=2、有意差なし）。「完全に回復した」とは言えないが「効果皆無」とも言えない中間的な結果。(2) 変化はP06（gpt-4.1-mini）のR3t2レコード1件に集中: baseline=dm×3/3サンプル→patched=contract_propose×2/3サンプル（同一model・同一会話文脈でprompt差し替えのみが差分）。同じP06のR6t8レコードでもbaseline=dm×3/3→patched=anonymous_broadcast×2/3という副次的な変化を確認。他26レコード（P02 kimi・P03/P04 deepseek・P09 gpt-4.1-mini・P11 grok）では3サンプルとも変化なし。(3) 実測コストは事前見積（realistic方式）の75%（$0.528249 vs $0.702764）で、hardened方式の見積$1.340888よりも遥かに保守的だったことを確認（0.80 tok/char＋出力満額の見積は日本語promptに対しても十分保守的）。(4) patchedのtruncation_rateがbaseline 0/81→patched 1/81（P02 R7t8、finish_reason=length）とわずかに増加、system_prompt長増加（5,901→7,079字）の副作用の可能性。(5) JSON有効率・parse成功率はbaseline/patchedともほぼ同水準（98.8%）で、prompt変更によるJSON出力品質の劣化は見られない。詳細は.devrelay-output/replay_probe_report_20260826_082742.mdに全文出力。commit/push・本走・次Cycleには進まず本タスクで停止。
