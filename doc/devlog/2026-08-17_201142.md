# 2026-08-17 20:11 JST | DeepSeek V4 Flash（M6/L6）thinking暴走の是正 — 原因特定・8コール実験・恒久設定変更

seed=701 の6体12R試験で `L6`（DeepSeek V4 Flash）がAPIエラー0件なのにJSON率P04 74%/P05 87%と低く、壁時計3734秒中78%を消費していた事象の原因を読み取り専用調査で確定し、L6単体・8コール・$0.10上限の実験で検証したうえで恒久設定を変更した。

**要求:** 「max_tokensを8000に上げる」等の力技ではなく根本原因を特定し、抑制策があるかをAPI実験（8コール以内・$0.10以内）で確認してから、合格すればM6/L6に恒久変更を適用する。ゲーム本体・viewer・systemdは一切触らない。

**実行:** seed=701の実ログ集計（`length`時は全件`reasoning_tokens==output_tokens==4000`＝thinkingがmax_tokens枠を食い潰し`content`が空）とコード確認（`llm/adapters.py`の`content`空時`reasoning_content`フォールバック）、DeepSeek公式仕様（thinking既定ON・effort=high、`extra_body={"thinking":{"type":"disabled"}}`で無効化可）を照合し原因を断定。`scripts/model_smoke.py`に`--suite thinking_matrix`を追加し、`content`/`reasoning_content`を分離観測する`complete_with_fields()`を新設（`llm/adapters.py`は無変更）。baseline(thinking ON)×2 / nothink×max_tokens(4000×1, 2000×3, 1000×2)の8条件でL6に実APIコールを実行した。

**検証:** `uv run pytest tests/test_model_smoke.py -v` 20件PASSED、`uv run pytest tests/ -q` 371件PASSED（既存361件＋新規10件、MODEL_REGISTRY変更後も回帰なし）。実APIコールは計画8回・実行8回。

**発見:** 8/8成功・0/8 length・8/8 JSON成功。`nothink_*`は全条件で`reasoning_tokens=0`（無効化が確実に効く）。latencyはbaseline 2.3〜5.0秒→nothink 1.4〜2.8秒に改善（seed=701の`length`時46秒から大幅短縮）。副次発見: baselineの1コールは`finish_reason=stop`の成功呼び出しなのに`content`が空で`reasoning_content`側に最終回答があった＝既存フォールバックは失敗時専用ではない。`nothink_1000`(2/2)・`nothink_2000`(3/3)とも成功したが、コスト・latency差が僅かなことと18体本番のフェイズ多様性を踏まえ、安全側のmax_tokens=2000を採用。`llm/models.py`のM6/L6（同一model_id）双方に`extra_params={"thinking":{"type":"disabled"}}`・`max_tokens=2000`を追加。実測コスト$0.001396（約¥0.21、上限の1.4%）。

**結論:** L6×6を18体本番ロースターに投入してOK。

**出力:** `scripts/model_smoke.py` / `tests/test_model_smoke.py` / `llm/models.py` / `logs/smoke/smoke_L6_thinking_matrix_20260817_200650.jsonl` / `.devrelay-output/l6_thinking_report.md`
