# 2026-08-26 22:42 JST ｜ サイクル5.5: 上位・思考モデル replay probe

## 背景

5.1〜5.4で、軽量・非思考モデル5種は「合意直後の手番」でも`contract_propose`転換率が1/38（2.63%）止まりだった。「これはモデルの推論能力の問題か」を切り分けるため、人間承認済みの案C（4モデル×19手番、上限$4.00、realistic基準判定）で上位・思考モデルへ同一手番をreplayした。

## やったこと

- `targets_cycle54.json`（5.4の38件STRONG＝19チェーン×2席）から、チェーンごとに1席だけを役割が偏らないよう交互に採った19件（proposer 10 / replier 9）を`select_cycle55_subset.py`（固定インデックス・API 0件）で`targets_cycle55.json`へ生成。5.4で唯一`contract_propose`に転換した★手番（P03 R6 t10, proposer）を含むことを検証。
- `scripts/replay_probe.py`に`--model-override <key>`（`_preflight`前に`dataclasses.replace`でモデルのみ差し替え、`--dry-run`併用禁止）と`--thinking {off,medium}`（provider別`request_options`テーブル。Anthropicは`adaptive`+`effort`実証済み形）を追加。既定モードは`diff -rq`でバイト一致を確認済み（非回帰）。`summary.json`に`by_model`セクション（Wilson CI・role/action分布・reasoning_tokens・truncation率）を追加。
- `llm/models.py`にQ2で許可された4設定値のみで`gpt-5.6-terra`（TERRA）を新規登録。`tests/test_replay_probe_targets.py`へ24件追加、全体`uv run pytest tests/ -q` 1373 passed。
- 4モデル1callスモーク実施: **deepseek-v4-pro**はthinking有効でJSON崩壊を再現（Plan §Eの想定通り）→thinking無効へフォールバックして成功。**claude-sonnet-5**は成功（Anthropicの`adaptive`+`effort`形を実測確認）。**gpt-5.6-terra**はHTTP 400（`max_completion_tokens`要求、実在は確認できたが対応にはH2同様のレジストリ変更＋`test_registry_18.py`許可リスト変更が必要で、本サイクルの許可範囲外のため除外）。**grok-4.6**は2回連続タイムアウト（既定`timeout_seconds=60`が常時思考+6,000トークン出力に構造的に不足、これも既存エントリの変更が必要で許可範囲外のため除外）。
- 残った2モデル（deepseek-v4-pro=thinking off、claude-sonnet-5=thinking medium）で19件×2モデル＝38callの本計測を実行。

## 結果

- 契約系変換率: **claude-sonnet-5 0/19（0%）・deepseek-v4-pro 0/19（0%）。合算0/38（0%）**。5.4の非思考モデルpatched 1/38（2.63%）を上回らず、むしろ下回った。
- ★手番では両モデルとも独立に「Free Cash（送金原資）が0円で契約の金銭的義務を履行できない」という同一の理由づけに到達し、`contract_propose`ではなく`pass`（M1）/`broadcast`（H6）を選んだ。
- JSON有効率・パース率100%、切り詰め率0%（両モデルとも6,000トークン上限内）。M1のreasoning_tokens平均491・最大1,275。
- 実測コスト: 本計測$0.474460（スモーク込み合計$0.501524）、上限$4.00の約12.5%。budget block 0件、打ち切りなし。

## 結論・次

「上位・思考モデルなら契約に踏み切る」という仮説は、本試行（各モデルN=19、合計2モデル）では支持されなかった。ただし4モデル中2モデルが運用上の制約（パラメータ名不一致・タイムアウト不足）で除外されており、これはモデルの推論能力とは無関係。gpt-5.6-terra・grok-4.6の対応（レジストリ拡張）と、より大きなNでの再検証を今後の課題としてレジストリ監査へ申し送る。5.6・seed 1202本走・上位ロスター試合・commitには進まず、ここで停止する。

## 非変更・非実施

`engine/`は無変更。`llm/models.py`はTERRA1エントリ追加のみ（ロジック変更なし）。`targets_cycle54.json`は無変更（読み取り専用）。5.6・R12本走・commit/pushは実施していない。

詳細: `.devrelay-output/replay_probe_report_5_5_20260826_224230.md`
