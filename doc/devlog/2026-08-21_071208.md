# 2026-08-21 07:12 JST — L1〜L6 R1〜R3 段階試験の安全停止基盤

## 実施内容

- Phase C専用の `--stop-after-round` を追加した。`GameConfig.num_rounds` は変更せず、
  指定ラウンドのReflectionと`ROUND_COMPLETE`の後に`GAME_STOPPED`を記録して終了する。
- `GAME_END`へ非完走の`completed=false`と`stop_reason=trial_stop_after_round`を記録する。
- manifestとPhase Cレポートに段階停止設定を残す。`--rounds`/`--preset`との併用は拒否し、
  S2の12R意味論を壊さない。
- DeepSeek V4 Flash（M6/L6）の cache-hit `$0.0028`、cache-miss `$0.14`、output `$0.28`
  を、予約・実績精算・BudgetedAdapterの共通計算経路で回帰固定した。

## 検証

- `uv run pytest tests/test_l6_r12_trial.py tests/test_memory.py tests/test_game_cost_budget.py tests/test_model_matrix.py -q` — 150 passed
- `uv run pytest -q` — 743 passed, 4 warnings
- `git diff --check` — 成功

## 実API試験

- 未実行。Google安全ゲートと実行直前のProvider料金照合を人間が確認した後、別承認で起動する。
