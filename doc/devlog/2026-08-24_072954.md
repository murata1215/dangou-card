# 2026-08-24 07:29 JST — Cycle 2: 契約・財務・倍掛けの「事故死」要因を除去（実装完了・停止）

## 背景

前回の12人R12実戦 `logs/llm/trial_C_l12_r12_20260822/` で脱落した7名のうち、
AUTO COMMIT経路（Cycle 1で解決済み）を除く残りが「盤面の事実がプロンプトに
出ていない／engineの実装と食い違う」ことによる事故死だった（Phase A監査済み）。

対象:
- A-1: P11 R3 矛盾契約（型Bカード義務2件同時ACTIVE） — 検出範囲がtype_b_card×2のみで、
  type_b_market×2やtype_b_market+type_b_no_marketの矛盾は未検出だった。
- A-2: P09 R6 手札に無いカードを要求する義務 — 義務行と手札行は表示されていたが
  「義務が要求するランクは手札に無い」という突合注記が無かった。
- A-3: P06 R9 / P01 R11 強制最低返済 — 除数がoff-by-one（`num_rounds - round_num`と
  誤表示、engineの真値は`num_rounds - round_num + 1`）、かつ強制最低返済の実額が
  全プロンプトで一度も提示されていなかった。
- A-4: P01 倍掛け→Finance の同一ラウンド処理順 — 倍掛けプロンプトにその後続で
  Financeが実行され確定支出が発生することが一切書かれていなかった。
- A-5: P07 Entry Fee不足 — A-3の修正で自動的に材料が揃うため個別対応は不要。

## 実装内容（`llm/prompt_builder.py` のみ。engineルールは無変更）

- **P0-1** `_render_finance_block()` / `_compute_finance_forecast()` 新設。
  今Rの借金残高・利息額・利息計上後残高・除数・強制最低返済額（実額）・差引後現金見込みを
  `engine.player.apply_interest` / `compute_mandatory_repayment` を直接再利用して算出し、
  negotiation/commit/reflection/double_upの4プロンプトに注入。
- **P0-2** 「残りラウンド」表示のoff-by-one解消（`num_rounds - round_num + 1`に統一）、
  RULES_SUMMARYの返済数式の説明文を実装（除数の定義）に合わせて訂正。
- **P0-3** `build_double_up_prompt`に同一ラウンドの処理順の事実（Finance実行順）と、
  TAKE/DOUBLE双方を選んだ場合のFinance後現金見込みを数値で併記。
- **P0-4** `_render_obligations_block()`が手札ランク集合を受け取り、`type_b_card`義務に
  「現在の手札にあります/ありません」の事実注記を付与。矛盾検出を3型
  （type_b_card×2 / type_b_market×2異市場 / type_b_market+type_b_no_market同一市場）に拡張。
- **P0-5** `contracts_pending`の描画に「署名した場合の合流後義務プレビュー」を追加
  （既存義務∪提案義務にP0-4と同じ突合・矛盾判定を適用）。
- **P1-1** Entry Feeの実額と自動徴収タイミングを財務ブロックに明記。
- **P1-3** 自分の倍掛け預託額を状態欄に表示。

助言語彙（すべき/推奨します/危険/安全な/おすすめ/注意しましょう）は一切追加していない
（D-7テスト・6スナップショットの機械スキャンで0件を確認）。

## Cycle 1 未了タスクの消化

- `tests/test_prompt_finance.py`（新規、17件）: D-1〜D-7 + P0-5プレビューの受け入れテスト。
  P06 R9（不足30,276円）・P01 R11（不足298,733円）の実測値とengine計算が完全一致することを
  直接検証。
- `tests/test_viewer.py` 拡張（T-V1〜T-V4、4件追加）: `viewer/log_parser.py`の
  `commit_kind`（"auto"/"self"、Cycle 1のA-7機能）がindex.htmlからのみ参照され
  テスト未カバーだった件を解消。COMMIT→REVEAL順・REVEAL単独（後方互換）・
  COMMIT単独（settlement前の中間状態、dedup含む）の4パターンを検証。
- `tests/test_trial_report.py`（新規、9件）: `scripts/llm_trial.py`のC-1（`_roster_summary`の
  12人戦「LLM×8」誤表示バグ再現）・C-2（`trial_manifest.json`からのrun_id/seed読込）・
  C-3（AUTO_COMMIT_FAILUREをAUTO列と別集計）を検証。
  **副産物のバグ修正**: `generate_phase_c_report()`で`total_input`等4変数がfor内で
  毎試合リセットされていたため、`results=[]`で`UnboundLocalError`、複数試合でも
  最終試合分しか集計されない潜在バグを発見・修正（ループ外初期化に変更）。

## 実装外で発見・修正した既存の不整合（Cycle 2起因ではない）

- `tests/test_phase2_schema.py`: P0-2のRULES_SUMMARY文言変更でsystem prompt長が変化した
  ためピン留め値2件を実測に合わせて更新（`test_model_matrix.py`と同型、2026-08-23の
  contract_cancel時と同じ前例パターン）。
- `tests/test_prompt_market_info.py`: Cycle 1で追加済みだった`auto`公開情報キーが
  `ALLOWED_COMMIT_KEYS`許可リストに未反映だったため追記（Cycle 2の変更が原因ではない）。

## 回帰確認

- `uv run pytest tests/ -q` → **1239 passed**（新規30件: finance17 + viewer4 + trial_report9）
- `uv run python scripts/dry_run.py`（12人・seed42）→ 生還0人・R12 condition_not_met（従来通り）
- `uv run python scripts/simulate.py --games 1000`（8人・S1）→ 平均生還者0.07/8（従来通り）
  ※ どちらもbot経路でprompt_builderを通らないため、非回帰の実証は下記diffで行った。
- `git diff --stat engine/autocommit.py` → 0行
- `git diff --stat engine/finance.py engine/settlement.py engine/contracts.py engine/actions.py` → 0行
- 人間レビュー用プロンプトスナップショット6点を `.devrelay-output/cycle2_prompt_snapshots_20260824_072949.md`
  に出力（P06 R9 commit / P01 R11 double_up / P11 R3矛盾3型 negotiation / P09 R6手札不一致 commit /
  P0-5署名前プレビュー negotiation / 助言語彙スキャン=0件）。実APIは一切呼んでいない。

## GO条件チェック結果（人間の判断待ち）

1. D-1〜D-7 全PASS — ✅（17件、`tests/test_prompt_finance.py`）
2. `pytest tests/ -v` 全件PASS — ✅（1239件）
3. `simulate.py --games 1000`の分布不変 — ✅（bot経路は無回帰、engine非改変で保証）
4. `engine/autocommit.py` diff 0行 — ✅
5. `engine/finance.py engine/settlement.py engine/contracts.py engine/actions.py` diff 0行 — ✅
6. スナップショット目視確認 — **未実施（人間の確認待ち）**
7. 助言語彙0件 — ✅（機械スキャンで確認済み、目視は6と合わせて人間待ち）
8. 人間の明示的GO — **未取得**

## 次のアクション

このCycleは計画の「停止条件」に従い実装完了時点で停止する。Cycle 3着手・12人R12実戦の
起動は行わない。人間による6番の目視確認とGO判定を待つ。
