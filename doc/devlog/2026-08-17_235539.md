### 2026-08-17 23:55 JST | サイクルX.X: 18モデル疎通テスト基盤の構築（Hティア追加 + Phase 0〜3スモークテスト）

**要求**: `MODEL_REGISTRY` を6社×2階級12モデル（M1〜M6/L1〜L6）から6社×3階級18モデルへ拡張し、
Opus級の強（H）ティアをフルゲームで誤って回して$10規模の出費を招かないよう、段階分割で
人間が確認しながら止められる低コスト検証基盤を作ってほしいという依頼。総額$1未満が目標。

**実行**:
1. `scripts/discover_models.py`（新規）で各社の models list エンドポイントのみ叩き$0でH候補を
   実発見。`claude-opus-5` / `gpt-5.6-sol` / `gemini-3.1-pro-preview` / `grok-4.6` / `kimi-k3` /
   `deepseek-v4-pro` の実在を確認し、`llm/models.py` にH1〜H6として追加（`doc/uso8000000_model_roster_v1_0.md`
   §3を単価出典に、xAI強ティアはロスター欠落のため既存M4からの推定値＋要再確認コメント付き）。
2. `ModelInfo` に `tier` フィールドと `vendor` property を追加。`get_models_by_tier` /
   `get_model_keys_by_id` / `list_vendor_endpoints` を新設。既存13エントリはM1〜M6/L1〜L7とも
   1バイトも変更せず、スナップショットテスト（`tests/test_registry_18.py`）で保証。
3. `engine/blog/cards_svg.py` の `_tier_label`/`_price_pitch` を修正（旧実装は `startswith("M")`
   判定のためH1が「軽量級」と誤表示されるバグがあった）。`viewer/log_parser.py` のmodel_id逆引きを
   `setdefault`化し、`llm.models.get_model()` の先勝ちルールと意味論を統一。
4. `scripts/model_matrix.py`（新規）にPhase 0（棚卸し・APIなし）〜Phase 3（1ラウンドのみの
   ミニゲーム統合テスト）を実装。実行順は弱→中→強、各ティア内は単価昇順（最高額が必ず最後）。
   `state.json`を唯一の正としフェーズゲート・予算ガード（フェーズ毎/通算/モデル毎の3層）・
   resume/forceを実装。Phase3は`BudgetedAdapter`でMODEL_REGISTRYを書き換えずにmax_tokensを
   コール単位でクランプし、`GameConfig.baseline_v1_s2(num_players=4)`を`model_copy`で
   1ラウンド化して実行。`scripts/run_matrix.sh`でsetsid nohupのデタッチ起動にも対応。
5. `scripts/api_check.py` は `--keys` 必須化（`--all`明示時のみ全走査）で高額Hティアの誤爆を防止。

**検証**: `tests/test_registry_18.py`（20件）と`tests/test_model_matrix.py`（32件）を新規追加し、
すべてフェイクアダプタ・オフライン専用（実API呼び出しゼロ）。`uv run pytest tests/ -q` は
既存369件＋新規52件＝計497件すべてPASSED。実リポジトリパスでもPhase 0とPhase 1 dry-runを
実行し、棚卸し表生成・弱→中→強の実行順・$0コストを確認。

**発見**: ロスター仕様書とレジストリ実装は既に乖離していた（M2はGPT-5.6 Terra→実装gpt-4.1、
L5はkimi-k2.5→実装kimi-k2.6等）。M5/L5・M6/L6は現状同一model_idで単価も一致しているため
実害はないが、`test_duplicate_model_ids_agree_on_economics`で今後の分岐を継続的に検知する。
Phase1は当初計画の生SDK直呼び`probe_raw()`層を見送り、既存`adapter.complete()`を再利用する
簡略実装とした（response.model照合による`id_verdict`分類は未実装、今後の課題）。
