# 2026-08-18 12:48 JST | サイクルF: Phase 3 BudgetedAdapterコスト計算修正（usageベース _usage_cost() へ統一）

## 要求
Phase 1（06:43）→ Phase 2（11:04）と順次修正してきた実測usageベースのコスト計算 `_usage_cost()`
統一の最後の1箇所として、`scripts/model_matrix.py` の Phase 3 `BudgetedAdapter.complete()` に残る
旧式 `estimate_cost(info, input, output)` 直呼び出しを修正する。Phase 3 は1モデルあたり3〜7回API
呼び出しがあり、`max_cost_per_model` の予算ガードが `spent_usd` の累積値に依存するため、コスト計算の
誤差がそのまま「止まるべきタイミングで止まらない」実害になる。実API呼び出しは禁止、pytestのみで検証。

## 実行
まず `BudgetedAdapter` の予算判定フロー（事前ガード＝worst-case予約方式で `spent_usd + worst >
max_cost` を呼び出し前にチェック／事後ガード＝実usageに基づく `spent_usd` 累積）を精査し、事前ガードは
既に構造的に正しいこと、バグは事後の実コスト計上（`estimate_cost()` に `cache_read_input_tokens`/
`total_tokens` を渡していない）のみであることを確定。Phase 3 の1モデルあたりコール回数
（`loan_choice`×1 + `negotiation`×1-3 + `commit`×1-3、通常3・最大7）を `build_phase3_config()` の
設定から算出し、`BudgetedAdapter` は既にコール単位で `spent_usd += cost` と正しく累積している（redesign
不要）ことを確認した上で、`:661` の1行を `_usage_cost(self._model_info, usage)` へ置換した。

`tests/test_model_matrix.py` に14テスト（parametrize4件展開込み）を追加: Gemini hidden thinking
計上・xAI cache割引・cache+reasoning併存・Anthropic正規化・通常ケース非回帰（M2/M5/M6/L2）・欠損
フィールド耐性・複数コール累積・予算到達時ブロック・API例外時非課金、および `_run_one_phase3_game`/
`run_phase3` を通したstate/JSONL/reportへの反映確認2件。

## 検証
`uv run pytest tests/ -q` で570件全PASS（既存556件+新規14件）。実API呼び出しは0回、課金$0。
`_worst_case_cost` hidden thinking未対応の残余リスク・`llm_agent.py` 自前コスト計算のAnthropic正規化
漏れは別課題として `rules/project.md` に明記した。

## 発見
Phase 3 も Phase 2 同様まだ一度も実行しておらず成果物（`phase3_calls.jsonl`・state の phase3 レコード）
が存在しなかったため、履歴の再計算・書き換えは一切不要だった。これで Phase 1/2/3 全てが `_usage_cost()`
に統一され、`scripts/model_matrix.py` から `estimate_cost()` の直接呼び出しは `_usage_cost()` 内部
（`:340`）のみとなった。
