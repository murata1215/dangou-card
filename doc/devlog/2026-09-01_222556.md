# 2026-09-01 22:25 JST ｜ サイクル9.10: 投稿セット出力とXフック文16本

X 上ではリンク付きツイートが伸びないため、日次投稿を「①フック文＋画像添付（リンクなし）→
②リプライでリンク」の2段構えに変更した。連載16本のフロントマターに、画像添付を前提とした
手動投稿用フック文 `x_hook` を指定16文言で一字一句追加（既存の `title/slug/category/tags/
lead/series/order/status/x_text` は無変更・削除0行）。`x_hook` の加重文字数（単体・URLなし）
は全16本 85〜140文字で280文字を十分下回った（`tweet_text` 側の値も9.8b時点から不変であることを
`--check-lengths` で確認）。

`scripts/publish_next.py` に `select_attachment_image()`（ファイル名の `_system`/`_summary`/
`_memoir` 判定から `{order}_*roster/results/moment.png` を選び、無ければ本文最初の画像参照へ
フォールバック。返り値はローカル絶対パス）と `build_post_set()` を新設し、公開後（および
`--dry-run` 時は仮URLで）の末尾出力を指示どおりの「投稿セット」形式に差し替えた。同じ内容を
`doc/blog/today_post.md` に上書き保存する。`x_hook` が無い記事は従来の `tweet_text` ベースの
下書き出力にフォールバックする。`--replay NN` を新設し、plan.md 上で公開済みの order のみ
投稿セットを再表示する（公開処理・plan更新・today_post.md書き込み・API呼び出しは一切なし）。
`--check-lengths` にも `x_hook` 列（加重文字数・280判定）を追加した。公開ペイロード
（`build_publish_payload`／`tweet_text`・`excerpt`）自体は無変更で、`x_hook` は API には送らない。

`doc/blog/pipeline.md`「10. X 連携」節の「運用上の代替」を「毎朝の運用手順（2段投稿）」に
書き換え、`tweet_text`（PixBlog の X ボタン用・URL自動付加）と `x_hook`（手動投稿用・画像添付
前提・リンクなし）の役割差を明記した。

検証: `--check-lengths` で16本全て280文字以内（超過0件）を確認。`--dry-run --order 01`
（system→`01_roster.png`）・`--order 02`（memoir→`02_P06_moment.png`）で画像選定と投稿セット
の体裁・添付画像の実在（絶対パス）を確認。`--replay 01` は plan.md が全記事未公開のため
「公開済みなしです」と報告して中断（exit 1）。`grep -c '^x_hook:'`＝16・`grep -Ec '^(lead|
x_text):'`＝32 で16本の差分が追加行のみであることを確認。実公開・画像アップロード・PATCH は
一切行っていない（`--dry-run`のみ）。`engine/`・`llm/`・`blog/`・`tests/`・`logs/`・
`doc/blog/*/plan.md` は無変更（`git diff --stat` 空）。LLM API 呼び出し0件、commit/push 未実施。
