# 2026-09-19 17:04 JST ｜ サイクル10.4: 6体本戦の事前確認

## 要求

6体・12ラウンド・S2 v0.10（型C＋首位公示＋順位通知）の本戦をtmuxから起動できる状態にする。
実装は禁止、賞金プリセット追加のみ承認（`--prize-preset l6`選択口を含む）。本戦は実行しない。

## 実行

- タスク前提の数値誤り（12体「1市場48万」）を8/30実測manifestで訂正、実際は64万・総賞金2,304万と確認。
- Bot 6種（Random/Conservative/StrongCardSave/HighPrizeHunter/Collusion/Betrayal）×1000試合（seed=42固定）で
  `--prize-scale` 0.625/0.75/0.875/1.0（1市場20/24/28/32万）の4アームを掃引。
- `engine/config.py`に`GameConfig.baseline_v1_s2_l6()`を新規追加（1市場24万、既存`baseline_v1_s2()`は不変）。
- `scripts/llm_trial.py`に`--prize-preset l6`選択口を追加（6人＋S2専用のバリデーション付き）。
- `tests/test_l6_r12_trial.py`にプリセット固定テスト3件を追加（既存9件は無変更）。
- 5試合の小規模ログ実行で`LEADER_ANNOUNCED`/`RANK_NOTIFIED`の発火を確認。
- 6社（L1〜L6）へping1回ずつ、計6回のAPI呼び出しで疎通確認。

## 検証

- `uv run pytest tests/ -q` → 1784件全PASS（新規3件含む、既存回帰なし）。
- `GameConfig.baseline_v1_s2_l6()`実測: `total_prize=8,640,000`・`prize_tiers[0]//3=240,000`・
  `type_c_enabled/leader_announce_enabled/rank_notice_enabled`すべてTrue。
- `--validate-only`でmanifest目視（API呼び出し0回）。
- 6社ping結果: L1(Anthropic)/L2(OpenAI)/L3(Google)/L4(xAI)/L5(Moonshot)/L6(DeepSeek) 全てOK。
  Kimi(L5)の`thinking:disabled`設定・DeepSeek(L6)の`thinking:disabled`設定は健在。
  Gemini(L3)は無効化不可のためhidden_thinking_reserve_tokens=512の安全予約方式のまま。

## 発見

- **§1.5「全員生還は算術的に不可能」の証明はS2の`surge_enabled=True`下では成立しない**。
  `engine/market.py`の高騰時`total_pool *= 2`が外部注入額を増やすため、素朴な
  「総賞金 vs 生存線合計」の比較では不十分（28万・32万アームで全員生還が実際に発生）。
  今回の賞金判断はBot 1000試合実測のみを根拠とし、この限界を`baseline_v1_s2_l6()`の
  docstringに明記した。
- 掃引結果（平均生還者数/6・全員生還/1000・生存者0/1000）:
  20万→2.35・0件・3件／**24万→2.82・0件・1件（採用）**／28万→3.23・3件・0件／32万(現行)→3.60・9件・0件。
  24万を採用: 目標帯(1〜3/6)に収まり全員生還0件、生存者0の試合も最少。
- 起動コマンド（実行はしない）:
  `bash scripts/run_trial.sh --phase C --ruleset S2 --roster "L1,L2,L3,L4,L5,L6" --games 1 --seed 1001 --max-output-tokens 2000 --abort-on-budget-block --per-player-cost-cap 3.0 --game-cost-cap 12.0 --prize-preset l6 --run-id l6_r12_v010_20260919`

`git push`は未実施。エンジン・プロンプト・ビューワーは無変更。LLM API呼び出しは疎通確認6回のみ。
