### 2026-08-18 04:18 JST | API返却モデル名の取得・記録対応（最小改修）+ Phase 1 弱6モデル再実測

**背景**: 前サイクル（2026-08-18_001755）の Phase 1 弱6モデル疎通は6/6 PASS したが、成果物レポートで
「API返却モデル名: 全モデル取得不可」と書くしかなかった。原因は `llm/adapters.py` の
`AnthropicAdapter.complete()` / `OpenAICompatAdapter.complete()` が SDK 応答の `response.model` を
一度も参照せず捨てていたこと。MODEL_REGISTRY に書いた requested model_id と、プロバイダが実際に
処理した returned モデルの一致を検証できない状態だった。

**実装（最小改修・DevRelayプランモード→承認→実行の1サイクル）**:

- `llm/adapters.py`: 両アダプタの `complete()` の戻り値シグネチャ `(text, usage)` は維持したまま、
  usage 辞書に `requested_model`（MODEL_REGISTRYのmodel_id）と `response_model`
  （`response.model` を正規化ヘルパ `_norm_response_model()` 経由で格納。取得不能・非文字列・
  空白のみは `None` とし、requested で埋めて「返却された」と偽装しない）を追加。
- `llm/llm_logger.py`: `log_call()` のエントリに `response_model` を1フィールド追加
  （`usage.get()` 経由なので既存呼び出し元への影響なし）。
- `scripts/model_matrix.py`: 新設 `_model_match(requested, returned)` が
  `match`(✓正規化後完全一致) / `alias`(≈一方が他方のprefix。OpenAI系のエイリアス→日付
  スナップショット解決を想定) / `mismatch`(⚠それ以外、リダイレクト等の要注意) / `unknown`(?取得不能)
  の4値を返す。Phase 1 の JSONL・state.json・`phase1_report.md`・最終 `matrix_report.md` すべてに
  requested/returned/一致列を追加。Phase 2/3 の関数本体・`llm/models.py`・`engine/` は無改変
  （Phase 2 は `usage` をそのまま埋め込む設計のため、コード変更なしで `response_model` が
  副次的に自動記録されることを確認）。
- テスト: `tests/test_llm.py` に6件（Anthropic/OpenAI互換それぞれの伝播・欠落時None化・
  Logger転記2件）、`tests/test_model_matrix.py` に `_model_match` の4分類＋正規化1件、
  Phase1記録・レポート反映6件の計12件、あわせて新規17件を追加。
  `uv run pytest tests/ -q` → **514 passed**（既存497 + 新規17、リグレッションなし）。

**実測検証（実API・新run_id・Phase 1のみ・1コール/モデル）**:

```
uv run python scripts/model_matrix.py --phase 1 --tier weak \
  --max-tokens 256 --retries 0 --max-calls 6 \
  --max-cost 0.02 --max-cost-per-model 0.01 --max-cost-total 0.05
```

run_id=`run_20260818_041810`。弱6モデル 6/6 成功、合計コスト $0.000532（上限$0.05に対し余裕）。
Phase 2/3 は未実行（state.json で `pending` のまま）。

結果: DeepSeek/Google/xAI/Moonshot/Anthropic の5モデルは requested と returned が完全一致（✓）。
OpenAI (`gpt-4.1-mini`) のみ `gpt-4.1-mini-2025-04-14`（日付スナップショット）を返却し `alias`(≈)
判定となった——これは異常ではなくOpenAIの正常なエイリアス解決仕様であり、`_model_match()` の
alias/mismatch 区別が意図どおり機能した最初の実例となった。

**未修正（今回のスコープ外・報告のみ）**: 前サイクル発見の xAI reasoning_tokens コスト計上漏れ
（`run_phase1` の `estimate_cost()` が `cache_read_input_tokens`/`total_tokens` を渡していない）は
今回も未修正。今回の実測でも L4 (grok-4.3) の記録コスト $0.000294 は reasoning_tokens=343・
cache_read=192 分を含まない過小値であることを再確認した。

**次段**: Phase 2（談合カードJSON互換）実行時、中/強ティアでの requested/returned 一致確認は
別サイクルで実施予定。
