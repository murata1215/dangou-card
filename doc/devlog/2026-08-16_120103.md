# 2026-08-16 12:01 JST | コストログ化 Stage 3: 試合サマリー＆ビューワーにコスト内訳パネル

Stage 1/2 で正確化したコストデータ（キャッシュ割引・thinking内訳・確定単価）をビューワーと試合サマリーで可視化した。

**実行内容:**
- `viewer/log_parser.py`: `get_game_state()` の stats にトークン内訳（input/output/cache/thinking）追加。`get_cost_breakdown()` 新関数でモデル別・プレイヤー別のコスト内訳集計（estimate_cost再利用、reasoning テキスト非混入で秘匿維持）。
- `viewer/server.py`: `/api/games/{trial}/{game}/cost` エンドポイント追加。
- `viewer/static/index.html`: ヘッダの `$X.XXXX` クリックでコスト内訳モーダル（Model Breakdown テーブル + Player Breakdown テーブル）。プレイヤーパネルに thinking バッジ（`🧠Xk`）。
- `viewer/static/style.css`: コスト内訳テーブル・thinking列のオレンジ強調スタイル追加。
- `scripts/llm_trial.py`: Phase C レポートのモデル別テーブルに Input/Output/Cache/Think 列追加。全体サマリに ¥換算とトークン合計。

**検証:** 新規テスト3件（集計正確性・秘匿維持・stats内訳）追加。全344テスト PASSED。
