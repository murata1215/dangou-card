# Gemini 3.7 Flash Phase 3 comparison — 2026-08-22 06:41 JST

## Scope

- Compared formal `M3=gemini-3.5-flash` with evaluation-only `M3_G37_EVAL=gemini-3.7-flash`.
- Kept formal M3 model ID, prices, and runtime configuration unchanged.
- Did not run Phase 4 or any game integration trial.

## Implementation

- Hardened `scripts/gemini_37_eval.py compare` with Standard-price/thinking preflight, interleaved fixed-input scheduling, per-model hard stops, shared cap accounting, case-level statistics, and interrupted-run resume.
- Added API-free tests for same-low-thinking preflight, interleaving, statistics, and one-model stop behavior.
- Added a 3.5/3.7 Google Standard-price comparison snapshot.

## Result

- Both models: 12/12 first-pass valid JSON, correction 0, transport retry 0, no length/usage/model errors.
- M3: $0.128597 total, $0.010716 per successful JSON, 3,715ms average latency.
- 3.7: $0.046244 total, $0.003854 per successful JSON, 3,073ms average latency.
- 3.7 reduced measured cost per logical call by 64.04%; Phase 3 PASS.
- Full regression: `uv run pytest` → 764 passed.

## Next boundary

- Phase 4 S2/R1–R3 integration requires separate human approval. Formal M3 promotion remains out of scope.
