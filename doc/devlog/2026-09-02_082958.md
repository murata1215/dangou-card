# 2026-09-02 08:29 JST ｜ サイクル9.11: 常設ガイドの公開と旧図解への誘導

連載16本・X・動画すべての着地点となる常設ルール記事が無かったため、
『嘘八百万 ―談合カード―』とは——AIたちが借金と嘘で殺し合う市場の、現行ルール完全ガイド
を新規作成・公開した（`doc/blog/guide/guide.md`、原稿は指示の文面を一字一句そのまま使用）。
図解3枚（`guide_flow.png` 1ラウンドの流れ・`guide_money.png` お金の循環・`guide_mechanics.png`
4つの仕掛け、いずれも `scripts/blog_images.py` に新設した生成関数で `doc/blog/guide/images/` へ
出力、50,078 / 28,789 / 57,461 バイト）を、指示どおりの本文位置（「1ラウンドの流れ」節直後・
「お金のルール」節直後・「4つの仕掛け」見出し直後）に埋め込んだ。`scripts/blog_images.py::main()`
は `--guide` フラグを追加し `--date` を任意化しただけで、既存の連載16本向け経路（`--date`）は
挙動不変。

公開はリポジトリ外の一時スクリプト（`/tmp/cycle911_publish_guide.py`）から
`client._request("POST", "/posts", payload)` を直接叩く方式で実施（`create_post()` に
`slug`/`tweet_text`/`excerpt` の引数が無いため）。画像3枚をアップロードし本文中の参照を
公開URLへ置換した上で、`tweet_text` = `x_text ＋ 半角スペース＋#嘘八百万`、`excerpt` = `lead`
をペイロードに設定。`x_hook` はAPIに送らない（手動投稿①用）。公開URL:
https://pixblog.net/u/uso8m/dangou-card-guide （post_id=241）。

あわせて Season 1 時点の旧図解2本（ID 213「図解：はじめての『嘘八百万 ―談合カード―』」・
ID 214「図解②：カードの見方」）に誘導注記1行を追加した。`{"content": ...}` 形式ではなく、
まず `GET /posts/{id}` で現本文（`content`）を取得して `doc/blog/guide/backup_213.md` /
`backup_214.md` に丸ごと保存し、冒頭に「※ 本記事は Season 1 時点の内容です。現行ルールは
[『談合カードとは』完全ガイド](公開URL) をご覧ください。」＋空行を足した全文で
`update_post(id, body=新全文)`（書き込みキー `body`）により更新した。PATCH後の再GETで、
本文の残り部分（tail）が backup と完全一致することを確認済み。

検証: `--guide` 実行で図解3枚が非ゼロサイズで生成されバイト数を報告・既存
`doc/blog/{20260824,20260830}/images/` は無変化。`--help` で `--date`/`--guide` 両方の
存在を確認。ガイドの公開URLを再GETし `tweet_text`/`excerpt` が設定どおり・本文に画像3枚の
公開URLが含まれることを確認。`doc/blog/guide/` に `guide.md`・`images/`（3枚）・
`backup_213.md`・`backup_214.md` の存在を確認。`git status --short` は
`scripts/blog_images.py`・`doc/blog/guide/**`・`doc/blog/pipeline.md`・`doc/devlog/**` のみで、
`engine/`・`llm/`・`tests/`・`blog/`・連載16本の `posts/`・両 `plan.md` は無変更（`git diff --stat`
空）。LLM API 呼び出し0件、commit/push未実施。
