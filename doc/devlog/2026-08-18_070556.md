### 2026-08-18 07:05 JST | 18モデル疎通テスト基盤 Phase 1 実行: 強6モデル

## 背景

18モデル疎通テストの最終ティア。強（H tier）6モデル H1〜H6 は models-list による$0発見のみで、実APIコールは一度もなかった。Phase 1コスト計算バグ（前サイクルで修正済みの `_usage_cost()`）を前提に、L6+M6+H6=18モデルの累積表を完成させる。

## 実行内容

- run_id `run_20260818_041810` に `--resume` で継続。Phase 1のみ、Phase 2/3は未実行のまま。
- `ordered_keys` の安価順: H6 deepseek-v4-pro → H3 gemini-3.1-pro-preview → H5 kimi-k3 → H4 grok-4.6 → H1 claude-opus-5 → H2 gpt-5.6-sol。
- thinking上振れリスクの大小で3 Runに分割し、コード変更なしで対応:
  - Run A: H6,H5,H4,H1 @max_tokens=1024（thinking無効化済み/既定OFF/max_tokens非消費のモデル群）
  - Run B: H3 @3072（Geminiのthinkingは制御不能、maxOutputTokensを消費するため隔離）
  - Run C: H2 @3072（最高単価かつreasoning effort制御不能のため最後に隔離）
- 各Runとも `--dry-run` 先行実行で対象モデル数・worst-case・累計コストの想定一致を確認してから実APIに切り替え。

## 結果

- 強6: **5/6 pass、1/6 fail**。
- H2 (gpt-5.6-sol) が400 BadRequestで失敗: `llm/adapters.py` のOpenAI-compatアダプタが `max_tokens` を送るが、gpt-5.6-solは `max_completion_tokens` を要求する新reasoning系モデルだったため。課金$0・コール1回のみ（400は`_should_retry()`の対象外で内部リトライも発生せず）。プラン方針どおり**修正はせず報告のみ**。
- H4 (grok-4.6) は reasoning_tokens=124 かつ cache_read=128 を同時に観測（キャッシュ割引とreasoning課金が両方効くケース）。
- H1 (claude-opus-5) は `thinking_tokens: 0` を実測し、既定OFFを確認。
- H3 (gemini-3.1-pro-preview) は hidden thinking 185トークンを実測（`total - input - output` の差分）。3072キャップに対し余裕があり `finish_reason=stop`（打ち切りなし）。
- 強6実測コスト合計: **$0.006432**（worst-case合計 $0.188238 の約3.4%）。
- 18モデル累積: コール数18、累計コスト **$0.008537**、Phase 1 pass 17/18・fail 1/18（H2のみ）。Phase 2/3は3ティアとも未実行のまま。

## 未確定事項の申し送り

- H4/H5の単価は `llm/models.py` 内で推定値（TODOコメント残存）。実測コストの絶対値はこの単価精度に依存する。
- H2はPhase 1未疎通のまま。`llm/adapters.py` の `max_completion_tokens` 対応が今後必要（本サイクルではソース変更なし）。

## 成果物

- `.devrelay-output/phase1_strong6_report.md`
- `logs/model_matrix/run_20260818_041810/`（`phase1_calls.jsonl` +6行、`matrix_report.md` を18モデル分に再生成、`state.json` 累計コール18・$0.008537）

## 検証

- `git diff --stat -- scripts/model_matrix.py llm/ engine/` で本サイクル起因の追加差分がゼロであることを確認（既存の無関係な未コミット差分のみ残存）。
- Phase 2/3は起動していない（`state.json` の該当フェーズは `pending` のまま）。
