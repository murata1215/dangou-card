# 2026-08-29 10:43 JST ｜ サイクル8.0: プロンプト修正前のエンジン実挙動監査

要求: `doc/prompt_dump/actual_prompts_20260829.md`（現行プロンプトの実文面）と仕様書v0.5/v0.7を突き合わせて残った、プロンプト文面だけでは真偽を確定できないエンジン実挙動10項目（契約発行料の課金タイミング、署名待ち提案の寿命、contract_proposeのround_num検証、市場高騰の少人数判定、contract_cancelの可否条件、公開情報の描画マトリクス、terms/detailsのバリデーション、card_trade_proposeの実挙動、強制返済不能時の処理、Settlementの実処理順）を、コード変更・テスト実行・LLM API呼び出しを一切行わない読み取り専用調査で確定する。

実行: `engine/contracts.py`・`engine/actions.py`・`engine/game.py`（`_build_visible_state()`含む）・`engine/settlement.py`・`engine/finance.py`・`engine/player.py`・`engine/config.py`・`llm/prompt_builder.py`をfile:line単位で直接読み込み、`GameConfig.baseline_v1_s2(12)`のS2実測定数（contract_fee=100000, surge_full_participation_max_alive=3, mandatory_repay_k=0, survival_cash=2000000, card_trade_broadcast_max=5等）を`uv run python`のワンライナー実行で確認。並行して汎用探索エージェント3体（契約ライフサイクル担当/高騰・財務・決済順序担当/公開情報描画・カードトレード担当）に読み取り専用の下調べを行わせ、その報告内容は全件、file:line引用を1次ソースへ照合し直して確度を検証（探索エージェントの誤り2件——「card_trade_broadcast_maxが未強制」「トレード提案がラウンドを跨いで残る」——をコード直読で否定して結論から除外）。10項目それぞれについて結論・根拠・現行プロンプトとの一致判定(○/△/×)・関連テストを整理し、`doc/analysis/prompt_engine_audit_20260829.md`に記載。

検証: 全文書は既存コードの引用のみで構成し、`*.py`・テスト・設定ファイルは一切変更していない。作成したのは`doc/analysis/`配下1ファイルと本devlogのみ。`logs/llm/`の最新ディレクトリが8/28付のまま増えていないことを確認しAPI呼び出しが発生していないことを担保。`git status --short`は本devlog群と分析ファイルの新規追加のみで既存ファイルへの差分ゼロ。

発見: 10項目中4項目が×（署名待ち契約は失効せず全12R残る／過去roundを指定した義務は監査されないまま`can_cancel_contract`の`round_num < 現在R`条件に恒久的に抵触し解除不能な死に契約になる／仕様§6.4「倍掛けの選択事実と預託額は全プレイヤーへ公開」がnegotiation・commit・reflectionでは未実装で他者の倍掛けを読むのは`build_double_up_prompt`の1箇所のみ／R12でも強制最低返済(Finance Step2)が生還判定より先に走るため残債完済不能は「条件未達」ではなく「破産」で脱落する）。さらに前タスクの成果物`doc/prompt_dump/actual_prompts_20260829.md`のメタ情報に2箇所の誤りを発見（「倍掛け預託ブロック発火」は誤りで実際は非表示が正しい実装／カードトレードの支払方向の説明が実装の符号規約と逆）——次回のプロンプト修正着手前に訂正が必要。
