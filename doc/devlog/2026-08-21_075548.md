# 2026-08-21 07:55 JST — Viewer round-detail participants型互換

## 変更

- `MARKET_RESULT.participants` が現行実ログで参加人数の整数である場合に、round-detail再構成が配列として反復していた不具合を修正した。
- 配列形式の旧ログは引き続き所属判定に利用し、整数形式では既存のcommit市場IDからプレイヤーの市場結果を関連付ける。
- round-detail返却値の`participants`は、`get_round_states()`と同じ元の型のまま保持する。

## 検証

- 整数participants fixtureを追加し、市場結果・Memory・reasoning・Native Thinking・安全なaction変換を回帰確認した。
- `trial_C_20260821_072321`のP01〜P06、R1〜R3を直接再構成する回帰テストを追加した（ログがない環境ではskip）。
- `uv run pytest -q`: 745 passed, 4 warnings。
- HTML内JavaScript構文検査、`git diff --check`成功。

## 範囲外

- Viewer再起動・デプロイ、ゲームengine、LLM prompt/memory生成、trialログの変更は行っていない。
