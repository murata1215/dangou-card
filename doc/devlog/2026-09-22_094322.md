# 2026-09-22 09:43 JST ｜ サイクル10.12: 504 タイムアウト原因切り分け

## 要求
l12r12_dr_1201本戦でP11（DR_OPUS48）に504タイムアウトが57件、P12（DR_SONNET5）に15件発生した原因を、`llm_calls`ログのみから「モデルが遅い」「順番待ち」「設定不一致」の3仮説に切り分ける依頼。コード・設定・テストの変更は行わず、分析結果をレポートとdevlogにまとめる。

## 実行
`/tmp/c1012/timeout.py`（repo外）で全12席1,553コールを再集計し、席別エラー表、504のelapsed_ms分布、成功コールの所要時間とトークンの相関、コール実行区間の重なり判定、リクエスト内容の一致確認、`engine/game.py`のAUTO_COMMIT経路とllm_calls行番号の突合を行った。

## 検証
504は全件`error_type=timeout`でelapsed_msが108.0〜108.5秒に張り付き、打ち切り秒数を約108秒と推定した。成功コールの所要時間と出力トークン数の相関は全席0.98前後で、生成速度（tok/s中央値）はP02 55.8・P06 54.3・P11 56.4とほぼ同一なのに、出力トークン中央値はP11 3,175・P12 2,936がP02 1,422・P06 1,286の2倍超だった。全1,553コールの実行区間重なりは0件、504時の直前60秒同時実行数（平均1.19）は成功時（1.53）より少なく、model_id・response_model・aiフィールドも504と成功で完全一致していた。

## 発見
最有力は仮説A（Opus 4.8/Sonnet 5がhidden thinkingを多く使い可視出力に対する出力トークン比率がOpus 5/Fableの約2倍で、打ち切り時間内に収まらない）で、仮説B（順番待ち）とC（設定不一致）は棄却した。commitのAPIエラー時再試行は`devrelay_http.py`が429/ConnectErrorのみリトライ対象とし504は即例外のため0回で、P11のR9/R10 commit（llm_calls 106行目・118行目）はそれぞれ1回の試行のみでAUTO_COMMIT（events 1597行目・1755行目）に直結していた。504による損失時間は合計7,890.3秒で試合全体38,133.5秒の20.7%（P11 16.2%・P12 4.3%）。成果物は`doc/analysis/l12r12_dr_1201_timeout.md`のみで、LLM API・DevRelay呼び出しは0件、`git push`未実施。
