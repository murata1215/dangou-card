# 2026-08-17 22:49 JST ｜ サイクルX.X: DM本文の秘匿化（§8.2是正）

**要求**: 前サイクル（引き継ぎメモリ導入）の調査で見つけ、スコープ外として見送っていた
「DM本文が全プレイヤーに見えている」バグ（game.py:949）を対応してほしいという依頼。
`doc/turn_order_investigation.md`に2026-08-14から既知の差異として記録されていたが放置されていた。

**実行**: `_build_visible_state()`の`messages`フィールドが`for_player_id`を無視していたのが原因。
`_visible_messages()`を新設し、DMは送信者・宛先以外に対し`message`キーごと削除して返す
（reasoning/memoryと同じ「構造的秘匿」）。メタデータ（sender/to/turn）は残し、密談の存在は
非当事者にも見えるままにした（プロンプト構造・LLMコール数を不変に保つため）。
調査の副産物として、匿名通信が誰にも届いていない（本文を`_round_messages`に積んでいなかった）、
commit/double_upフェイズで`for_player_id`未指定のため契約義務ブロックが常に空になっていた、
という2つの実装漏れも同時に見つけて是正した。

**検証**: 新規`tests/test_dm_secrecy.py`21件全PASS。`tests/test_memory.py`に非当事者視点の
reflectionでもDM本文が見えないことを検証する1件を追加、全445件PASS（回帰ゼロ）。
`dry_run.py`完走。`simulate.py --games 1000`（S1既定）は修正前後で平均生還者数0.07/8が
完全一致——Bot 8種は全種DM未使用のため統計への影響が構造的にゼロであることを実測で確認。
`--ruleset S2 --games 100`もreflectionフェイズ込みで例外なく完走。実プロンプトを目視し、
非当事者には`[P01→P02] （非公開のDM）`、当事者には本文がそのまま表示されることを確認した。

**発見**: 修正対象が1バグだと思っていたら、同じ関数（`_build_visible_state`まわり）に
連鎖する実装漏れが2つ埋まっていた。匿名通信は手数料だけ徴収して本文を捨てており、
§7.1の機能が試合開始以来一度も動いていなかった可能性がある。Bot戦のシミュレーション統計が
一切変わらないという確認は、「ルールベースBotはDMを送らない」という設計上の性質のおかげで
今回はほぼノーリスクで検証できたが、逆に言えばこのバグはLLM戦でしか顕在化せず、
Bot中心の1000試合シミュレーションを何度回しても絶対に見つからない種類のものだった。
