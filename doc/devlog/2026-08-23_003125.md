# 2026-08-23 00:31 JST ｜ サイクル: 脱落済みplayer宛DM問題の根本修正（Engine D1-D4 + Viewer D5）

**要求**: `trial_C_l12_r12_20260822`のViewer God ViewでP06@R9に`P06→P09`の秘密DMが多数表示されて
いた件、既知の調査Plan（`/home/uso8m/.claude/plans/linked-stirring-catmull.md`）に従い実装を開始。
Engine側の合否判定非対称性（D1）、不成立が本人へフィードバックされない問題（D2）、脱落公示欠如
（D3）、脱落者を含む契約表示の紛らわしさ（D4）、ViewerがLLMログとEngineイベントをjoinせず本文だけ
表示する問題（D5）の5点をまとめて修正。

**実行**: Phase 1（Engine/Prompt）は`engine/actions.py`（`ContractProposeAction`/
`CardTradeProposeAction`の生存チェックを`DmAction`/`TransferAction`と対称化）、`engine/game.py`
（`_action_failures`記録・`_build_visible_state()`へ`my_failed_actions`/`my_action_budget`/
`eliminated_players`/`contracts_public.eliminated_parties`追加）、`llm/prompt_builder.py`
（脱落公示ブロック・不成立フィードバックブロックを`build_negotiation_prompt`/
`build_reflection_prompt`へ配線、RULES_SUMMARY文言4箇所改訂）、`llm/llm_agent.py`
（`AUTO_PASS_ON_NO_NEWS`が新規失敗発生時はバイパスするよう修正）。Phase 2（Viewer）は
`viewer/log_parser.py`に`_index_negotiation_outcomes()`/`_match_delivery()`（純関数、
`(player_id, round_num, turn)`でjoin）を新設し、`_collect_round_messages()`へ`events`引数を
追加してdelivery判定を各メッセージへ付与、`_project_message()`へ2箇所の重複投影ロジックを統合、
`get_player_round_detail()`の`round_totals`へ`messages_rejected`/`messages_rejected_shown`を追加。
UI側は`viewer/static/index.html`に`deliveryBadge()`/`deliveryClass()`ヘルパを追加し状況パネル・
プレイヤー詳細モーダルの両方へバッジ表示、`style.css`へ`.delivery-badge`系クラスを追加。

**検証**: 新規テスト`tests/test_dead_target_and_feedback.py`（48件）・`test_dm_secrecy.py`追記1件・
`tests/test_viewer.py::TestNegotiationDeliveryJoin`（20件）を追加し、全体回帰は1014/1014 PASS
（既存945 + 新規69）。副産物としてRULES_SUMMARY文言追加でシステムプロンプトのトークン数が変動し、
`test_model_matrix.py`/`test_phase2_schema.py`のハードコード済みコストスナップショット2件が
崩れたため実測値へ更新（意図的な一度きりの変更）。実データ照合で`trial_C_l12_r12_20260822`の
delivery分布が想定通りdelivered:653/rejected:25/unverified:4となり、P06→P09のR7-R9全25件が
`rejected`かつ理由`"Target P09 is not alive"`で一致することを確認。Viewerを再起動しcurlで
god/public両viewを実地確認、public viewでは`delivery`/`reject_reason`のいずれも一切出現しないこと
（"P09"文字列も0件）を機械確認した。

**発見**: 元バグの実害は表示問題だけでなく、P06が本来25枠を無駄打ちしたことでR7-R9の実質的な
交渉が枯渇し、R9の破産に直結していたゲームプレイ上の実害だった。D2のフィードバック配線により
今後は同種の「無限に不成立DMを送り続ける」自滅パターンをLLM自身が回避できる可能性が高い。
D5のjoin実装では"unverified"（未検証）を"rejected"（不成立）と混同しないことを最重要の不変条件
とし、turn無しの旧ログでは`delivery`フィールド自体を一切付与しない後方互換モードとした
（機械的に埋めることが新たな誤情報源になるため）。
