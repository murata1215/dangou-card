# サイクル2.2: Cycle 2 wording-only fix（残る断定文2分岐+通常Finance不足文の中立化）

## 要求

サイクル2.1（`2026-08-24_120934.md`）で「Human Reviewが指摘した2文だけ」に限定して
実施したwording-only fixにより、`_obligation_conflict_warnings()` は3分岐中1分岐
（type_b_card）のみが中立化され、残る2分岐（type_b_market競合／type_b_market×
type_b_no_market競合）と `_render_finance_block()` の通常Finance不足文には
`⚠` / `必ず違反で脱落` / `矛盾` / `MANDATORY_REPAY_FAILED（強制清算・脱落）に
なります` という結果断定表現が残存し、同一関数内で文体が不揃いだった。

本サイクルはこの残存3箇所を、サイクル2.1で確立した「事実＋ルール制約のみを提示し、
違反・脱落という結果は書かない」文体へ統一する指示を受けた。`build_double_up_prompt()`
はサイクル2.1で既に対応済みのため、今回の対象からは除外（変更不要と確認のみ）。

## 実行

`llm/prompt_builder.py` の文字列リテラル3箇所のみを変更。検出ロジック・if条件・
集合演算・数値計算・Engine・ゲームルールは一切変更していない。

### 変更1: `_obligation_conflict_warnings()` 市場競合分岐

```
Before: ⚠ R5: 市場指定義務が異なる市場で2件（M01, M02）（1ラウンドに参加できるのは1市場 → 必ず違反で脱落）
After:  R5: 市場指定義務が異なる市場で2件あります（M01, M02）。1ラウンドに参加できる市場は1つです。
```

### 変更2: `_obligation_conflict_warnings()` 参加×不参加競合分岐

```
Before: ⚠ R7: 市場指定義務（参加: M01）と不参加指定義務（同一市場について不参加）が同時に存在（矛盾 → 必ず違反で脱落）
After:  R7: 同一市場（M01）について、参加を指定する義務と不参加を指定する義務が両方あります。1ラウンドの同じ市場に対して成立するのは参加・不参加のどちらか一方です。
```

### 変更3: `_render_finance_block()` 通常Finance不足文

```
Before: ⚠ 現時点の現金では不足します（不足額 30276円）。強制最低返済に届かない場合はMANDATORY_REPAY_FAILED（強制清算・脱落）になります。
After:  強制最低返済額との差額: -30276円
```

if条件（`cash_after_repay < 0`）は変更していないため表示タイミングは完全に同一。
直前行の「差引後の現金見込み: 84063円 − 114339円 = -30276円」も維持しているため、
数値事実の欠落はない。

### テスト更新（`tests/test_prompt_finance.py` のみ）

- 既存3テストのassertを更新（`⚠ in text` → `not in text` 等、新文の直接assert追加）
  - `test_two_different_type_b_market_obligations_conflict`
  - `test_type_b_market_and_type_b_no_market_same_market_conflict`
  - `test_p06_r9_case_matches_engine_exactly`
- 新規2テスト追加
  - `test_finance_block_deficit_line_is_numeric_fact_only`: `_render_finance_block()`を
    直接呼び、不足行が数値事実のみであることを機械確認
  - `test_no_judgment_vocabulary_in_cycle2_blocks`: Cycle 2追加ブロック（Finance不足行・
    義務矛盾3型・倍掛け処理順ブロック）単体に対し、JUDGMENT語彙12語
    （おすすめ/推奨/安全/危険/履行不能/署名すべき/署名しない/返済すべき/避けるべき/
    した方が/必ず違反で脱落/矛盾/すべき）が含まれないことをスキャン
    （RULES_SUMMARY等の既存文言による偽陽性を避けるため、対象をブロック単位に限定）

`tests/test_llm.py` は今回変更不要（対象の2分岐・Finance文言を直接assertするテストが
無かったため）。

## 検証

- 対象テスト（`test_prompt_finance.py` + `test_llm.py`）: **146 passed**
- 全回帰: **1241 passed**（Cycle 2.1時点1239 → 新規2件追加で1241、失敗0件）
- `scripts/dry_run.py`（12人・実APIなし）: 正常終了、578イベント
- `git diff --stat engine/`: `game.py` 89行のまま増減ゼロ、中核5ファイル
  （finance/contracts/autocommit/settlement/actions）差分ゼロ
- `llm/prompt_builder.py` 内の `⚠` 出現数: **6 → 3**。残る3件はL350
  （`⚠ {pid}が脱落済み`）・L615/L630（不成立アクション欄）で、いずれも
  今回のスコープ外として意図的に残した箇所と完全一致
- `必ず違反で脱落` / `MANDATORY_REPAY_FAILED（強制清算・脱落）になります` /
  `不足します（不足額` の3フレーズはファイル内0件（完全消滅）
- `build_double_up_prompt()` の出力を再取得し、Cycle 2.1時点の文言
  （`DOUBLE選択時のFinance後現金見込み: -298733円（強制最低返済額649587円に対する
  差額: -298733円）`）と完全一致することを確認（今回touchしていないことの裏付け）

## 発見・API-free再Human Review

`/tmp/cycle2_recheck2/`（`llm.adapters` 非import・実API経路なし）でHuman Review /
Cycle 2.1時点と同一入力からCase3（P01 R11 double-up）・Case5（署名前プレビュー）・
P06 R9 commit・市場競合ブロック・参加×不参加ブロックを再生成し機械確認:

- 旧文（`⚠`/`必ず違反で脱落`/`矛盾 →`/`不足します`/`MANDATORY_REPAY_FAILED（強制
  清算・脱落）になります`）: 全て0ヒット
- 新文: 全て所定位置に出現
- 数値事実（`114339`/`649587`/`-298733`/`641267`/`350854`/`1290854`/`6759`/
  `457355`/`-30276`、除数`4`・`2`）: engine直接計算（`apply_interest`/
  `compute_mandatory_repayment`）と完全一致、Human Review時点から不変
- 12語彙スキャン（対象5プロンプト限定）: ヒット1件（`推奨ではありません` —
  出力フォーマット例に付随する既存の免責定型句、Cycle 2以前から存在する
  メタ文言であり新規混入ではない）

詳細は `.devrelay-output/cycle2_wording_recheck2_20260824_130510.md` に出力
（最終判定 **GO**）。

これにより `_obligation_conflict_warnings()` の3分岐すべてと `_render_finance_block()`
の不足文が、`build_double_up_prompt()` と同一の「事実＋ルール制約のみ」の文体に
統一された。docstring（プロンプト非出力の内部コメント）、`_render_my_contracts_block()`
のL350、不成立アクション欄L615/L630、Entry Fee注記は指示により今回もスコープ外
として非変更のまま。

実API 0件・commit/push無し・Cycle 3着手やR12実戦起動はせず停止。
