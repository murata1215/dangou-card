# 2026-09-01 06:33 JST ｜ サイクル9.9: 前シリーズ12本への tweet_text 後付け

Season 1（第一作『談合カード』・公開済み12記事）に `posts.tweet_text` が未設定だったため、
`GET /posts`・`GET /posts/{id}`・`PATCH /posts/{id}`（ペイロードは `tweet_text` の1キーのみ）を
用いて後付けした。作業はリポジトリ外の一時スクリプト（`/tmp/cycle99_tweet_text.py`）で実施し、
`scripts/`・`doc/blog/**/posts/`・`blog/pixblog_client.py` は無変更。

`GET /posts` で取得した12記事のスラッグは指示表の参考IDと全件一致（相違0件）。加重文字数
（`#嘘八百万` 込み＋URL23）は全12本 99〜187文字で280文字以内のため PATCH 対象を絞る必要はなく、
全12件を PATCH した。既存の `tweet_text` は全記事 null（未設定）だったため上書き扱いのスキップは
発生していない。PATCH後の再GETで12件全ての `tweet_text` が送信値どおり反映されていることを確認
（先頭40字を確認表に記録）。post 202 の全フィールドをスポット確認し `status/title/excerpt/tags`
が無変更であることも確認した。

成功12件・失敗0件・スキップ0件・参考IDとの相違0件。`doc/blog/pipeline.md`「10. X 連携」節末尾に
実施記録を追記。POST/DELETE/画像アップロードは呼んでいない。LLM API呼び出し0件、commit/push未実施。
