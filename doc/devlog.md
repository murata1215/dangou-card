# 嘘八百万 —談合カード— 開発記録(devlog)

> 開発ブログの素材となるナラティブ記録。「要求 → プラン → 実行 → 検証 → 修正」のサイクルで記述する。
> 技術的な変更明細は doc/changelog.md、課題は doc/issues.md を参照。ここには物語と発見を書く。
> 体制: けいすけ(企画・意思決定・別AIレビューの持ち込み) × Claude(設計・発注・検証) × DevRelay(実装)

## 2026-08-09: 開発初日 — 着想からLLM初対戦まで

### サイクル0: 着想 — 「AIにカイジをやらせたい」
ライアーゲーム/カイジ的世界観で複数LLMを対戦させ観戦する企画として始動。限定ジャンケンを下敷きに、公正証書(拘束力ある契約・破れば即死・当事者全員生存中のみ有効=暗殺インセンティブ)、賞金首・怪文書・果たし状、カイジ方式の自己選択借入(ルールを知る前に借入額を選ばせる=開幕からベンチマーク)を発明。この時点で「合理的に嘘をつく能力を測る既存ベンチはほぼない」と気づき、エンタメ兼ベンチマークの二重性が確立。仕様書v0.1。

### サイクル1: 著作権対応の全面改設計(v0.2〜v0.3)
別AI(ChatGPT)の指示書を検証・採用し、ジャンケン・★制度を廃止して市場争奪方式(毎R3市場へ秘密コミット、ポーカー役12枚、金銭ベース生還条件)へ。Claudeレビューで「賞金総額が生還者数の算術上限を決める=全員協調でも全員生還不可能な総額が裏切りを強制する」経済原理を明文化。タイトル「談合カード」、シリーズ名「嘘八百万」(嘘八百+八百万円+やおよろず)決定。

### サイクル2〜3: 穴塞ぎと仕様凍結(v0.4〜v0.5)
往復レビューで: Claude自身の経済計算ミスの指摘と承認、Free Cash導入(借金ミュール封鎖)、パス廃止、契約失効の義務単位化(3者契約の生贄トリック封鎖)、型A自動執行/型B自動監査の区分、Atomic執行、脱落時清算(残金はシステム没収=遺産漁り略奪の排除)、Settlement 8Step厳密順序(Claude発見の順序矛盾を追加裁定)。v0.5で仕様凍結、受け入れテスト16件を定義。

### サイクル4: モデルロスターと環境構築
6社(OpenAI/Anthropic/Google/xAI/Moonshot/DeepSeek)×2階級=12体を実勢価格調査の上で選定。匿名化ルール(P01〜、モデル名は自他とも秘匿)を決定。本番サーバーに専用ユーザーuso8m作成、Python 3.12+uv+pydantic、DevRelayにプロジェクト登録。

### サイクル5: Step 1 ルールエンジン
発注→16モジュール実装(約35分)→外出先からDevRelay経由でリモート受け入れ検証(急所コード8箇所の抜粋レポート化)→7/8一致、懸念2件→Step 1.1で最小修正。テスト36件。

### サイクル6: Step 2 Botシミュレーション — 3連続の発見
①1000試合全員死亡: Botが返済せず利息(複利19.6%)が賞金注入を食い尽くす会計構造を発見→返済ポリシー+24セル掃引(2400試合7.7秒)で目標帯(平均生還2.62/8人)に着地。②StrongCardSave 98.4%支配→逓増賞金仮説をA/B対照実験で棄却(フラットでも94.1%)。③ミラーマッチ試験(レビューAIの指摘で同期現象対策の個体別tie-break seed+Random×8対照群を導入): SCS×4で95.9%→0.6%に自壊、SCS×8は上位カード衝突率98.3%で共倒れ、隣のRandomが83.9%で漁夫の利。「単独では強いが普及すると自壊する非均衡戦略」=ゲーム構造は健全と判定し、Botフェーズ終了。

### サイクル7: Step 3 LLM投入 — 初対戦と創発談合
RULESET_BASELINE_V1固定(flat/scale2.0/200万)。段階投入(3A接続→3B交渉)、初手はHaiku 4.5。
- 3A: 有効JSON率99%、12R完走、$0.35/試合。ただしrepay未発行(利息を垂れ流し脱落)
- 3B: Haiku同士がR12で「3市場分割で全員生還」の談合を自発的に組織(創発)。観戦者だけが知る皮肉: その提案は算術的に不可能(救えるのは最大3人)
- バグ発見: コミット健忘(交渉ログがコミットプロンプトに未含有→約束と逆の市場にコミット)、交渉中のmarket_commit混入
- 障害: APIハングでプロセス無限待機が2回発生(タイムアウト未実装)。killしてもエージェントが自律リトライで復活し、最終的にAPIキー無効化で遮断→新キーに交換。「コスト暴走時の最終遮断手段はコンソール側のキー削除」という運用知見
- Phase B最終試合は有効JSON 2/35の異常値(API障害の即時エラーと推定)。本物の完走4試合は有効JSON率100%

### サイクル7.1: Step 3.1 修正・堅牢化(9点)
コミット健忘修正/交渉中commit除去/プロンプトキャッシュ最適化/空回り削減(新規情報なし巡の自動pass)/検証モード(1試合・6R・5巡=118秒$0.08)/ハイライトログ自動生成(検出14ルール: 約束破り・内心と発言の乖離ほか)/API堅牢化(60秒タイムアウト+指数バックオフ+フォールバック)/Phase Bレポートを完走4試合から再生成/ルール要約監査(R12自動返済の記載漏れを発見・補完)。テスト73件。
- 再生成レポートの発見: 全エージェントが借入選択で1.015^12≈1.196を自力計算し利息と資金のトレードオフを論述(開幕ベンチマーク機能の確認)。game01は協調ムード、game04は「P02の提案は表面上は協力的だが実は危険」と疑心暗鬼——同一モデルでも試合ごとに信頼/警戒が分岐する多様性を確認
- 持ち越し: キャッシュヒット未確認(6R版はルール文が短く閾値未達の可能性。フル12Rで実測)

### 初日の教訓
- スペックファースト+凍結宣言が全工程の判定基準になった
- 複数AIのレビュー往復が単独では見えない穴(算術ミス・生贄トリック・同期現象)を補完した
- シミュレーションは仮説を殺すためにある(利息・傾斜・温存、3連続で直感が数字に敗北)
- LLMの不可解な行動は、まずプロンプトを疑う(初戦の「裏切り」は記憶の欠落だった)
- 総費用: 約8400試合のBotシミュ+LLM実対戦のAPI実費は数ドル

### サイクル7.2: devlog設置
開発ブログ素材となるナラティブ開発記録(本ファイル)を設置し、以後のタスク完了時に自動追記されるルールを `rules/project.md` に導入。

### サイクル8: Step 3.2調査 — 初の自発的契約提案がエンジンをクラッシュさせた
`--seed 7` の試合が272秒時点で「エラー: 'obligor'」で死亡。調査の結果: LLMが初めてcontract_proposeアクションを自発的に発行したが、プロンプトにtermsの内部構造（obligor/counterparty/ob_type等5フィールド）が一切記載されていなかったため、LLMが独自形式でtermsを構築→エンジンの`term["obligor"]`直接アクセスでKeyError。二重の不幸: (1) `llm_trial.py`のexceptがスタックトレースを握りつぶし、(2) エラーでllm_logger.save()が呼ばれずLLMの生出力（初の契約提案）が回復不能に消失。既存完走4試合を全文検索しても`"type": "contract_propose"`のアクション出力は0件——つまりseed=7が本当に「初」であり、その記録が失われた。修正方針としてP0(トレースバック保全+ログ保全)、P1(terms形式変換+プロンプト例示)、P2(試合を落とさない防御)を提案。テスト73件は非変更。

### サイクル8.1: Step 3.2修正 — 4段階の防御層を構築
P0〜P3の全修正を実装。LLMログを逐次書き込み化（クラッシュしても直前までのログが残る）。契約termsのスキーマ検証をパーサに追加し、不正なtermsはリトライで是正。game.pyの_execute_negotiation_action()をtry/exceptで囲み、アクション起因の例外をACTION_ERRORイベントとして記録しつつ試合を続行する防御層を設置。bounty_postの変換処理も追加。seed 7の検証試合は484秒で完走——DM 19件+broadcast 17件の交渉を記録。LLM同士が「弱カードで様子見」「市場配分協力」を議論する12Rフルゲームが成立した。キャッシュヒットは12Rでも0（Anthropic側のキャッシュ条件要調査）。テスト78件。コスト$0.38。

### サイクル9: Step 3.3 — キャッシュの謎を追う、そして仕様書を読ませる
キャッシュcreationすらゼロの原因を追跡。最初の仮説は「system promptが閾値(2048トークン)未達」——2251文字≈推定562〜1125トークンで閾値の半分。仕様書v0.5の情報を忠実に補完し3972文字/推定3489トークンに拡張。しかし実API検証（10000文字=3345トークンの確実に超える入力でもcreation=0）で**コード側ではなくAPI側の制約**と判明。アカウント/モデル/リージョンのいずれかがキャッシュ非対応。キャッシュなしでもコスト$0.19〜$0.38/試合で運用可能なため、cache_controlコードは将来のモデル移行に備えて残置。副次効果として、RULES_SUMMARY詳細化はLLMのルール理解を向上させる独立した価値がある（脱落3種別、Settlement 8Step、キャリーオーバー、匿名通信、報奨、口約束vs契約、全員生還不可能性の経済原理を明記）。テスト80件。

### サイクル10: Step 3C-prep — 6社12モデルを叩いて回る
全LLM戦(Step 3C)の前提として、ロスター仕様書の12モデル全てに疎通チェック。Geminiアダプタは新SDK不要——OpenAI互換エンドポイント(`generativelanguage.googleapis.com/v1beta/openai/`)を発見し、既存OpenAICompatAdapterで対応。モデルIDの仮置きを実在IDに更新する過程で2つの罠を踏む: (1) Sonnet 5がtemperature非対応+ThinkingBlock返却（extended thinking時代の仕様変更）、(2) Gemini 2.0系が全て404（2.5のみ生存）。結果: **疎通7/12、JSON成立6/12**。Anthropic(Haiku+Sonnet5)、OpenAI(GPT-4o+4o-mini)、xAI(Grok3+3-fast)の3社6モデルは完全疎通。Geminiは疎通OKだがフリーテキスト返答（JSON抽出失敗）。Moonshot 401認証エラー、DeepSeek 402残高不足——これらはコードではなくアカウント/課金の問題。総コスト$0.009。テスト82件。

## 2026-08-10: 開発2日目

### サイクル11: Step 3C-prep2 — 3つの罠を解く
前回api_check(疎通7/12)の3問題を追跡: (1) GeminiのJSON抽出失敗はfinish_reason=lengthによるtruncated responseが原因——閉じフェンス(```)がないためextract_jsonの正規表現が不一致。開始フェンスのみのフォールバック処理を追加。(2) Kimiの401はエンドポイントが違っていた——`api.moonshot.cn`(中国版)→**`api.moonshot.ai`**(国際版)が正しい。さらにtemperature=1のみ許可という制約も発見し、OpenAICompatAdapterにtemperatureフォールバックを追加。(3) モデルIDを各社APIのmodels.list()で総点検→OpenAIはgpt-4.1系、xAIはgrok-4.5/4.3が最新世代と判明（前回gpt-4o/grok-3に落ちていた）。再疎通結果: **疎通10/12、JSON成立8/12**。DeepSeekが入金完了で復活。残る未解決はGeminiのJSON安定性とKimiの思考系出力によるJSON不安定。3C推奨8体はAnthropic/OpenAI/xAI/DeepSeekの4社で確定。テスト87件。総コスト$0.019。

### サイクル12: Step 3C-prep3 — 6社12モデル完全制覇
残る4モデル(Kimi K2.6×2、Gemini Flash×2)のJSON成立を追跡。Kimiはcontentフィールドが空でreasoning_contentに本文が入るreasoning系特有の構造——max_tokens=1000ではreasoning(約300〜1000トークン)が全消費しcontentに到達しない。Geminiも同様にthinkingトークンがmax_tokensに含まれJSON途中切断。共通解決策: DEFAULT_MAX_TOKENSを1000→4000に引き上げ+reasoning_contentフォールバック。GeminiモデルIDは2.5系から3.5系に更新（gemini-3.5-flash/gemini-3.5-flash-liteが実在確認済み）。結果: **6社12モデル全て疎通OK+JSON成立（12/12）**。Kimi K2.6のJSONレイテンシは30〜43秒とreasoningに時間を要する（他社の3〜8秒と比べて遅い）が成立はする。3C推奨8体は6社カバーで選定可能に。テスト89件。総コスト$0.03。

### サイクル13: 観戦ビューア v1 — 8パネルWebUIで見る
試合の進行を目で追いたい——Phase Cの8体全LLM戦が走っている間、進捗はターミナルの1行ログだけ。観戦ビューアをFastAPI+vanilla JS（ビルドなし）で構築。既存のJSONLログを読み取り専用でパースし、8パネルグリッドに各プレイヤーの状態（生存/脱落/思考中のバッジ、strategy要約、DM/broadcast/エラー数、コスト）をリアルタイム表示。パネルクリックで時系列詳細（発言全文、strategyメモ、reasoning折りたたみ）。差分キャッシュ（mtime+sizeベース）で2秒ポーリングに耐える。127.0.0.1:8787バインド（SSHトンネルでアクセス）。検証: サーバー起動→11試合のログ列挙→trial_Cの8プレイヤー状態を正常表示→HTMLの8パネルグリッド+自動更新動作確認。テスト103件。

### サイクル14: 観戦ビューア公開対応 — TestFlight連携調査と改修
SSHトンネルではなく`dangou-card-viewer.devrelay.io`で公開するための調査と改修。TestFlightの仕組みを調査: `{appname}.devrelay.io`サブドメイン→Caddy逆プロキシ→localhost:PORT。全testflightアプリはdevrelayユーザー(PM2管理)で実行されているが、uso8mのホームは700で権限不可。判定: **案A（uso8m常駐+Caddy逆プロキシ）を推奨**。ビューアを0.0.0.0:9025にlisten、環境変数で設定可能に（HOST/PORT/ROOT_PATH/LOG_ROOT/TOKEN）。簡易認証（VIEWER_TOKEN設定時のみ?token=で照合、未設定なら公開）を実装。HTMLの静的パスを相対化してroot_path対応。curl検証: トークンなし→401、正しいトークン→200を確認。けいすけ側はCaddy設定1ファイル追加+reload。テスト106件。

### サイクル15: 観戦ビューア公開起動
`https://dangou-card-viewer.devrelay.io/` でビューアが公開稼働を開始。port 9023で127.0.0.1にbind、Caddyの逆プロキシで公開。`python -m viewer.server` の起動方法に罠があり（`__main__.py`経由だとnohopで即終了）、`-c "from viewer.server import main; main()"` 方式で解決。ローカルcurl→200、公開URL→200を確認。`/api/games` で11試合が列挙され、進行中のPhase C（8体全LLM戦、135エントリ蓄積中）も表示。恒久化（systemd/PM2）は未実装でnohop常駐のまま（サーバー再起動後は手動再開が必要）。

### サイクル16: 感情表示機能 — AIに喜怒哀楽を語らせる
strategyスキーマに`emotion`フィールドを追加し、LLMに毎思考時に6感情（喜/怒/哀/楽/焦/疑）から1つ自己申告させる仕組みを実装。列挙外・欠落は"平静"にフォールバック（感情の不在でゲーム進行を止めない設計）。ビューアの各パネルに大きな絵文字で現在の感情を表示し、直近5コールの推移を小さく並べる。タイムラインの各エントリにも感情バッジ。画像差し替え機構も準備（`viewer/static/emotions/{vendor}_{emotion}.png`→絵文字のフォールバックチェーン）。dev検証試合(Haiku×1, 6R)では全24コールでemotionが出力——主に"楽"(余裕)を維持しつつ、R1T3でのみ"焦"(危機感)に変化する微妙な感情推移を確認。テスト115件。

### サイクル17: Phase C初戦の集計完了 + 感情画像英語化

**Phase C結果（6社8モデル全LLM戦、seed 301）**: 12R完走、唯一の生還者は**P02 Claude Sonnet 5**（Cash=2,449,342円）。総コスト$11.06、総コール数1131。実行時間は**10.0時間**だが、そのうち**72%をKimi K2.6（P08）が占有**（262コール×平均99.5s、JSON率23%で大量リトライ）。Grok 4.5もコール単価29.9sで2位の時間消費。一方DeepSeek V3/GPT-4.1系は3s/コール前後と高速。

**観測された戦略パターン**: 全8体が序盤から「温存戦略」「空き巣狙い」を明示的にstrategyメモに記録。GPT-4.1(P03)はR2からブラフ言及、Gemini Flash(P05)とKimi(P08)も序盤からブラフを戦略に組み込み。Sonnet 5(P02)はR3でカウンティング言及——使用済みカードの追跡を試みた唯一のモデル。R12では「P07のSF宣言を信じてM02を避ける」「P05のTWO_PAIR宣言が嘘でRFだと読む(P07)」等、宣言と実手の乖離を前提とした複雑な読み合いが発生。

**信頼関係の構造**: GPT-4.1 Mini(P06)がGemini Flash(P05)との協力を最も頻繁に言及（21回）。逆にSonnet 5(P02)は全プレイヤーに対して警戒的言及が多く、唯一の生還に繋がった可能性。Grok 4.5(P07)はR9以降4連勝で巻き返したが、最終的に利息が追いつかず脱落。

**ハイライト**: 契約28件締結（承諾0件=全て片務提案のみ）、P01(Haiku)がR12でEntry Fee不足で破産、P05(Gemini)がCash=1,715,937円で際どい落選（200万に28万不足）。AUTO COMMIT 7件中6件がKimi（JSON率低下の影響）。

**感情画像英語化**: ビューアの感情画像ファイル名を日本語から英語に変更（`anthropic_喜.png`→`anthropic_joy.png`）。`EMOTION_EN`マッピングをJSに追加し、画像→絵文字のフォールバックチェーンをimgタグのonerrorで実装。テスト117件。

## 2026-08-10

### サイクル5.1: 実況システムv1 — ずんだもん×めたんAI実況台本生成

trial_C(6社8体戦)の完了済みログから「トレース(リプレイデータ)」を構築し、Haiku 4.5で実況・解説の掛け合い台本を自動生成するシステムを新設した。

**狙い**: 試合ログは数値と構造の羅列で人間には読みにくい。キャラクター付きの掛け合い台本に変換することで、試合の面白さ(裏切り・ブラフ・脅迫)を表面化させ、観戦体験の土台を作る。ずんだもん(公開情報のみ)とめたん(神視点)の情報非対称を制約にすることで、「視聴者が知らない裏側をめたんが暴露する」構造が自然に生まれる。

**実行**: events.jsonl + 全8プレイヤーのllm_calls.jsonl + highlights.jsonlを読み取り専用で解析し、ラウンド単位のパケット(PublicLayer/SecretLayer)に分離。重要な発見として、NEGOTIATION_ACTIONイベントにはメッセージ本文が未記録で、broadcast/DMのテキストはllm_callsのresponse_text内JSONから復元する必要があった。台本生成は1コール/ラウンド方式を採用(掛け合いの自然さ+コスト半減)。秘匿分離はプロンプトで厳格に制約+生成後に自動キーワードチェック。

**検証**: テスト136件全パス(既存133+新規3)。trial_C全12ラウンドの台本を1回の実生成で完了。APIコール12回、総コスト$0.36(上限$1に対し十分余裕)。秘匿情報混入チェック: 警告0件。R12台本ではP07(Grok 4.5)の脅迫→P05(Gemini Flash)のROYAL_FLUSH隠蔽→P07の敗退という劇的展開がめたんによって種明かしされ、ずんだもんは公開情報の範囲で驚くという分離が正しく機能した。

**発見**: 1コール方式でもプロンプトの制約が効き、ずんだもんへの秘匿情報漏洩は0件。Haiku 4.5は日本語の掛け合い台本生成に十分な品質を示し、キャラ口調(のだ/ですわ)の維持も安定していた。173セリフ・12ラウンド分を$0.36で生成でき、量産可能なコスト水準。

### サイクル5.2: 実況システムv2 — ファクトシート導入+役割違反チェック強化+レイヤー分離

v1台本レビューで発見した6箇所の品質問題(役割違反2件・計算混乱1件・尻切れ1件・矛盾1件・ハルシネーション1件)を根治するv2を構築した。

**狙い**: v1の根本原因は「AIが入力数値を自前で解釈・再計算して間違える」と「禁止語をプロンプトだけでは守れない」の2点。事前計算済みファクトシートで数値の自己矛盾を構造的に排除し、禁止語の自動検出+リトライで役割違反を機械的に是正する。加えてプロンプトをキャラ層(ゲーム非依存)とゲーム層(談合カード固有)に分離し、第2ゲーム以降での再利用性を確保する。

**実行**: trace.pyにPublicLayer.factsheetフィールドを追加。_build_factsheet()が市場結果サマリ・決算後残高・利息情報・破産イベント・生還判定(不足額計算付き)を自然言語で事前生成。prompts.pyを新設しCHARACTER_BLOCK(ゲーム非依存)+GAME_KNOWLEDGE_BLOCK(談合カード固有)の3ブロック構成に再編。commentary.pyにZUNDAMON_BANNED_WORDS(15語)によるチェック+是正リトライ(1R1回・全体最大3回)、尻切れ検出、max_tokens 2000→4000を実装。

**v1誤り照合結果**:
- R5/R3 役割違反(ずんだもん「内心メモ」参照): v2ではR2/R3/R5で禁止語違反を検出し再生成。3件中3件が再生成で解消
- R7 カード混乱(「STRAIGHTではなくSTRAIGHT」): v2では市場結果サマリのファクトシートを引用しカード名の自己矛盾なし
- R10 尻切れ: v2ではmax_tokens=4000で14セリフ完走
- R11 矛盾(「誰も得られず…均等配分」): v2では論理的に一貫した記述
- R12 P05ハルシネーション: v2ではファクトシートに「現金1,715,937円 < 2,000,000円(284,063円不足) → 未達」が事前記載され、めたんがこれを正確に引用。「システムエラー」発言は完全消滅

**検証**: テスト154件全パス(既存133+v1新規3+v2新規18)。trial_C全12ラウンドv2生成完了。APIコール15回(うちリトライ3回)、総コスト$0.49。禁止語違反検出: 5件(R2/R3/R5で再生成成功、R8は上限到達で残存1件)。v1台本はcommentary/に、v2台本はcommentary/v2/に共存保存。

**発見**: ファクトシートが最も効果的だった箇所はR12の生還判定。v1では「ラインに達しているように見える…システムエラーか」というハルシネーションが、v2では正確な不足額引用に変わった。禁止語チェックは12R中4Rで違反を検出(33%)——プロンプト制約だけでは3分の1のラウンドで役割違反が発生していたことがわかり、機械的チェック+リトライの有効性が実証された。コスト増は$0.13(+36%)で、品質改善に対して十分に合理的。

### サイクル5.3: ビューア実況表示 — ずんだもん&めたん実況席をUIに追加

生成済みの実況台本(commentary v2/v1)を観戦ビューアに表示する実況席エリアを追加した。

**狙い**: 台本はJSONL/Markdownファイルとして存在するが、観戦体験としてはビューアの8パネルグリッドと一緒に見えるべき。キャラ立ち絵枠+吹き出し形式の掛け合い表示で、試合の流れと実況解説を一画面で把握できるようにする。

**実行**: `GET /api/games/{trial}/{game}/commentary` エンドポイントを追加(v2優先・v1フォールバック・404)。既存のLogCache差分キャッシュを再利用。UIはメイン画面のグリッドとフッタの間に実況席エリアを追加: 左にずんだもん立ち絵枠、右にめたん立ち絵枠、中央にセリフ表示(ずんだもん=左寄せ緑系吹き出し、めたん=右寄せ紫系吹き出し)。ラウンドセレクタ(前後ボタン)と自動再生(4秒間隔で1セリフずつ表示+話者強調)を実装。台本のない試合では実況席を非表示。ヘッダに実況トグル追加。CSS変数11個(--commentary-*)で色・サイズ・高さ等をユーザー調整可能にした。

**検証**: テスト158件全パス(既存154+commentary API 4件)。trial_Cでcurl確認しv2台本の12ラウンド分が正常に返る。ビューアをport 9023で再起動し、dangou-card-viewer.devrelay.io から実況席が表示される状態を確認。

### サイクル5.4: 感情マスコット画像の表示調査

スマホ表示で感情が絵文字(😐)のままとの報告を受け調査。結果: **仕様どおりの動作**と確定。感情画像36枚(6ベンダー×6感情: anger/doubt/ease/joy/panic/sadness)は正しく`viewer/static/emotions/`に配置されており、HTTPでも正常配信(200)。しかし「平静(neutral)」用の画像が0枚であり、trial_Cは感情フィールドがnullで記録された試合のため全員が`_extract_emotion()`のフォールバックで「平静」扱い。フロントは`{vendor}_neutral.png`→`neutral.png`→絵文字😐のフォールバックチェーンを正しく実行していた。

続けて、neutral専用画像は作成しない方針としeaseで代用表示するよう`emotionDisplay()`のフォールバックチェーンを拡張: `{vendor}_neutral.png`→`{vendor}_ease.png`→`neutral.png`→`ease.png`→絵文字😐。neutral以外の感情は従来どおり2段。これによりtrial_C(全員平静)でも各社のease画像が表示される。テスト158件全パス。

**追補(真因訂正)**: 上記フォールバック拡張後も画像が表示されないとの再報告を受け再調査。**真の原因は感情画像のURLパスバグ**と判明。`emotionDisplay()`は画像を`emotions/...`と参照していたが、ページは`/`配信のため`/emotions/...`に解決される一方、静的ファイルは`app.mount("/static", ...)`により`/static/emotions/...`に配信されていた。結果、neutralに限らず**全ての感情画像が404**し常に絵文字へフォールバックしていた（→サイクル5.4の「仕様どおり」判定は誤診）。キャラ画像(zundamon/metan)が表示できていたのは`static/characters/...`と正しく指定されていたため。修正: `emotionDisplay()`内の4つの画像パステンプレートすべてに`static/`プレフィックスを付与。curlで`/static/emotions/anthropic_ease.png`=200、`/emotions/...`=404を確認。テスト158件全パス。

**追補2(ちらつき対策)**: 画像表示後、自動更新(2秒毎)のたびに感情画像が激しくちらつくとの報告。原因は`renderState()`が毎回`grid.innerHTML=''`で全パネルを破棄し、約48枚の`<img>`を作り直して画像を再読込していたこと。単純な状態比較では揮発フィールド`last_active_sec_ago`(`log_parser.py`で毎秒増加)により毎回差分ありと判定され無効なため、**表示に関係する安定フィールドのみから署名を作り、無変化なら再描画をスキップ**する方式(`computeStateSig()`+`renderState`先頭の早期return)を実装。`last_active_sec_ago`は署名から除外し、そこから導出した`isThinking`真偽値のみ含める。試合切替時は`lastRenderSig=null`でリセットし初回描画を保証。完了済み試合ではちらつき完全消失、ライブ試合でも実変化時のみ再描画。テスト158件全パス。

**追補3(パネルUI改善)**: 観戦者に分かりやすい見せ方へパネルを再設計。要望5点を実装: ①感情画像を左に大きく(56px, `.emotion-big`) ②モデル名を大きく表示(seat_map `"M1:Claude Sonnet 5"`を`:`で分割し`splitModelName()`でモデル名を`.model-large`、`P02/M1/バッジ`を`.panel-sub`に小さく) ③感情履歴5個(`.emotion-history`)を削除 ④所持金を大きく表示(`¥2,457,776`, `.cash-large`) ⑤最終発言を追加(`💬 ...`, `.last-utterance`)。バックエンド`log_parser.py`の`get_game_state()`に2フィールド追加: `cash`(=`SNAPSHOT`イベント`data.snapshots[pid].cash`の最新値。脱落者は直近記録値)、`last_message`(=各プレイヤー最後のbroadcast発言120字)。`computeStateSig()`にも`cash`/`last_message`を反映。API・フロント双方で動作確認済み。テスト158件全パス。

**追補4(ダーク/ライトテーマ切替)**: `style.css`全体を**CSSカスタムプロパティ化**し、`:root`にダーク既定値、`[data-theme="light"]`にライト上書きを定義（構造色約20変数＋実況席の`--commentary-*`）。ハードコード色を`var(--...)`へ置換（ステータスバッジ緑/赤/橙は自前背景で両テーマ可読のため据え置き）。`index.html`は①`<head>`に描画前テーマ適用スクリプト(FOUC防止)②ヘッダに🌙/☀️トグルボタン③`applyTheme()`でクリック切替＋`localStorage('viewer-theme')`永続化。既定ダーク、リロード後も選択維持。バックエンド変更なし。テスト158件全パス。

**追補5(テーマ導入後の3不具合修正)**: ①ライトモードで実況席だけ暗いまま→原因は`style.css`実況席セクションの**2つ目の`:root`**が`--commentary-*`をダーク値で再定義しており、`[data-theme="light"]`より後方かつ同一詳細度のため後勝ちしていた。ライトセレクタを`html[data-theme="light"]`に変更し詳細度を(0,1,1)へ上げて解決。②実況で▶次ラウンドにしてもすぐR1に戻る/③再生を押してもすぐ終わる→**同一原因**。自動更新(2秒毎)でラップされた`loadState`が毎回`loadCommentary()`を呼び、その中で無条件に`commentaryRound=1`と`stopCommentaryPlay()`を実行していた。`commentaryLoadedKey`を導入し、ロード済みゲームでは`loadCommentary()`を早期returnさせる(成功時のみkey設定、404時は未設定で再試行許容)。ゲーム切替時はkey変化で自動再ロード。テスト158件全パス。

**追補6(ルール解説セクション追加)**: 観戦者がAIの挙動を理解できるよう、ビューア下部に常設の「📖 ルール解説」セクションを追加。ゲームの流れ／はじめの借入額（120万〜1000万・利息1.5%複利/R・公開）／生存条件（12R完走＋借金0＋現金○万以上）／使えるカード（役カード12枚: ROYAL FLUSH〜HIGH CARD、1R1枚使用・消滅・譲渡不可）／公正証書（発行料10万、型A金銭・型B行動、違反即脱落）／Entry Fee・賞金・匿名通信の6カードを`<details open>`で表示。**構造ルールは凍結仕様の確定値を静的HTMLにハードコード、試合依存の数値（生存現金・利息・人数・Entry Fee・総賞金）は`GAME_START`イベントのconfigから動的反映**するハイブリッド方式。表示対象試合は`baseline_v1`生成で**生存現金は仕様暫定300万でなく実運用200万**（LLMにも同値が伝達済み）のため、`log_parser.get_game_state()`が返すstateに`config`フィールドを追加し（`GAME_START.data.config`を格納）、フロントの`fillRules()`が初回stateで数値を一度だけ上書き（`rulesFilled`フラグ、署名計算には非含有）。CSSは既存テーマ変数を用い両テーマ対応（`.rules`系）。バックエンド追加は非破壊でテスト158件全パス。curlで`/state`にconfig(survival_cash:2000000等)、HTML`rules-section`、CSS`.rules-card`を確認。

**追補7(ラウンド状況ビューア追加)**: 実況再生中に盤面を確認したいという要望に対応し、実況席の下に「📊 ラウンド状況」セクションを新設。新API `GET /rounds`（`log_parser.get_round_states()`）が`{game}_events.jsonl`＋LLMログからラウンド別の構造化盤面を再構成: ①市場の賞金(`MARKET_OPEN`＋`MARKET_RESULT`で勝者/分配額をmerge) ②各プレイヤーの所持金・借金(`SNAPSHOT`の`cash`/`free_cash`から**借金=cash−free_cash**を導出) ③提出カード(`REVEAL.commits`、rank付き。`COMMIT`由来の重複はREVEALで置換) ④手札(全員固定12枚−累積提出) ⑤公正証書(`NEGOTIATION_ACTION`のcontract_propose/signから提案者・当事者・署名状況を累積構築、条項termsはLLMログの`contract_propose`アクションからproposer+round一致でbest-effort付与) ⑥誰が誰にメッセージ(eventsは本文/宛先を持たないためLLMログの`_extract_action`でdm/broadcastを収集、dmは`to`付き) ⑦脱落(`SURVIVAL_CHECK/BANKRUPTCY/FORCED_LIQUIDATION`)。加えて資産ランキング(cash降順)・生還条件充足マーク(借金0かつcash≥survival_cash)・カード強さ表示等の付加情報。フロントは`situationData`をゲーム毎1回ロード(進行中はラウンド増で再取得)、実況の▶◀でラウンド移動すると状況も追従(`setSituationRound`)、状況側の独自◀▶でも単独移動、ヘッダ「状況」トグルで表示切替。CSSは既存テーマ変数で両テーマ対応(`.situation`/`.sit-card`/`.card-chip`等)。`tests/test_viewer.py`に受け入れ1件追加(rounds非空・R1主要キー・debt=cash−free_cash検証)。テスト159件全パス。curlで`/rounds`のmarkets3/cash8/msgs20、契約(型A/型B terms)を確認。

**追補8(公正証書の締結条件表示)**: 「公正証書には締結条件があるはずで表示したい／何ラウンドの話かも」という要望に対応。締結条件データは既に`/rounds`の各契約`terms`（LLMログ`contract_propose`の義務リスト、要素=`{obligor,counterparty,ob_type,round_num,details}`）としてフロントに届いていたが、`renderSituation()`の公正証書ブロックが`ob_type`を素チップで出すだけだったため、**フロントのみで人間可読な条件文に描画**（バックエンド/API/テスト不変更）。`fmtObligation(t)`ヘルパを追加し義務種別ごとに整形: 型A `type_a_payment`→「💴 obligor→counterparty ¥金額 支払」、型B `type_b_market`→「🎯 …は市場に参加」/`type_b_no_market`→「🚫 …は市場に不参加」/`type_b_card`→「🃏 …はランクを使用」（`card_rank`は`CARD_INFO`で整形、`market_id`はそのまま）。ラウンド情報は契約見出しに`R{round_created}提案`、各義務に`R{round_num}履行`を明示（義務ごとに履行ラウンドが異なり得るため義務単位）。署名状況も`◯/◯署名 ✅成立／…署名待ち`を表示。型A/型Bは枠線色タグ（`--cash`/`--action-type`）で区別し両テーマ可読。`terms`未記録の契約は注記表示（best-effort踏襲）。実データ(trial_C)で4種のob_type全出現・条件付き契約214件を確認、viewerテスト24件パス。

### サイクル5.5: 7感情目「奸」(ニヤリ)の追加

**狙い**: けいすけが6キャラの「ニヤリ(悪い顔)」立ち絵を描き上げアップロード。策略・裏切りこそがこのゲームの核なのに、これまでの6感情(喜怒哀楽焦疑)には"してやったり"の顔がなかった。アップロードした smirk 絵を、ブログカードとゲーム内感情の両方で実際に使われる状態にする。

**発見**: 表情には独立した2系統があった。①ブログカードは`_face_uri`がファイル名を自由文字列で拾うだけ(ホワイトリスト無し)＝`smirk`は置いた時点で`--card-emotion smirk`で使えたが、`scripts/blog.py`が全カードに既定`ease`を一律で渡すため、策略テーマの LIE DETECTED/運命の一手でも既定では悪い顔が出なかった。②ゲーム内感情はLLMが`VALID_EMOTIONS`(6値)から選ぶホワイトリスト制で、ビューアのJP→EN変換にも smirk が無く、AIは smirk を選べずビューアも出せなかった。

**実行**: (A)カード側は`--card-emotion`既定を`None`化し、未指定時に lie/showdown=smirk、他=ease へ種別分岐(明示指定は最優先)。(B)ゲーム側は`VALID_EMOTIONS`に「奸」を追加、プロンプト選択肢に`"奸"(策略・ニヤリ・してやったり)`を提示、ビューアの`EMOTION_EN`に`'奸':'smirk'`・`EMOTION_EMOJI`に`😏`を追加。日本語表記は「奸」(よこしま・悪だくみ)を採用。ドキュメント(SKILL/brand/README/changelog)とテスト(EMOTION_EN検証を8感情へ、プロンプトに"奸"検証)も同期。

**検証**: 過去ログ・既存6感情は不変(追加のみ)。以後の新規対局でAIが策略時に「奸」を表明でき、ビューアが各社の smirk 絵＋😏 を表示する。

## 2026-08-13

### サイクル6.0: S2課題1 — Kimi長考問題の根治

**狙い**: trial_CでKimi K2.6がJSON有効率23%・平均レイテンシ99.5s・コスト$5.01(試合全体の45%)・時間占有率72%のボトルネック。根本原因を特定し、JSON率90%以上・レイテンシ60s以下・コスト$1.5/game以下を達成する。

**発見**: 根本原因は**reasoning(thinking)トークンがmax_tokens=4000を食い尽くす**こと。Kimi K2.6のthinkingモードではreasoning_contentとcontentがmax_tokensを共有し、4000トークンの大半を内部推論に消費→JSON回答が空/切断。Moonshot公式APIで`"thinking":{"type":"disabled"}`が使える。openai SDKでは`extra_body`経由で送信(直接kwargsは拒否)。K3($15/MTok)はコスト不達で除外。

**実行**: ModelInfoに`max_tokens`+`extra_params`追加、M5/L5に`extra_params={"thinking":{"type":"disabled"}}`設定、OpenAICompatAdapterで`extra_body`マージ+finish_reasonキャプチャ、llm_agentでper-model max_tokens+finish_reason=lengthリトライ、llm_trial.pyに`--model`引数追加。

**検証**: 調査12コール($0.084)でthinking=disabled=6/6=100%JSON・8.9s平均。検証試合(6R dev,M5×1+Random×7,35コール): **JSON率97%(34/35)・平均レイテンシ8.1s・コスト$0.15(12R換算$0.31)・全コールfinish_reason=stop・AUTO COMMIT 0・282秒**。テスト176件パス。

### サイクル6.3: LP修正 — 参加体数・OGP・ブログタイル動的化 (2026-08-13 07:14 JST)

**狙い**: 公開LP(`viewer/static/lp.html`)の事実誤認(20体→8体)修正、OGP画像のSNSクローラー対応(相対→絶対URL)、ブログ記事タイルの未公開記事表示問題の解消。

**実行**: (1) 参加体数「AI 20体」→「AI 8体」を6箇所修正(title/meta description/og:description/kicker/hero sub/Overviewカード)。(2) og:imageを`https://dangou-card-viewer.devrelay.io/static/lp_hero.png`に絶対URL化、og:urlを新設。lp_hero.pngは2400x1260pxでOGP要件(1200x630)を満たす。(3) ハードコード4タイル(未公開のソネット/総括含む)→静的フォールバック2件(公開済みの開幕+Grok、個別記事URL、aria-label付き)+JS fetch(`pixblog.net/u/uso8m/feed.json?limit=4`)で動的差替え。fetch失敗時は静的2件がそのまま残る。CSSグリッドを`auto-fill,minmax(240px,1fr)`に変更し件数可変に自然対応。

**発見**: lp_hero.pngの解像度は2400x1260pxでOGP推奨の1200x630を上回っていた。pixblog feedは現在3件公開(開幕/Grok/500万借金)、CORS許可・published_at降順。

| 指標 | Before | After | 目標 |
|---|---|---|---|
| JSON率 | 23% | **97%** | 90%+ |
| レイテンシ | 99.5s | **8.1s** | 60s以下 |
| コスト/game | $5.01 | **$0.31** | $1.5以下 |

### サイクル6.1: 観戦ビューアURLに LP（ランディングページ）を新設

**狙い**: 公開URL `https://dangou-card-viewer.devrelay.io/` を開くといきなり観戦UIが出ていたのを、まず作品の入口=LPに差し替え、「観戦する」と「ブログを読む」の2導線を作る。

**実行**: 観戦UI(index.html)が `fetch('api/games')` 等の相対パス依存と判明したため、観戦本体を **`/watch`（末尾スラッシュ無し）** へ移し、`/` は新規 `viewer/static/lp.html`（自己完結・ダークトーン、ヒーロー＋概要3カード＋ブログ記事ピックアップ＋OGP）を返すよう `server.py` を変更。CTAは「▶観戦する」→`/watch`、「📖敗者の手記を読む」→PixBlogハブ `pixblog.net/u/uso8m/`（下書き記事の404回避のため個別記事でなくハブに集約）。ヒーロー画像は既存タイトルバナーを `lp_hero.png` に流用。観戦ヘッダに🏠トップリンクを追加。

**検証**: 新規 `tests/test_viewer_lp.py`（`/`=LP・`/watch`=観戦UI・`/api/games`=200・`/watch`はリダイレクト無しで200 の4件）追加。全体172件パス。サーバ再起動後、`/`(LP)・`/watch`(観戦)・`/api/games`(200) を実地確認。

**発見**: FastAPIで相対パス資産の子ページを配る場合、末尾スラッシュの有無でブラウザのベースURL解決が変わり `api/...` が `/watch/api` に化ける。`/watch`（スラッシュ無し・リダイレクト非設置）に固定することで観戦UIを無改修のまま流用できた。